# Research-to-Implementation Gap Analysis

## Reconciled Current Status (2026-07-11)

## Phase 68 - Enforced Universal Task Workflow (implemented; lifecycle/bypass/host/visual gates PASS)

Reconciled gap: Forge already had a five-task agent loop, firewall stages, artifacts, reviewer gates, evidence, and terminal AAR, but the user's universal `CLASSIFY -> PLAN -> BASELINE -> RECONCILE -> DOCUMENT PLAN -> IMPLEMENT -> VALIDATE -> REVIEW -> DOCUMENT CLOSE -> AAR` workflow existed only as prose. Task status could be manually advanced without a durable baseline/acceptance contract, and no deterministic gate prevented an otherwise role-valid mutation from skipping reconciliation and planning.

Implemented result: `workflowGovernance.ts` now creates a required versioned state object for every run. Conservative deterministic lane classification selects Light only for documentation/text-only goals and defaults ambiguous or behavioral work to Full. Initialization captures a bounded baseline (workspace, version, Git revision/status, file count, prior Forge state, rollback method), compiles a structured acceptance contract, and marks classify/plan/baseline complete before model work. Full runs require accepted exploration before plan documentation; Light runs use the generated bounded one-line plan. Architect plans are deterministically augmented with Acceptance Contract, Validation, Negative Path, and Rollback sections when weak models omit them.

The workflow validator runs between role validation and the generic firewall. File/command implementation is rejected until baseline, reconcile, and documented plan are complete; evidence is rejected before validation; success is rejected until baseline, reconcile, plan, implementation/no-op proof, validation, review, and close are complete. Accepted tool events, automatic post-mutation oracles, no-change diff reviews, harness-authored green evidence, and terminal AAR advance only their corresponding stages. Workflow violations persist and enter the blocker/reflection system as `workflow_gate`; task-graph manipulation alone cannot bypass them. A successful run becomes `VERIFIED` only after AAR and all 11 stages complete.

Evidence persists in `.forge/workflow-governance.json` and human-readable `.forge/workflow-task-record.md`; both open in native editors. The required model prompt carries lane, current stage, stage statuses, acceptance criteria, and validation contract. Compact footer telemetry renders `flow stage/blocks` without another panel. Behavioral proof covers Full/Light/ambiguous classification, “fix README typo” Light classification, early mutation rejection with unchanged bytes, early evidence rejection, plan section completion, full stage lifecycle, blocker taxonomy, compatibility with layered firewall/network tests, no-op implementation proof, successful product rate split, and terminal `VERIFIED` with 11/11 completed stages. Inspected persisted fixture evidence records zero violations and all stage evidence; visual proof shows `flow review/0`.

Compile, smoke, 100/100 worker stress, extension-host E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.68.0`. The VSIX contains both compiled `workflowGovernance.js` and `UNIVERSAL_AI_TASK_WORKFLOW.md`. Honest boundary: the universal governance core is enforced, but project-specific adapter commands and live-domain gates still come from repository knowledge/configuration rather than a typed adapter registry; OS/socket containment and live eval gaps remain separate.

## Phase 67 - Verified Procedural Skill Memory (implemented; causal banking/retrieval/host/visual gates PASS, live uplift pending)

Reconciled gap: `HarnessState.skills` and `.forge/skill-registry.json` existed structurally but were always initialized to an empty array. AAR banked vague lessons from failures, yet Forge could not convert a verified successful recovery into an executable workflow or retrieve that workflow for a later related run. The research requirement for a skill registry was therefore presence-only.

Implemented result: pure deterministic `bankProceduralSkills` and `selectProceduralSkills` own the registry. Only terminal `success` with concrete recovery signals can bank a procedure; failed/gave-up runs and clean first-pass successes bank none. Templates cover rejected proposal repair, red-oracle repair, reviewer rejection, and bounded escalation. Each skill records category, exact workflow, trigger tokens, confidence, occurrence and verified-success counts, source session IDs, use count, applied-session IDs, timestamps, and last use. Repeated verified recoveries strengthen the same language/category skill ID rather than duplicating it. Retrieval scores current goal/task tokens plus deterministic blocker categories, selects at most three, deduplicates use accounting within one session, and injects a bounded `procedural-skills` prompt section with confidence and verified-run provenance. The model cannot author or mark a skill successful.

Causal proof establishes four negative/positive boundaries: failed recovery banks zero skills; clean success invents none; repeated verified TypeScript proposal recovery strengthens one stable procedure; unrelated CSS explanation retrieves none. Product-loop proof loads a prior registry from disk, injects “Copy the SEARCH block verbatim” into a later matching worker prompt, records application provenance, and opens the registry through the native artifact command. Existing reflection A/B fixtures now prove a real reflection-on terminal success banks a skill while the reflection-off failed lane persists an empty registry. Oracle procedures require actual oracle-reflection evidence rather than any generic reflection.

Compile, smoke, 100/100 worker stress, extension-host E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.67.0`; inspected visual proof shows `skill 1/2` in the existing compact footer. This proves skill lifecycle mechanics and causal prompt delivery, not live-model solve-rate uplift; a live or held-out A/B is still required before claiming quality improvement.

## Phase 66 - Transactional Multi-File Command Execution (implemented; copy/worktree/conflict/rollback/host/visual gates PASS)

Reconciled gap: Phase 65 isolated explicit file-edit tools, but `run_command` still executed in the active workspace and only recorded a before/after effect ledger. A command could partially modify many files, fail after mutation, overwrite concurrent user changes, or touch shared Git metadata from a detached worktree.

Implemented result: validated `run_command` proposals now execute through `TransactionalCommandExecutor` in a detached temporary worktree for Git roots or a full filtered workspace copy for non-Git roots. Current dirty source files overlay the Git worktree before execution. Forge computes the complete staged created/modified/deleted set, rejects symlinks, caps transactions at 200 changed files and 20 MiB, checks every changed source path against its pre-execution SHA-256/size state before any merge, and applies the set only after all paths pass. A merge failure restores every changed path from byte backups. Failed commands never merge staged effects. Explicit command references to the active workspace root are rejected before worker launch, and command policy now blocks Git index/history/config/worktree mutations while retaining read-only status/diff/log inspection.

`.forge/worker-command-transactions.json` records command, role, staging mode, base commit, changed/created/modified/deleted paths, merged files/bytes, conflict, rollback, cleanup, PID, duration, and error. `.forge/command-effects.json` links each committed effect entry to its transaction ID/mode. Native artifact opening and compact `txn edit+command/conflicts` telemetry exist.

Behavioral proof covers a three-path copy transaction (create+modify+delete), failed command non-merge, explicit source-root rejection before worker launch, concurrent user-edit conflict with zero merged paths, injected second-file merge failure with successful first-file rollback, dirty Git-worktree two-file execution, worktree registry cleanup, shared Git mutation rejection, persisted main-loop linkage, and artifact opening. Compile, smoke, 100/100 worker stress, extension-host E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.66.0`; inspected visual proof shows `txn 1+1/0`.

Honest boundary: this controls cwd-relative command effects and rejects explicit active-root paths, but it is not kernel filesystem containment. A process exploiting an unknown absolute path, OS facility, child process, or unclassified socket remains outside this guarantee. Excluded dependency/build directories are not copied into command staging; commands requiring them should use dedicated verification tools or require a future container/sandbox implementation.

## Phase 65 - Transactional Role File Edits (implemented; worktree/sparse/conflict/host/visual gates PASS)

Reconciled gap: Phase 62 moved validated tools into role-tagged child processes, but `apply_patch` and `write_file` still mutated the active workspace from those processes. A stale proposal could overwrite user edits made between validation and commit, and the role boundary had no isolated filesystem transaction or merge evidence.

Implemented result: validated file mutations now execute through `TransactionalEditExecutor`. Git-backed workspaces create a detached temporary worktree at `HEAD`, overlay the current target bytes so dirty user state is the actual patch base, execute the role-tagged worker there, compare the active target's SHA-256 before merge, and copy back only the validated target when the source hash is unchanged. Non-git workspaces use an explicit sparse staging directory with the same optimistic concurrency check. Temporary worktrees/staging roots are removed before dispatch resolves. `.forge/worker-edit-transactions.json` records transaction ID, role, proposal, target, mode, source/staged hashes, base commit, worker PID, conflict/commit/cleanup outcome, duration, and error; native artifact opening and compact `txn transactions/conflicts` telemetry exist.

Negative proof changes the active target from a fake worker after staging but before merge; Forge rejects the transaction and preserves the concurrent user bytes. Git proof starts from committed baseline plus dirty target bytes, applies a patch that only matches the dirty overlay, merges the result, and proves `git worktree list --porcelain` is identical before and after. Non-git proof commits through sparse staging. Compile, smoke, 100/100 worker lifecycle stress, extension-host E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.65.0`; inspected visual proof shows `txn 1/0`.

Phase-close boundary: Phase 65 covered one target file per `apply_patch`/`write_file` proposal and did not cover commands; Phase 66 above supersedes that command limitation with bounded multi-file staging. Forge still lacks persistent autonomous role worktrees, deterministic cross-worker branch merge, bounded parallel workers, socket containment, and OS/container sandboxing.

## Phase 64 - Opt-In Hybrid Semantic Retrieval (implemented; mocked causal/cache/fallback gates PASS, live uplift pending credential/opt-in)

Reconciled gap: `.forge/retrieval-index.json` ranked files by path/content token overlap, remembered-file boost, and file type. It could not retrieve conceptually related code when the goal and source used different vocabulary, and Phase 8 still lacked an embedding path, vector cache, cosine ranking, or semantic provenance.

Implemented result: `EmbeddingProvider` abstraction plus `OpenRouterEmbeddingProvider` uses the official batched `POST /api/v1/embeddings` contract, default model `openai/text-embedding-3-small`, normalized vectors, cosine similarity, 32-input batches, and content/model/path-addressed `.forge/embedding-cache.json`. External source embedding is explicitly opt-in through `forge.semanticRetrievalEnabled` (default false) with configurable `forge.embeddingModel`; missing credentials, disabled mode, malformed response, or provider errors leave deterministic retrieval active. Before prompt assembly, up to 80 bounded retrievable documents are embedded/cached and semantic scores blend with deterministic ranking. Candidate `source`, cosine score, model/provider/status, cache hits, embedded document count, query, and errors persist in `.forge/semantic-retrieval.json` and retrieval candidates; native artifact opening and compact `sem refreshes/failures` telemetry exist.

Causal proof uses a mock vector provider where “repair login flow” and credential verification share vectors despite no lexical overlap; `src/auth.ts` ranks first and the model prompt contains `semantic cosine` provenance. Repeating identical query/documents makes zero new embedding calls and reports three cache hits. A throwing provider records `status: failed`, increments semantic failures, and retains deterministic candidates without failing the agent run. Validation exposed an existing contamination bug: root `.forge` was not excluded because only `.forge/` descendants matched, and harness-owned root artifacts repeated the goal. Retrieval now excludes the `.forge` directory plus `PLAN.md`, `SCRATCHPAD.md`, `todos.json`, and `evidence_ledger.json`; those artifacts already enter prompts through dedicated state sections. Compile, smoke, worker stress, E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.64.0`; inspected visual proof shows `sem 1/0`. No live source text was sent during this phase.

## Phase 63 - Deterministic Blocker Taxonomy (implemented; local behavioral/visual gates PASS)

Reconciled gap: provider/schema/firewall/review/tool/oracle/budget/no-progress failures were recorded across logs, reflections, AAR counters, and prose halt reasons, but Forge had no durable blocker object or category-specific retry policy. A weak model therefore received generic failure prose and the user could not distinguish active, resolved, or terminal blockers machine-readably.

Implemented result: deterministic `classifyBlocker` maps failures to provider, schema, role capability, workspace scope, command policy, network policy, patch format, patch applicability, generic firewall, pre-commit review, worker process, tool failure, oracle, budget, no-progress, or step-cap categories. `.forge/blockers.json` records source, category, open/resolved/terminal lifecycle, retryability, task/role ownership, occurrence count, timestamps, summary, deterministic suggested action, and resolution. Repeated same-category failures on one role/task update one object; corrected validation/tool/oracle outcomes resolve matching blockers; terminal failure/give-up freezes unresolved blockers as terminal; success closes remaining goal-level blockers with an explicit resolution. Open blockers are required bounded prompt context. Run stats expose event/unresolved/resolved counts; compact footer renders `blk unresolved/events`; terminal blocker counts/categories feed deterministic AAR and lesson banking.

Behavioral proof distinguishes role, network, patch-applicability, and non-retryable budget blockers; proves role/network rejections persist open during bounded reflection; proves cost halt terminalizes a budget blocker; verifies native artifact opening; and confirms successful runs close blockers without erasing occurrence history. While validating this phase, the new worker-process category exposed an actual IPC shutdown race from Phase 62. A failing-first stress script proved `dispatch()` returned while its PID remained alive and locked the fixture. Parent-owned disconnect/exit handling replaced timing and ACK schemes; `npm run test:workers` now proves 100/100 sequential workers exit before dispatch resolves, and final E2E state records zero worker-process failures. Compile, smoke, worker stress, E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.63.0`; inspected visual proof shows `blk 1/3` in the compact footer.

## Phase 62 - Process-Isolated Workspace Tools (implemented; local behavioral/visual gates PASS, container isolation still missing)

Reconciled gap: Phase 61 enforced role tool scopes and provider session identity, but all workspace tools still executed inside the extension-host process. A tool crash, blocked command, or filesystem operation therefore shared the host's process and inherited environment boundary.

Implemented result: after parent-owned PROPOSE and VALIDATE, workspace reads/searches/edits/tests/diffs/commands execute through `ProcessWorkerExecutor` in a short-lived role-tagged child process. The child receives only workspace root, role, and the validated proposal over IPC; its inherited environment is allowlisted before launch, with blocked key names recorded. It returns the structured tool result and PID, then exits. Parent state/oracles/reflection remain authoritative. Worker timeout, startup failure, early exit, and tool failure return failed tool results and enter the existing reflection/halt path. `.forge/worker-contexts.json` now records process executions/failures, last PID/duration, and blocked environment keys per role; run stats expose `workerProcessExecutions`/`workerProcessFailures`, rendered compactly as `proc executions/failures`.

Behavioral proof writes a fixture file through an Editor child whose PID differs from the extension host, strips `FORGE_WORKER_SECRET`, proves a descendant command prints `false` for that key, and propagates an unknown-tool failure without false success. Full harness E2E terminaled success with five process executions across external role PIDs. The first integration run found that upstream-stripped keys vanished from the inner command-sandbox ledger; the fix unions worker-boundary denials into command metadata, preserving complete transitive environment evidence. Compile, smoke, E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.62.0`; inspected visual proof shows `proc 4/0` in the compact footer. This is real process isolation for tool execution, not a container, kernel sandbox, persistent autonomous worker, or parallel role execution.

## Phase 61 - Enforced Role Worker Boundaries (implemented; local behavioral/visual gates PASS, process isolation still missing)

Reconciled gap: `.forge/role-handoffs.json` listed allowed tools for Explorer, Architect, Editor, Reviewer, and Escalation, but those lists were prompt guidance only. Every role also reused the run-level provider `session_id`. A Reviewer could propose file mutation, an Editor could propose success/evidence tools, and the deterministic firewall would evaluate those proposals without enforcing role ownership.

Implemented result: the typed role capability matrix now runs before generic schema/scope/command/patch validation. Cross-role proposals fail with `[role_capability_blocked]`, increment `roleCapabilityBlocks`, enter the normal bounded reflection path, and never reach COMMIT. Editor may search/read/edit and run tests diagnostically, but cannot inspect/approve diffs, record evidence, or declare success. Reviewer may run verification commands, inspect diffs, record evidence, and declare success, but cannot directly edit files. Architect and Explorer remain plan-only/read-only respectively. Distinct stable provider sessions use `<run>:worker:<role>`, and `.forge/worker-contexts.json` persists each role's session, allowed tools, provider-call count, accepted/rejected proposal counts, recent tools, and last task. The artifact opens in the native editor. Compact UI telemetry adds `perm <blocked>` to the existing footer.

Behavioral proof covers Editor `declare_success` denial, Reviewer `apply_patch` denial before filesystem mutation, distinct Editor/Reviewer provider session IDs, persisted rejection evidence, and the legitimate no-op Editor path: `run_tests` may complete the edit phase only when green, while Reviewer still owns diff/evidence/success. Full extension-host regression passed after network and command-effect fixtures were assigned to Reviewer so each test continues to isolate its intended validation layer. Compile, smoke, E2E, visual, package, and Antigravity install/list gates pass for `kennyg.forge-agent@0.61.0`; inspected visual proof shows `perm 1` in the compact footer. This is context/tool isolation, not process isolation: roles still share one harness process, state graph, and workspace.

## Phase 60 - Outbound Network Intent Governance (implemented; local behavioral/visual gates PASS, OS containment still missing)

Reconciled gap: command execution already sanitized inherited environment variables and captured filesystem side effects, but the firewall did not distinguish a read-only download from an outbound mutation such as `git push`, `npm publish`, an HTTP upload, remote file transfer, cluster mutation, or infrastructure apply. The command ledger also contained no network intent or endpoint evidence.

Implemented result: the pure `classifyCommandNetworkIntent` policy is shared by validation and execution evidence. Explicit read-only network commands remain allowed and are recorded with risk, operation classes, and extracted endpoints. Explicit outbound writes and unknown remote shells are rejected before command execution with `[network_intent_blocked]`; rejected commands create no command-effect entry. Run stats expose `networkIntentCaptures` and `networkWriteBlocks`, rendered compactly as `net allowed/blocked` in the existing status line. This is command-intent governance, not socket observation or containment: subprocesses can still open sockets through unclassified programs, so OS/container sandboxing remains a high-severity gap.

Behavioral proof covers read-only curl classification and endpoint extraction, HTTP upload rejection, `git push` rejection, package publication rejection, a model-proposed `git push` that increments the block counter while leaving command effects empty, and an executed `curl.exe --version` probe whose metadata records read intent without making a request. Compile, smoke, extension-host E2E, visual, and package gates pass. Visual inspection confirms `net 1/2` is visible without adding UI panels. Package output is `forge-agent-0.60.0.vsix`.

The delivered product has crossed both requested capability thresholds once, but repeatability and evidence retention are not yet strong enough to close the research objective. Console evidence records Qwen 7B architect at Tier-3 5/5 and DeepSeek-v4-pro architect with Qwen 7B executor at Tier-4 3/4. Those live reports were later overwritten by mocked validation because every run writes the same `latest-weak-model-eval-tier<n>.json` path. The current retained Tier-3/Tier-4 JSON files are mock reports (`live: false`), so the live results remain credible historical evidence but are not durable machine-verifiable artifacts.

Current gap priority after Phase 67:

1. Repeat live Tier-4 runs into immutable archives and measure task-level variance; current shell validation remains credential-free.
2. Run Tier-3/4 slice 2 against real repositories through the isolated worktree path.
3. Measure procedural-skill on/off uplift with held-out tasks before expanding templates or allowing cross-repository sharing.
4. Add persistent autonomous role worktrees and bounded parallelism only where repeated eval data justifies the cost.
5. Add socket-level network observation and OS/container containment before unattended real-repository use.
6. Finish remaining session/history UX without reintroducing a cloned-IDE surface.

## Phase 58 - Immutable Evaluation Evidence (implemented; local gates PASS, live archive pending credential)

Problem: `.forge/evals/latest-weak-model-eval-tier<n>.json` is a mutable convenience path. Mock regression runs replaced the live Qwen/DeepSeek reports, defeating the project's artifact-first trust rule and preventing a requirement-by-requirement completion audit.

Build slice: allocate one `runId` and `startedAt` before each Tier-2/3/4 run; persist every partial and final report to both the existing latest path and `.forge/evals/runs/tier-<n>/<runId>.json`; expose `archivePath` in the report and CLI summary. Parallel or later runs may update `latest`, but must never share an archive path.

Validation contract: compile and smoke gates; two sequential scripted runs against one report root produce distinct archives; after run two, run one's archive remains present and still identifies run one; partial writes and final closure use the same archive; mocked Tier-3 and Tier-4 CLI runs print their durable archive paths. Live repeat remains a named open gate when no OpenRouter credential is available.

Implemented result: each shared tier run now receives `runId`, `startedAt`, and `archivePath`. Partial and final payloads dual-write the compatibility `latest` path and one tier-scoped run archive. Two-run negative proof preserved run one byte-for-byte after run two advanced `latest`; a delayed two-task proof observed `partial:true` at completed count 1 and `partial:false` at count 2 in the same archive. Mocked Tier-3 architect remained 5/5 and mocked Tier-4 remained the expected pattern-blind 0/4, each printing its archive path. Compile, smoke, visual, package, and extension-host E2E gates pass. The lean VSIX contains only package metadata, icon, compiled extension/harness code, and compiled webview assets. Antigravity now lists installed `kennyg.forge-agent@0.58.0`. This is ◐ rather than fully closed because `OPENROUTER_API_KEY` is absent from the current shell, so no fresh live Qwen/DeepSeek archive was created.

Non-goals: changing prompts, task fixtures, judges, lane solve accounting, model routing, or reconstructing missing live task-level JSON from console summaries.

## Phase 53.1 - Deterministic Context Budget Scheduler (implemented; local gates PASS, live uplift pending)

Reconciled gap: `ContextBundle.compacted` currently becomes true when scratchpad/files cross fixed thresholds, but that flag does not change provider prompt content. `tokenEstimate` measures only a subset of the actual prompt and excludes role handoff, tool contract, logs, reflections, and escalations. Forge therefore records compaction without performing it.

Selected design: preserve full-fidelity details in filesystem artifacts and assemble each provider system prompt from named priority sections under a hard character budget sized for a 32k-class model with output reserve. Required goal, active task, role/tool contract, task guidance, and open task state survive. Lower-priority scratchpad, logs, reflections, and escalations clear from the prompt first. Every included, cleared, or truncated section and every dropped character is persisted; no artifact is deleted.

Validation contract: scheduler unit proof stays within budget; negative proof retains goal/open tasks while stale tool output clears; a long scripted-provider fixture refuses oversized prompts and must still reach terminal success; persisted context bundle exposes actual prompt chars/tokens, budget, included/cleared/truncated sections, dropped chars, and compaction reason. Regression gates cover compile, smoke, Tier-3, Tier-4, E2E, visual, and package. Live-model uplift is not claimed without a fresh live run.

Implemented result: `assemblePromptWithinBudget` now bounds named prompt sections under a 96,000-character working-set budget for a 32k-class model with reserve. Goal, active task, role/tool contract, task guidance, and open tasks are required; optional known-file/retrieval state and stale scratchpad/log/reflection/escalation outputs clear by priority when they do not fit. `ContextBundle` persists prompt budget/size/token estimate, included/cleared/truncated sections, dropped characters, and reason; `RunStats` counts compactions and cleared tool-result sections. A 140k-character goal fixture held all three provider prompts to exactly 96,000 characters, retained goal/task/open-task markers, edited a failing file, passed tests, and terminaled success with persisted accounting. Validation first exposed and fixed a fair-share defect where short required sections were falsely truncated and unused budget was not reallocated. Compile, smoke, E2E, visual, package, mocked Tier-3 architect 5/5, and expected pattern-blind Tier-4 0/4 pass. Installed Antigravity version is `kennyg.forge-agent@0.58.1`. Live uplift remains unclaimed because the current shell has no OpenRouter credential.

Non-goals: embeddings, semantic retrieval, model-written summaries, isolated worker memory, changing eval truncation physics, or claiming 32k live improvement from a synthetic proof.

## Phase 59 - Product Architect/Editor Rate Split (implemented; local causal/install gates PASS, live product run pending)

Reconciled gap: the eval architect lane injects premise/target/subtasks and focused file contents into its executor. The product loop only selects separate Architect/Editor model IDs. It does not inject `PLAN.md` into Editor context, does not focus plan-named files, and generic progress completion allows Architect or Editor tasks to complete after a read/search. The product therefore does not yet reproduce the rate split that satisfied the research benchmark.

Build slice: Architect `update_plan` creates `.forge/architect-handoff.json` with exact existing focus paths, premise checks, ordered steps, and plan markdown. Architect guidance requires those sections. Editor context gets the plan and each exact focus file through Phase 53.1 budgeting. Role-specific completion requires Architect=`update_plan` and Editor=`apply_patch|write_file`; inspection remains on the same task.

Validation contract: scripted distinct bindings (`deepseek/deepseek-v4-pro` Architect, `qwen/qwen-2.5-7b-instruct` Editor); Architect read does not advance; plan commit does; Editor prompt contains plan and full focus file; Editor read does not advance; patch does; fixture tests/review/evidence terminal success; handoff artifact persists; zero fallback and model-driven accounting. Live capability remains unclaimed without live calls.

Implemented result: product Architect selection now honors `Architect`, webview `plan`, then native `forge.architectModel`; Editor honors `Editor` then webview `code`. Architect plans require premise/focus/ordered-step headings. On Architect `update_plan`, the harness persists `.forge/architect-handoff.json` with exact existing plan-named paths only; basename guesses, nonexistent paths, and lookalike suffixes are rejected. Editor prompts receive the committed plan and each focus file as required Phase-53.1-budgeted sections. Architect read/search remains on Architect until `update_plan`; Editor read/search remains on Editor until `apply_patch|write_file`; `run_tests` and `record_evidence` cannot bypass those completion gates.

Causal product-loop proof used only UI-shape bindings `plan=deepseek/deepseek-v4-pro`, `code=qwen/qwen-2.5-7b-instruct`: two Architect calls routed to DeepSeek, two Editor calls routed to Qwen, read-only steps stayed active, Qwen received the committed plan and full `src/math.js`, patch changed subtraction to addition, tests/review/evidence passed, terminal status was success, provider calls were model-driven, and fallback was zero. E2E embeds the same proof. Validation also exposed a role-blind reflection A/B fixture that had depended on generic task completion; the fixture was corrected to read as Explorer, plan as Architect, and patch as Editor. Reflection A/B returned to 5/5 on versus 0/5 off. Compile, smoke, E2E, visual, package, mocked Tier-3 architect 5/5, expected pattern-blind Tier-4 0/4, install, and compiled-marker gates pass for `kennyg.forge-agent@0.59.0`. Live DeepSeek/Qwen product capability remains unclaimed because no credential-backed run was available.

Non-goals: copying the fixture-specific eval loop, model-as-judge, weakening review/oracles, basename-guess path repair, parallel workers, or claiming live DeepSeek/Qwen success from scripted routing.

## Executive Verdict

Forge Agent is now a real VS Code/Antigravity extension with a basic success-harness shape, not a cloned IDE. It has an installable VSIX, compact agent UI, OpenRouter provider layer, typed tool registry, deterministic firewall, persistent run artifacts, an autonomous run command, and an early weak-model eval.

It is not yet a mature success-driven coding agent by the standard of the full research blueprint. The deterministic loop, repair gradient, reviewer gates, verification fixtures, budgets, artifact ledgers, enforced role capabilities, role-scoped provider sessions, worktree isolation, optional hybrid semantic retrieval, and multi-tier eval harness exist. The remaining hard gaps are repeated durable live evaluation, real-repository use of the worktree path, live semantic-retrieval uplift measurement, true process/worktree-per-role parallel workers, socket-level containment, and OS/container sandboxing.

Blunt status: Forge has reached an early Founding-release harness, but not the Quality or Triple-A research target.

## Research Consensus

All four research passes converge on the same thesis: the harness matters more than the raw model.

- ChatGPT research emphasizes a durable goal loop, six persistent state objects, OpenRouter routing policy, artifact-first trust, and eval discipline.
- Claude research emphasizes a simple while-tool-use loop wrapped in enforced scaffolding: task state, scratchpad memory, verification, context engineering, edit-format reliability, and sub-agent isolation.
- Gemini research emphasizes an autonomous command center: stateful orchestration, local verification, and background managers that coordinate long-running work.
- The consolidated Blueprint converts those ideas into machine-verifiable phases and non-negotiables: all mutation must flow through `PROPOSE -> VALIDATE -> COMMIT -> NARRATE`, and terminal success must be backed by green oracle evidence.

For this project, all "IDE" language should be read as "extension-host integration." We should use VS Code/Antigravity native editors, terminals, file explorer, and diff surfaces instead of rebuilding an IDE.

## Current Implementation Snapshot

Current Forge implementation, based on source and `BUILD_LOG.md` evidence:

- Extension packaging exists, including VSIX packaging and Antigravity disk-level refresh.
- Provider layer exists with OpenRouter, OpenAI-compatible fallback, `session_id`, fallback `models`, JSON-schema response format, catalog refresh, model sorting, and cost/context metadata.
- Tool registry exists for repo search, symbol search, file reads, patch application, commands, tests, diffs, plan/task/evidence updates, and success declaration.
- Firewall exists for schema validation, workspace path containment, command policy checks, patch applicability, manifest-backed safety checkpoints, targeted restore, workspace snapshot restore, and rejected-action behavior.
- Verification oracles exist for tests, typecheck/lint/build-like checks where detectable, and no-test-suite rejection.
- Basic autonomous run loop exists with inspect, plan, edit, verify, evidence phases.
- Normal runs now persist provider calls/failures, schema repair attempts, model-driven proposals, fallback proposals/actions, validation failures, no-progress turns, and `actuallyModelDriven`.
- Firewall rejection, tool failure, and red-oracle failure now queue bounded reflection notes before retrying, and reflection counters persist in normal run state.
- Mutating proposals now pass a pre-commit review step after deterministic firewall validation and before COMMIT. The ledger persists under `.forge/precommit-reviews.json`; configured review models can block a patch before filesystem mutation, otherwise a deterministic approval is recorded.
- Success now requires a persisted reviewer diff gate. `get_diff` records `.forge/diff-reviews.json`, `.forge/reviewer-critiques.json`, reviewer counters, critique counters, and diff excerpts on evidence entries before final success.
- `run_command` now captures before/after workspace file effects and persists `.forge/command-effects.json` with created, modified, and deleted file lists, output excerpt, sanitized command environment metadata, allowed/blocked env key names, timeout, duration, exit code, and signal.
- Runs now carry explicit budget state under `.forge/budget.json`. Wall-clock caps halt before provider/tool work, and cost caps halt after provider usage is recorded but before validation/commit can mutate the workspace.
- Isolated run API exists through `forge-agent.runIsolatedAgentGoal` and Proof-tab controls. It copies the source workspace to a temp root, runs the normal harness against the copy, compares source before/after, and persists `.forge/isolated-runs/latest-isolated-run.json` plus a diff summary.
- Verification fixture matrix now covers passing tests, failing tests, missing tests, typecheck failure, lint failure, malformed patch, out-of-workspace path, and blocked command behavior. The report persists at `.forge/verification-fixture-matrix.json`.
- The verification fixture matrix also includes an unsolvable step-cap case that proves exhausted runs terminate as `gave_up` without green evidence.
- Bounded escalation routing now sends proposals to an escalation/review model binding after repeated reflection failures and persists `.forge/escalations.json`.
- Context engineering now persists `.forge/context-bundle.json` with goal, open tasks, recent files, reflection/escalation/review notes, scratchpad summary, retrieval policy, token estimate, and compaction flag. The bundle is fed into the next proposal prompt.
- Deterministic retrieval now ranks likely relevant workspace files from goal/task text, persists `.forge/retrieval-index.json`, stores `retrievalCandidates` in the context bundle, and feeds those candidates into proposal prompts.
- Role handoff artifacts now persist under `.forge/role-handoffs.json` with active role, scoped allowed tools, responsibilities, role-open tasks, recent context, and a handoff summary. The next proposal prompt consumes only the active role's handoff slice.
- Safety checkpoints now persist under `.forge/safety-checkpoints.json`; each mutating proposal records checkpoint strategy, protected paths, manifest path, and compact `safe` run status. Targeted file checkpoints restore original file content in tests.
- Persistent artifacts exist: `.forge/state.json`, goal contract, task graph, plan, evidence ledger, repository knowledge, skill registry, plus `PLAN.md`, `SCRATCHPAD.md`, `todos.json`, and evidence files.
- Weak-model eval exists as a 15-task disposable fixture loop with mocked default path and opt-in live direction. The latest verified mocked scorecard records bare solved 1/15, harness solved 15/15, actually model-driven 15, and fallback solved 0.
- Compact UI exists and avoids cloned IDE surfaces.

Important limitation: the harness exists, but it is not yet mature. Current agency is basic and still needs deeper self-repair, richer state transitions, real worker isolation, worktree execution, process sandboxing, and larger evals.

## Phase-by-Phase Blueprint Comparison

| Phase | Blueprint intent | Current Forge state | Status | Gap |
|---|---|---|---|---|
| Phase 0: Extension skeleton + provider layer | Loadable extension, OpenRouter provider, chat/webview, capability probe, fallback provider, `session_id`, structured outputs | VSIX, extension host, OpenRouter provider, OpenAI-compatible fallback, chat, model catalog, `session_id`, fallback models, JSON-schema format exist | Partial | Capability probing is heuristic, PONG integration against multiple providers is not fully proven, and streaming is not mature |
| Phase 1: Tool surface + firewall | Repo-native tools, deterministic validation, no direct mutation, diffs, reversible checkpoints | Tool registry, firewall, patch/path/command validation, pre-commit review, checkpoints, transactional file edits and multi-file command staging with conflict/rollback evidence, network-intent policy, native diffs, and reviewer artifacts exist | Partial | Git/copy transactions prevent normal cwd-relative direct mutation and shared Git writes are blocked, but arbitrary OS paths, socket-level observation, and kernel/container containment remain missing |
| Phase 2: Verification oracles | Tests/build/lint/typecheck with real pass/fail fixtures; no-test handling | Typed adapter contracts drive separate test/lint/typecheck/build oracles; composite green requires every detected required oracle; the 11-case disposable matrix covers pass/fail/no-test/typecheck/build/lint/firewall and unsolvable step-cap cases | Partial | Detection and terminal honesty are proven, but automated no-test/build remediation and monorepo scheduling remain underdeveloped |
| Phase 3: Goal loop | Run until green or honest cap; anti-spin/no-progress detection; weak model never false-successes | Autonomous loop, green-evidence success, caps, blocker lifecycle, and enforced universal workflow/success gates exist | Partial | Retry actions remain centrally orchestrated and persistent autonomous role lifecycles are absent |
| Phase 4: Auto-plan + todos + scratchpad + evidence | Create artifacts before edits; todos transition correctly; evidence backs completion | Artifacts plus baseline/acceptance/task record persist; edits require reconciliation and plan; validation/review/close/AAR are evidence-gated; typed root project adapters persist exact oracle contracts | Partial | Todo quality still depends on the planner, and adapters do not yet schedule monorepo/package-specific validation graphs |
| Phase 5: Reflection/self-critique | On validation/oracle failure, inject bounded reflection and retry with changed approach | Bounded reflection, reviewer critique, reflection A/B, deterministic blocker taxonomy, and verified procedural recovery skills exist | Partial | Skill lifecycle/prompt delivery are proven locally, but no held-out or live skill on/off solve-rate uplift exists and reflection remains single-agent |
| Phase 6: Sub-agents | Isolated planner/implementer/reviewer/explorer/escalation roles with non-nesting contexts | Role capabilities/provider sessions are isolated; tools run in role-tagged child processes; edits and cwd-relative multi-file commands use temporary transactions with conflict/rollback evidence | Partial | Model calls and orchestration still share the parent; no persistent autonomous workers, parallelism, long-lived worktree-per-role lifecycle, or kernel sandbox |
| Phase 7: Model-agnostic enablement | Structured-output repair, V4A/context edits, forced decomposition, weak-model uplift | JSON-schema response format, bounded malformed-output repair, SEARCH/REPLACE patch validation, and weak eval model/fallback separation exist | Partial | No V4A `*** Begin Patch` format, no fuzzy/lenient patching, no whole-file fallback policy, and decomposition is shallow |
| Phase 8: Context engineering | Compaction, filesystem memory, tool-result clearing, retrieval of relevant files | Hard prompt budgeting, required-state preservation, stale-result clearing, deterministic/hybrid retrieval, role contexts, and verified procedural skill rehydration exist | Partial | Semantic and skill mechanics pass causal local gates, but live uplift and fully independent worker memory stores remain unproven |
| Phase 9: Eval harness + UX polish | 15-25 task benchmark, bare-vs-harness scorecard, weak fixed model, regression tracking, polished UX | 15-task mocked weak eval, scorecard JSON, proof tab controls, compact UI, VSIX packaging | Partial | Eval now reaches the lower benchmark-size target, but is still mostly mocked by default; no large live benchmark or target solve-rate ratchet |

## Cross-Research Gap Matrix

| Research requirement | Current Forge state | Gap severity | Why it matters | Useful takeaway | Suggested priority |
|---|---|---:|---|---|---:|
| Durable goal loop with explicit done criteria | Goal contract and run loop exist | Medium | Goal state exists but is not rich enough to guide complex work | Keep the extension path; deepen the harness | 1 |
| Six persistent state objects | All six are persisted in basic form | Medium | Presence is not the same as useful lifecycle enforcement | Add schema tests and lifecycle assertions | 2 |
| OpenRouter-first policy | `pareto-code`, `auto`, `session_id`, fallback models, structured output exist | Low | Provider substrate is largely aligned | Keep OpenRouter as routing layer, not tool owner | 5 |
| Artifact-first UX | Compact UI and native artifact open commands exist | Medium | The user can inspect artifacts, but the run surface does not yet explain progress well | Keep artifacts behind native surfaces, add better status summaries | 5 |
| While-tool-use loop | Basic loop plus first repair/no-progress/accountability slice exists | High | Current loop is still phase-like and lacks red-oracle reflection | Add reflection and richer retry mechanics before adding more UI | 1 |
| Verification feedback carries weak models | Test oracle gates success and red-oracle output now queues bounded reflection | Medium | Failed tests feed the next proposal context, but repair quality is not benchmarked yet | Add reflection on/off evals and richer failure classification | 2 |
| Context-anchored edit reliability | SEARCH/REPLACE patching exists | Medium | Better than line numbers, but not the full V4A format or lenient matcher | Upgrade edit format after loop hardening | 4 |
| Sub-agent isolation | Role scopes/sessions are enforced; tools run in external role processes; edits and commands stage in temporary transactions | High | Cwd-relative filesystem effects and merge conflicts are contained, but orchestration/model calls/state remain shared and no persistent role worker owns a branch | Add persistent role worktrees and bounded autonomous workers before parallelism | 4 |
| Autonomous command center | Basic autonomous command exists | High | It can run, but not yet manage blockers, retries, escalation, or background jobs | Build deterministic run state first, then background managers | 1 |
| Background managers | Not implemented | Medium | Useful for longer tasks, but premature before loop maturity | Defer until no-progress/reflection/eval are solid | 6 |
| Machine-verifiable phase gates | Build log, tests, and eight-case verification fixture matrix exist | Medium | Core failure detection is now behavior-tested, but larger benchmark and remediation coverage remain thin | Expand fixture suite into repair/remediation scenarios | 2 |
| Weak-model benchmark | 15-task mocked scorecard exists | Medium | Current eval proves the measurement loop and fixture breadth, but not live weak-model strength | Add opt-in live runs, then ratchet against real weak-model baselines | 5 |

## Useful Takeaways

- The IDE-fork idea should remain scrapped. Nothing in the useful research requires a standalone IDE; the essential parts map cleanly to an extension-host harness.
- The most valuable research idea is still the firewall: model proposes, deterministic code validates and commits, model narrates after the fact.
- The second most valuable idea is verification-as-carrier. Forge should prioritize failed-test repair loops over more model-picker UI.
- The third most valuable idea is artifact discipline. Plans, todos, scratchpads, and evidence should be automatically generated and enforced, but not permanently visible as clutter.
- The weak-model eval is useful, but only as an instrument. It should not become the product's main purpose.
- "Sub-agents" should not be implemented as UI personas. They need isolated context, narrow tool permissions, and handoff artifacts, or they are not real sub-agents.
- Background managers and parallelism are attractive but should wait until single-run correctness is stronger.

## Recommended Next Build Order

1. Harden the real agency loop.
   - Status: first slice implemented in Phase 18.
   - Added schema/tool-call repair loops for malformed provider output.
   - Added no-progress detection and honest `gave_up`.
   - Added normal-run accounting for model-driven proposals, fallback proposals/actions, provider failures, validation failures, and repair attempts.
   - Status update: explicit budget cap enforcement implemented in Phase 32.
   - Runs persist `.forge/budget.json`; wall-clock caps halt before provider calls, cost caps halt after usage is recorded and before validation/commit mutation.
   - Status update: deterministic blocker taxonomy/lifecycle implemented in Phase 63 and injected into bounded retry context.
   - Status update: Phases 71-72 add category-specific remediation capsules plus signature-based unchanged-oracle `gave_up` with changed-diagnostic and eventual-green causal controls.
   - Remaining: user-configurable budget presets, semantic detection of lateral thrashing across changing diagnostics, and live weak-model remediation uplift.

2. Strengthen verification.
   - Status: first fixture matrix implemented in Phase 21.
   - Added disposable fixtures for passing tests, failing tests, missing test suite, typecheck failure, lint failure, malformed patches, path escapes, and blocked commands.
   - Matrix report persists at `.forge/verification-fixture-matrix.json` and is callable from the extension command surface and Proof tab.
   - Status update: unsolvable step-cap fixture implemented in Phase 23.
   - Status update: Phase 70 adds typed Node/Python/Rust/Go/unknown project adapters, an independent build oracle, exact command provenance, and composite all-required-oracles truth.
   - Status update: Phase 71 adds durable typed failure capsules, duplicate failure folding, category-specific retry guidance, and green-only resolution; five scripted causal remediation lanes pass.
   - Remaining: live weak-model remediation uplift, framework-specific missing-test synthesis, monorepo validation graphs, and stronger cross-run evidence lifecycle checks.

3. Add bounded reflection.
   - Status: first slice implemented in Phase 19.
   - Firewall rejection, tool failure, and red-oracle failure now queue bounded reflection notes before retry.
   - Reflection attempts are capped and logged in state, scratchpad, and run stats.
   - Status update: reviewer critique artifact implemented in Phase 28.
   - `get_diff` now creates `.forge/reviewer-critiques.json`; if a `review`/`Reviewer` binding is configured, a structured reviewer-model critique is requested, otherwise a deterministic critique is recorded.
   - Status update: blocking pre-commit review slice implemented in Phase 29.
   - Mutating proposals now create `.forge/precommit-reviews.json` before COMMIT; configured reviewer models can block before filesystem mutation and trigger bounded reflection.
   - Status update: reflection A/B eval implemented in Phase 36.
   - The A/B runs the REAL `AgentHarnessLoop` (the weak-eval mini-lane has no reflection, so toggling there would measure nothing). `reflectionEnabled` lives in harness state (default on); when disabled, `scheduleReflection` suppresses and the existing red-oracle/tool-failure halt branches fire honestly. `runReflectionAbEval` executes disposable fixtures where a scripted provider only emits the correct patch after harness-injected reflection context appears in the prompt, making the solve causally dependent on the reflection mechanism.
   - Verified A/B number (mocked scripted lanes): reflection on 5/5 solved, reflection off 0/5 with 5/5 honest red-oracle halts, solve-rate delta +1.00. Scorecard persists at `.forge/evals/latest-reflection-ab.json`; CLI `npm run eval:reflection`; extension command `forge-agent.runReflectionAbEval` with native artifact open path.
   - Status update: terminal-state AAR hook implemented in Phase 37.
   - On any terminal status, `persistStateToDisk` fires `recordAar` exactly once: `.forge/aar.json` records deterministic trigger counts (reflections, suppressed reflections, escalations, budget halts, pre-commit blocks, validation/provider failures, repair attempts, safety reverts, no-progress turns), derived sustain/improve-work/improve-tools notes, and `clean: true` on zero-trigger runs. Non-clean runs bank signature-deduped durable lessons to `.forge/lessons.json` (capped at 50), and `refreshContextBundle` rehydrates `recentLessons` into future proposal context — the AAR feeds future RECONCILE. The harness records facts; the model never writes AAR truth.
   - Behaviorally proven: reflected success closes non-clean with a banked lesson and sustain note; reflection-suppressed failure closes with suppression cited in improve-tools; zero-trigger run closes clean with no lessons file; a second run in the same workspace rehydrates the banked lesson into its context bundle.
   - Status update: structured deterministic failure taxonomy implemented in Phase 63; terminal categories now feed AAR/lesson banking.
   - Remaining: a live-model reflection A/B once the live weak-model benchmark lands.

4. Implement real reviewer and escalation behavior.
   - Status: first reviewer-gate slice implemented in Phase 20.
   - Reviewer inspects the current diff through `get_diff` before final success.
   - Diff review artifacts persist under `.forge/diff-reviews.json`, reviewer counters, scratchpad, and evidence diff excerpts.
   - Status update: first escalation-routing slice implemented in Phase 22.
   - Repeated reflection failures now route the next proposal to `Escalation`, `escalation`, `review`, or `Reviewer` model bindings.
   - Escalations persist under `.forge/escalations.json`, scratchpad, logs, and run stats.
   - Status update: first role-handoff artifact slice implemented in Phase 25.
   - Active role handoffs persist under `.forge/role-handoffs.json` with allowed tools, responsibilities, role-open tasks, recent context, and a concise handoff summary.
   - Proposal prompts now include the active role's handoff slice instead of treating roles as UI labels only.
   - Status update: reviewer-model critique slice implemented in Phase 28.
   - Reviewer critiques persist under `.forge/reviewer-critiques.json`, count in run stats, open through native artifact command, and use configured review model bindings when present.
   - Status update: pre-commit review mode implemented in Phase 29.
   - Pre-commit reviews persist under `.forge/precommit-reviews.json`, count approvals/blocks in run stats, open through native artifact command, and block mutating proposals before filesystem change when the review model returns `blocked`.
   - Status update: enforced role worker boundaries implemented in Phase 61.
   - Role tool scopes now block cross-role proposals before generic firewall validation; provider sessions are role-scoped; `.forge/worker-contexts.json` persists call/proposal history and opens natively.
   - Status update: process-isolated workspace tool execution implemented in Phase 62. Role-tagged one-shot children execute validated workspace tools with sanitized inherited environments and persist PID/outcome evidence.
   - Remaining: persistent autonomous worker processes containing model calls, worktree-per-role execution, independent memory stores, deterministic merge, and bounded parallelism.

5. Harden workspace safety.
   - Status: first manifest-backed safety slice implemented in Phase 27.
   - Added `.forge/safety-checkpoints.json`, checkpoint manifests under `.forge/checkpoints/<id>/manifest.json`, targeted file snapshots, workspace snapshots, native artifact opening, and compact `safe` status counter.
   - Extension-host tests verify targeted restore returns original file content and normal harness runs persist checkpoint manifests.
   - Status update: command side-effect capture implemented in Phase 30.
   - `run_command` now records `.forge/command-effects.json`, created/modified/deleted file lists, output excerpts, native artifact opening, and compact `cmd` status counter.
   - Extension-host tests verify a command-created file is recorded in the command-effects ledger.
   - Status update: command process sandbox metadata implemented in Phase 34.
   - `run_command` now executes with a curated environment allowlist and records allowed/blocked env key names, timeout, duration, exit code, signal, and sanitized-env status in `.forge/command-effects.json`.
   - Extension-host and local smoke tests verify a deliberate secret env key is blocked and the child process cannot read it.
   - Status update: isolated workspace-copy execution implemented in Phase 33.
   - `forge-agent.runIsolatedAgentGoal` copies the workspace to a temp root, runs the normal harness against the copy, proves whether the source workspace changed, and persists `.forge/isolated-runs/latest-isolated-run.json` plus a diff summary.
   - Extension-host tests verify an isolated patch changes the temp copy, leaves the source file unchanged, and opens the report through the native artifact command.
   - Status update: true git worktree execution implemented in Phase 35.
   - Isolated runs on git-backed sources now execute in a real detached `git worktree` (not a plain copy), overlay dirty tracked edits and untracked files into the worktree, record `isolationMode`, `baseCommit`, `dirtyFilesOverlaid`, and `sourceDirtyStatusPreserved` in the report, remove and prune non-kept worktrees, fall back to workspace copy for non-git sources with an explicit `isolationFallbackReason`, and fail honestly when `isolationMode: 'worktree'` is forced on a non-git source.
   - Behavioral tests verify worktree linkage (`.git` file), dirty-state overlay and preservation, source non-mutation, worktree registration cleanup, and forced-mode rejection.
   - Status update: command-level network intent governance implemented in Phase 60. Read-only intent and endpoints are captured; explicit outbound writes/unknown remote shells are blocked before execution.
   - Remaining: socket-level network observation/containment and OS/container sandboxing. Phase 60 must not be represented as process-level isolation.

6. Expand weak-model eval.
   - Status: 15-task mocked disposable benchmark implemented in Phase 31.
   - Default CLI and Proof-tab eval now run 15 fixture tasks covering syntax, failing tests, edge cases, refactors, validation branches, async returns, mutation safety, range handling, defaulting, parsing, and collection behavior.
   - Latest verified mocked scorecard: `qwen/qwen2.5-coder-7b-instruct`, bare solved 1/15, harness solved 15/15, actually model-driven 15, fallback solved 0.
   - Status update: first live OpenRouter runs executed (2026-07-08). Finding 1: `qwen/qwen2.5-coder-7b-instruct` and `mistralai/mistral-7b-instruct` are delisted (404 no endpoints); `qwen/qwen-2.5-7b-instruct` is live. Finding 2 (critical, empirically confirms the research's format-strictness caveat): `TOOL_SCHEMA`/`ACTION_SCHEMA` declared `arguments: { type: 'object' }` with no properties inside a strict schema, so live constrained decoders forced `arguments: {}` on all 75 calls — every proposal had an empty path and was correctly firewalled. Result: live 0/15 both lanes, fallback 15/15 (honest accounting held; zero false successes). The mocks structurally could not catch this because they never enforce the grammar.
   - Status update: schema fix + live-eval guards implemented in Phase 38.
   - `TOOL_SCHEMA` and `ACTION_SCHEMA` now enumerate the full tool-argument inventory as optional properties (lenient for weak models; firewall still owns content validation). A strict-decoder simulation reproduces the live bug against the old schema (arguments emptied) and proves the full harness loop reaches success behind simulated grammar enforcement with the new schema. Live eval now probes endpoint liveness first (dead slug rejected fast; network failure degrades gracefully) and runs a one-call schema canary that aborts with an actionable error before burning the task suite if `arguments` round-trips empty; the canary result persists in the scorecard.
   - Second live run (post-schema-fix): canary passed (path/patchContent round-trip), 75/75 calls succeeded, but 0/15 both lanes — dominant error is now `Malformed patch: expected SEARCH/REPLACE hunk`. The model delivers patches in the wrong format; the eval prompt names the format without showing it, and the harness mini-lane retries without feeding the rejection reason back, so bare and harness lanes are nearly identical on format errors.
   - Status update: format exemplar + rejection feedback implemented in Phase 39.
   - Eval task prompts now show a concrete SEARCH/REPLACE exemplar (exact fence tokens, character-for-character copy rule) instead of just naming the format. Harness-lane retries now feed the last firewall rejection reason plus the exemplar back to the model — verification feedback for format errors, the channel bare lacks by design. Lane results capture `lastValidationError` and a truncated `rejectedPatchSample` for diagnosis. Behaviorally proven: a format-confused scripted provider scores bare 0/1 vs harness 1/1, recovery causally dependent on the rejection feedback; diagnostics captured in the scorecard.
   - Next rungs if live still underperforms: lenient patch matching (whitespace drift), V4A format, whole-file fallback.
   - MILESTONE (2026-07-08): first clean live baseline established — `qwen/qwen-2.5-7b-instruct`, bare 2/15 vs harness 13/15, delta +0.733, all 13 harness solves model-driven, zero false successes, total live-run cost under $0.01. The core thesis (harness > model) is now confirmed with a real weak model, not mocks.
   - Ratchet floor: +0.73 delta on this suite. Next rungs from run-3 diagnostics: whitespace-lenient SEARCH matching (the `syntax-error` miss), deterministic path repair for occasional empty-path first proposals, and convergence tuning for `sort-copy`-shaped tasks.
   - Status update: lenient edit application + eval-lane path repair implemented in Phase 40.
   - Shared `findLenientMatch` (exact substring first — legacy semantics preserved; else sliding window of trim-equal lines, unique-or-ambiguous-reject, replacement rebuilt from the file's actual bytes) now backs both firewall applicability and patch application; lenient applications are visible in tool output. Eval harness lane repairs empty apply_patch paths to the task file, counted in `pathRepairs` (assistance visible, never silent); bare lane unaided by definition. Behaviorally proven including a combined run-3 failure-mode provider (drifted SEARCH + empty path): bare 0/1, harness 1/1 with `pathRepairs: 1` and lenient apply. Regressions green.
   - Live run 4 (post Phase 40): harness held 13/15 with providerCalls down 58→40 (retry burn cut ~30%); pathRepairs fired on 9+ tasks; bare rose 2→4 on live variance (~±2 noise — ratchet metric revised to "harness ≥ 13/15 AND delta > 0" pending multi-run means). Remaining misses are structurally malformed hunks on specific task shapes — a diff-format capability limit, not a matching problem.
   - Status update: whole-file rewrite recovery implemented in Phase 41. After 2 consecutive malformed-patch rejections the harness lane switches its request to `write_file` with complete corrected content; recoveries counted in `wholeFileRecoveries`; step cap 6. Behaviorally proven, regressions green.
   - MILESTONE (live run 5): **15/15, suite saturated.** Bare 0/15 vs harness 15/15, delta +1.00, all model-driven, zero fallbacks, 5-run total cost < $0.02. Prompt improvements were shared by both lanes (bare got them and scored 0), so the uplift is attributable to the loop. Solve anatomy: 10 quick patch solves, 2 feedback recoveries, 3 whole-file recoveries on exactly the run-4-predicted chronic tasks.
   - The 15-task suite is now a REGRESSION suite, not a measurement instrument. Next measurement requires tier-2 tasks (multi-file, missing-test, small features) — and that is where multi-agent context multiplication (Phase 6 workers) gets its A/B.
   - Live tier-2 baseline (2026-07-08): bare 1/7, harness 5/7, delta +0.571 (multi-file 2/3, missing-test 1/2, features 2/2). Ratchet floor: harness ≥ 5/7 AND delta > 0. Dominant fixable loss: empty-path proposals unrepaired in multi-file fixtures (t2-broken-import burned all 8 steps).
   - Live tier-2 run 2 (post-46): multi-file 3/3 (rung verified), features 2/2, missing-test 0/2 (variance-dominated), harness 5/7 @ 38 calls.
   - LIVE SWARM A/B (2026-07-08): bare 1/7, solo 4/7, swarm 3/7 @ ~2.5× calls. Kind-level tradeoff: swarm swept missing-test 2/2 (solo 0/2, and 1-for-4 historically) but lost multi-file 1/3 (solo 3/3) and features 0/2 to seam blindness — the implementer can't see relationships across arcs. Next rungs: repeat runs for means → interlocking arcs (dependency signatures, deterministic, zero extra calls) → terrain-based lane dispatch if means hold.
   - LIVE RESULTS (2026-07-08, final): TIER-2 SATURATED — dispatch 7/7 (bare 0/7) at 43 calls, every kind routed to its winning formation. Swarm+interlocks: multi-file 1/3→2/3 (seam-sight verified live), missing-test 2/2 again (4/4 across runs — best-replicated cell). New ratchet floor: dispatch ≥ 6/7. Next measurement requires tier-3 (large files, real repos) and the architect/editor rate-split lane.
   - Status update: Phases 48–49 IMPLEMENTED. Interlock unit + causal seam proofs green (both sides of the broken-import seam visible from either arc; a seam-blind provider solves only with interlocks present); dispatch routes per terrain table with override, mock 7/7, all regressions green. Live re-runs pending. Original specs follow.
   - Phase 48 plan (reconciled scope, documented before build): interlocking arcs. Deterministic `extractInterlocks(files)` builds the require-graph (relative-require regex, path-resolved) and per-file interface signatures (function/const declarations + module.exports lines). Explorers see their file PLUS the export signatures of its direct dependencies and the import lines of its dependents — arcs overlap at the seams, like real defensive sectors. The swarm implementer gets the same interlocks for the prime suspect. Zero extra provider calls; interlocks recorded in the handoff artifact. Proven by: unit extraction on the broken-import fixture (index.js↔format.js seam), and a CAUSAL e2e — a seam-blind provider that only emits the correct cross-file fix when the neighbor's exports appear in its prompt solves with interlocks and fails without.
   - Phase 49 plan (reconciled scope, documented before build): terrain dispatch. A `dispatch` mode in the tier-2 runner routes each task to ONE lane by a deterministic kind-routing table (data-derived defaults: missing-test→swarm, multi-file-bug→solo, feature→solo; overridable via options for when repeat-run means revise the terrain map). Per-task `dispatchLane` recorded; report gains `dispatchSolved`. The router is a lookup, not a model — deterministic code draws sector lines. Proven by: mock dispatch run routes per table and solves 7/7; routing override respected; no-dispatch regressions unchanged. Non-goals: model-based routing, mid-task lane switching.
   - Status update: Phase 47 IMPLEMENTED — swarm lane mechanics fully proven (explorers per file, handoff artifacts, measured prompt metrics, suspect rotation, mock 7/7 all lanes). HONEST FINDING: on tiny fixtures the swarm is context overhead (ratios 1.08–1.18×) — compression requires large corpora; the live A/B on this suite tests suspicion-guided focus, and true compression needs a large-file tier-3. Live swarm A/B pending. Original spec follows.
   - Phase 47 plan (reconciled scope, documented before build — blueprint Phase 6, depth-1, non-nesting): a third `swarm` lane in the tier-2 runner, A/B'd against bare AND the solo harness on identical fixtures with the same held-out judge. Explorer workers: one provider call per source file, each in a FRESH session with only that file + the goal, returning structured `{summary, suspicionScore, keyLines}` (schema-constrained). Handoff artifact `.forge/swarm-handoff.json` (compact, deterministic — partition context via artifacts, never transcripts). Implementer: the standard harness mini-loop with full gradient of assistance, but its prompt carries the FULL text of only the top-suspect file plus one-line summaries of the rest — summaries for breadth, full content for focus; on red oracle the next step rotates to the next suspect. Context multiplication measured: implementer prompt chars vs solo-harness prompt chars recorded per task (`promptChars`), worker calls counted separately (`explorerCalls`). CLI `--swarm`. Proven by: mock swarm solves multi-file tasks with implementer prompts strictly smaller than solo-lane prompts; suspicion ranking drives file selection; workers cannot nest. The A/B question the live run answers: does the swarm beat 5/7 at acceptable cost — and specifically on multi-file/missing-test kinds. Non-goals: parallel execution (sequential workers this slice; parallelism is an optimization, not a correctness question), main-loop swarm integration. Resolver units, exact live-failure-mode replay (t2-broken-import now solves with contentAddressedRepairs=1), and the ambiguity-feedback round-trip all proven; regressions green. Live tier-2 re-run pending (floor 5/7, target 6/7). Original spec follows.
   - Phase 46 plan (reconciled scope, documented before build): content-addressed path repair. `resolvePatchTargetByContent(candidates, patchContent)` in tools.ts — a repair fires ONLY when the path is empty, the proposal is apply_patch, and EVERY hunk matches (exact or lenient) in EXACTLY ONE candidate file; candidates are the task's declared source files read from current disk state (never the model-authored test); zero or multiple matches → no repair, and the retry feedback reports the candidate count so the model can emit the path explicitly. write_file gets no content guessing, ever. Repairs counted in `contentAddressedRepairs` + `pathRepairs`. Proven by: unit cases (unique exact, unique lenient, ambiguous 2-file, zero-match), e2e scripted provider solving a 2-file fixture with empty-path patches, ambiguous-case provider recovering via the explicit-path feedback channel, tier-1/tier-2 mock + reflection regressions. Non-goals: main-loop wiring (candidate policy belongs to the retrieval/swarm work), write_file content matching.
   - Status update: Phase 45 IMPLEMENTED — tier-2 suite live-ready. Mock mechanics proven: held-out judging (bare blind shots fail the judge regardless of workspace state), oracle authoring on missing-test tasks (authoredTest + green workspace oracle), multi-step multi-file features (≥2 steps). CLI `npm run eval:tier2 -- --live --model <slug>`. Live tier-2 baseline pending. Original spec follows.
   - Phase 45 plan (reconciled scope, documented before build): tier-2 suite in new `src/harness/weakEvalTier2.ts`. Task shape: multiple files per fixture, optional MISSING workspace test (the harness must first author `test.js` to have an oracle), and small features (new function + export wiring). THE JUDGE IS HELD OUT: final `solved` is decided by a runner-owned verification test executed after the run, never by the model's own test — the model cannot grade its own homework; the workspace oracle it sees is tracked separately (`workspaceOracleGreen`). Lanes mirror tier 1 (bare one-shot vs harness mini-loop, 8 steps, full gradient of assistance except single-file path repair, which is disabled where multiple files make the target ambiguous). ~7 tasks: 3 multi-file bugs, 2 missing-test, 2 features. Artifact `.forge/evals/latest-weak-model-eval-tier2.json`; CLI `scripts/weak-model-eval-tier2.mjs` + `npm run eval:tier2` (reuses liveness probe + schema canary for --live). Deterministic mock provider proves suite mechanics; real numbers come from live runs. Non-goals this phase: extension command/Proof-tab surface (CLI-first, rides with next host batch), multi-agent workers (they get A/B'd AGAINST this suite next).

8. Goal interface (/goal equivalent) — IMPLEMENTED in Phases 42–44 (2026-07-08); behavioral proofs all green (parser, override flow, pause with zero provider calls, mid-run steer without restart, cross-session resume that respects terminal states). Extension-host e2e for the five new commands rides with the next host-gate run (◐ named). Original spec follows.
   - Phase 42 — Goal elicitation. New `src/harness/goalContract.ts`: `parseGoalDirective(text)` deterministically parses `/goal <text>` with optional sections (`done when:`, `constraints:`, `non-goals:`, `budget: $N`, `max steps: N`; `;`/newline separated, case-insensitive). Compiled `doneWhen` = user's criteria PLUS the mandatory oracle gates ('run_tests oracle passes', evidence entry) — user criteria add to, never replace, the oracle; the model still cannot argue with the stop condition. `initializeHarness` gains `goalOverrides` (doneWhen/constraints/nonGoals/budgetUsd/maxSteps → contract + runBudget + maxSteps). Extension: `forge-agent.setGoal` (parse + preview without running); `runAgentGoal` accepts `/goal` text; chat bridge intercepts messages starting with `/goal` and returns the compiled contract instead of a chat completion (webview render deferred — no UI claim). Proven by: parser unit cases (full syntax, partial, non-directive), contract compilation preserving oracle gates, override flow through a live loop run.
   - Phase 43 — Mid-run steering. `.forge/control.json` written by `forge-agent.pauseGoal` / `forge-agent.resumeGoal` / `forge-agent.steerGoal(overrides)`. `runStep` applies control before proposing: goal edits merge into the contract mid-run WITHOUT restarting (tasks/evidence retained, steer logged); pause sets status `paused` (already in HarnessStatus), persists, returns without a provider call. All driving loops (runAgentGoal, webview bridge, isolation, resume) exit on `paused` as a non-terminal, resumable state. Proven by: pause between steps makes zero further provider calls; steer mid-run changes the contract in the next proposal context while preserving task state; resume continues from the same step index.
   - Phase 44 — Session-spanning resume. `loop.resumeFromDisk()`: rehydrates `.forge/state.json`; terminal states are respected (no zombie resurrection) unless the halt was a budget cap and the user explicitly resumes; resume grants a fresh wall-clock window and step allowance (`maxSteps = currentStepIndex + 30`) while COST accumulation persists (money spent is never forgotten); paused/mid-run states continue from their exact step. Extension: `forge-agent.resumeAgentGoal` drives to terminal/paused. Proven by: kill-and-resume continues from the same step index with prior evidence intact; budget-halted state resumes only via the explicit path; terminal success refuses resume.
   - Non-goals for 42–44: webview UI surfaces (visual proof is host-gated; harness machinery first), infinite unbounded loops (resume extends windows deliberately, never removes caps).
   - Ratchet target solve-rate only after the first clean live baseline.

7. Add context engineering.
   - Status: first context-bundle slice implemented in Phase 24.
   - Added retrieval policy, scratchpad summarization, token estimate, compaction flag, recent file/task/failure rehydration, and `.forge/context-bundle.json`.
   - Extension-host tests verify persisted context bundle goal, open task state, retrieval policy, and native artifact opening.
   - Status update: deterministic retrieval candidate slice implemented in Phase 26.
   - Added ranked `retrievalCandidates` to context bundle, `.forge/retrieval-index.json`, prompt injection, native artifact opening, and compact `ret` status counter.
   - Status update: hard token budgeting and stale tool-result clearing implemented in Phase 53.1; role-scoped worker context artifacts implemented in Phase 61.
   - Status update: opt-in cached hybrid semantic retrieval implemented in Phase 64 with causal mock, cache, failure-fallback, and contamination-boundary proofs.
   - Remaining: credential-backed semantic retrieval A/B/uplift measurement and fully independent worker-specific memory.

## Hard-Region Roadmap (Phases 50–53, specs documented before build)

- Phase 50 — MAIN-LOOP PORT: IMPLEMENTED. Both behavioral proofs green (empty-path repair with pathRepairs=1; fence-blind recovery via whole-file guidance with exemplar in reflections); proof 2 caught and fixed a streak-clear lifecycle flaw before close; regressions green. Ships to Antigravity via `npm run package` + VSIX reinstall. Original spec: the proven eval-lane gradient into `AgentHarnessLoop`. (a) Content-addressed path repair pre-validation: empty-path apply_patch resolved via `resolvePatchTargetByContent` over `state.files` (bounded, deterministic candidate policy: files the agent has actually read; unique-or-refuse, ambiguity count into the rejection detail). (b) Malformed-patch streak tracking; at ≥2 the proposal context gains whole-file guidance (STOP patches, write_file with COMPLETE content). (c) Firewall-rejection reflections for malformed patches carry the format exemplar. All counted in runStats (`pathRepairs`, `wholeFileGuidanceInjections`) — assistance visible in the product exactly as in the evals. Proven by: main-loop behavioral runs with empty-path and fence-blind scripted providers reaching success; deployment note: requires `npm run package` + VSIX reinstall to reach Antigravity.
- Phase 51 — TIER-3 SUITE slice 1: IMPLEMENTED. Structural results (information-gated mock): bare 0/5, truncated solo 1/5, swarm 5/5. Banked findings: the ORACLE PIERCES TRUNCATION for crash-class defects (test output quotes the offending line — verification feedback is a context channel); COMPRESSION IS A HAYSTACK PROPERTY (0.36 many-files vs 1.14–1.41 when one file dwarfs the budget — future rungs: chunked explorers, interlock-signature capping); judges/mocks require product-grade review (two self-inflicted validator bugs caught by proofs). Live tier-3 pending. Original spec: large-corpus fixtures where a 32k context stops being free. DETERMINISTICALLY GENERATED fixtures (no network dependency; real cloned repos are slice 2): 300–400-line files built from parameterized utility functions with one seeded defect, in three kinds — `large-file-bug` (defect deep in one big file), `haystack` (defect in one of ~12 files), `large-seam` (relationship defect across two large files). Runner reuse: `Tier2EvalRunner` gains `options.tasks` injection and the task `kind` union widens; `taskPrompt` gains a deterministic CHARACTER BUDGET with visible per-file truncation markers — the solo lane experiences honest context overflow (head of each file + '[truncated]'), while swarm explorers each see ONE file in full. This makes the compression thesis falsifiable: implementer-prompt/solo-prompt ratio must drop below 1 on tier-3 (it was 1.08–1.18 on tiny tier-2 files), and the CAUSAL proof seeds the defect BEYOND the solo lane's truncation horizon — solo structurally cannot see the bug; the swarm's explorer can. Held-out judges throughout; CLI `npm run eval:tier3` with `--swarm`; dispatch routing for new kinds defaults to `harness` until live data draws the terrain map. Non-goals this slice: real cloned repos (slice 2, host-gated for network), tier-3 features/missing-test kinds.
- Phase 52 — IMPLEMENTED. Structural results (tier-3): architect 5/5 with correct goal→file mapping on every task, ONE planning call each, 10 total calls vs swarm's 35 for the same solve rate. Lane matrix complete: bare/solo/swarm/architect/dispatch, all held-out judged, all separately metered. Live same-model ablation + true rate-split runs pending. Original spec: a fourth lane `architect` in the shared runner. ONE schema-constrained planning call (`PLAN_SCHEMA`: premiseCheck, targetFile, approach, doneWhen) against the SAME truncated corpus view the solo lane gets — planning is judgment over goal + structure, not reading; then the standard 7B implementer mini-loop (full gradient of assistance) with the architect's plan injected and the planned target file shown IN FULL. Separate model binding (`--architect-model`, defaults to the worker model for ablation purity) with SEPARATE call/cost metering — the cookbook's rate-split economics, measurable. The structural thesis the mock proves: on haystacks the architect replaces 12 explorer calls with ONE planning call (goal names the defective function; a planner maps it to its file without reading the corpus) — same 5/5 as the swarm at a fraction of the calls. Premise check carried into the implementer prompt (cookbook caveat 3, first slice). Oracle still owns truth. Non-goals this slice: architect-as-completion-judge, multi-subtask decomposition (single targetFile plans), tier-2 mock architect (physics proof lives on tier-3).
- Phase 53 — CONTEXT ENGINEERING for 32k survival on real repos: compaction at threshold, tool-result clearing, token-budget scheduler, keyword-vs-semantic retrieval A/B.

## Phase 57 — TIER-4: TASKS THAT CANNOT BE PATTERN-MATCHED (specced after live tier-3 saturation)

CONFESSION DRIVING THE DESIGN: tier-3 goals name the defective symbol, so planning reduces to goal→file regex mapping — the same-model architect's live 5/5 proves focus works, NOT that reasoning works. Tier-4's one law: **no goal may contain any symbol, file name, or string that appears at the defect site.** Enforce with a generation-time assertion (the banked truncation-horizon technique): tokenize the goal, assert zero overlap with identifiers within N lines of the seeded defect.

Task dimensions (each raises a different wall):
1. SYMPTOM-ONLY GOALS: "discounted items total wrong at checkout" — behavioral description; the planner must trace data flow to locate the cause. This is where regex-planners and reasoning-planners finally separate (re-run the DeepSeek-vs-7B architect A/B HERE).
2. CAUSAL CHAINS: fixes requiring 2-3 coordinated edits where any single edit alone leaves tests red (the true choke point; suspect-rotation and single-targetFile plans both break — plans need multi-step structure, PLAN_SCHEMA gains ordered subtasks).
3. DISTRACTORS: seeded plausible-but-innocent anomalies (weird-but-correct code, stale comments claiming bugs, near-miss function names) that pattern-matchers bite on; judge unchanged — biting a distractor costs steps and shows in the metrics.
4. SPEC-GAP FEATURES: "implement X per this behavioral spec" where the held-out judge tests edge cases the goal does NOT enumerate — measures design judgment; workspace-test authoring becomes load-bearing.
5. REAL REPOS (merge tier-3 slice 2 here): clone into Phase-35 worktrees, real bugs or reverted fixes, held-out judge = the repo's own withheld test. The final wall.
Metrics to add: located-correct-file-by-step-N (finding speed), distractor-bite rate, plan-subtask coherence. Expected honest outcome: the 7B architect stops acing; the rate-split (strong planner + 7B executor) gets its first real capability test; some cells go to zero and stay there — that residue is the true model ceiling, measured at last.

- Phase 57.2 — COORDINATED-SUBTASK EXECUTION: IMPLEMENTED. Reconciled finding: `PLAN_SCHEMA` already accepted `subtasks`, but `runArchitectLane` stored only `targetFile`/`premiseCheck`/`approach`, prompted the planner for ONE target file, ignored ordered subtasks, and repaired empty-path edits back to the single planned target. That explained the live Tier-4 residue: `t4-causal-timeout` requires `src/settings.js` and `src/service.js`; any single edit remains red. Built: (1) planner prompt explicitly requests ordered subtasks with exact file paths when a fix spans multiple files; (2) architect result records `planSubtasks` and `subtaskChecks`; (3) implementer prompt advances through one current subtask at a time, shows that subtask's referenced files in full, feeds prior subtask oracle output forward, and records a workspace/held-out check after every committed subtask; (4) empty-path repair targets the current subtask's single referenced file before falling back to the primary target; (5) Tier-4 solvability helper applies `provenFix` plus `PROVEN_EXTRA` and proves pristine fixtures fail while proven-fixed fixtures pass. Non-goals: multi-agent worker fan-out, model-as-judge, weakening held-out judges, or counting deterministic proven fixes as model solves. Validation: `npm run test` PASS; `npm run compile` PASS; mocked Tier-3 architect remains 5/5; default Tier-4 pattern-blind mock remains 0/4 after the new solvability gate; focused coordinated-provider proof solves `t4-causal-timeout` through two ordered subtasks, with subtask 1 red and subtask 2 green. Live Tier-4 rerun pending.
- Phase 57.2a — LIVE SUBTASK NORMALIZATION HOTFIX: IMPLEMENTED. DeepSeek planner + 7B executor after 57.2 produced Tier-4 live: bare 0/4, solo harness 4/4, architect 2/4 @41 calls. Important diagnosis: `t4-causal-timeout` architect plan returned `subtasks` as objects even though the schema asked for strings; the runner normalized them with `String(object)` into `"[object Object]"`, so focus-file extraction and empty-path repair lost both `src/settings.js` and `src/service.js`. Built: robust subtask normalization from either strings or common object shapes (`path`/`file`/`targetFile`, `action`/`description`/`instructions`), preserving JSON as fallback instead of `[object Object]`. Focused object-subtask proof solves `t4-causal-timeout` with subtask 1 red and subtask 2 green. Follow-up live rerun after this hotfix: bare 1/4, solo harness 2/4, architect 3/4 @45 calls, cost ~$0.011; `t4-causal-timeout` still red, but now with usable subtasks (`src/settings.js`, `src/service.js`) and diagnostic subtask checks.
- Phase 57.3 — EVAL ORACLE IMMUTABILITY: IMPLEMENTED / live rerun inconclusive. After subtask normalization, DeepSeek/7B still missed `t4-causal-timeout`; the subtask trace showed useful progress (`1000 -> 250 -> 5000 vs 42`) followed by attempts to rewrite `test.js`, producing `Cannot find module './service'`. The held-out judge prevented false success, but the runner allowed visible-oracle tampering attempts and wasted remaining steps. Built: in eval lanes, if a task already supplies `workspaceTest`, mutating `test.js` is rejected unless `task.kind === 'missing-test'`; the rejection reason feeds reflection like other firewall failures. Non-goals: blocking missing-test authoring, changing held-out judges, or claiming live improvement before rerun. Validation: `npm run test` PASS, `npm run compile` PASS, focused provider proof confirms `write_file test.js` is rejected and the source fix can still solve. Live rerun attempt exceeded 10 minutes and was stopped without writing a live report, so no live improvement claim is made.
- Phase 57.4 — LIVE-EVAL ROBUSTNESS: IMPLEMENTED. Reconciled failure: shared tier eval reports were only persisted after whole-suite completion, and provider calls had no timeout wrapper. A hung live model call could therefore leave no current report and force manual cleanup. Built: `providerCallTimeoutMs` on tier eval options; a `TimeoutProvider` wrapper around provider `generateChat`; `--call-timeout-ms` on tier-2/3/4 CLIs; tier-4 live default timeout = 90000ms; report flushing after each completed task with `partial`, `completedTaskCount`, `lastUpdatedAt`, and full scheduled `taskCount`; final report rewrites `partial: false`. Held-out judging, prompts, and solve accounting unchanged. Validation: `npm run compile` PASS; `npm run test` PASS; mocked tier-3 architect remains 5/5; mocked tier-4 architect remains 0/4; focused stalled-provider proof returns in ~2.3s with provider failures and a persisted report; two-task write interception proves the mid-suite write is `partial: true` with `completedTaskCount: 1` and `taskCount: 2`. This phase does not claim live Tier-4 improvement; it makes the next live run measurable.
- Phase 57.5 — INSTALLED ANTIGRAVITY PRODUCT PROOF: IMPLEMENTED. Reconciled gap: `kennyg.forge-agent@0.57.4` was installed and disk-verified, but that proved only packaging/current bits. Existing e2e runs a development extension host and earlier had a wrapper exit-code issue; it did not prove the installed Antigravity webview running a real `/goal`. Built/proved: Playwright/Electron can launch `Antigravity IDE.exe` with the installed extension dir, open the installed `kennyg.forge-agent` webview, enter a `/goal`, and capture UI plus `.forge` artifacts. Final proof used installed `kennyg.forge-agent@0.57.5` on a committed git fixture with failing `src/math.js`; the live model edited `a - b` to `a + b`; UI terminal state was `success`; persisted state was `success`; tests passed; `providerCalls: 3`, `providerFailures: 0`, `fallbackProposals: 0`, `actuallyModelDriven: true`; `diffReviewAttempts: 1`, `reviewerApprovals: 1`, `reviewerCritiques: 1`; diff review status `approved` and excerpt contained the math change; screenshot saved at `artifacts/antigravity-live-goal-0575-git-final.png`.
- Phase 57.5a — REVIEWER GATE PRODUCT HOTFIX: IMPLEMENTED. Product proof first found a real installed-webview failure: the model fixed `src/math.js`, tests passed, provider calls were live/model-driven, fallback was zero, but the run ended `failed` because all tasks completed without any diff review (`diffReviewAttempts: 0`, `reviewerApprovals: 0`). Root cause: the deterministic fallback knew to emit `get_diff` when green tests lacked review, but fallback only ran after provider/schema failure; a working model could skip `get_diff` and complete the Reviewer task with `record_evidence`, causing the terminal gate to fail after the fact. Built: deterministic `reviewerGateProposal` before provider calls. On reviewer/verification tasks, green tests with no required review force `get_diff`; green review without evidence forces `record_evidence`; green evidence plus required review forces `declare_success`. Non-goals kept: terminal success guard remains, diff review requirement remains, unreviewed green tests still cannot pass. Validation: focused provider that would throw if called stayed at `providerCalls: 0`, recorded a diff review, and reached success; installed Antigravity webview proof rerun terminaled `success`.

## Phase 54 — UX PARITY (specced, requires visual-gated session: host visual smoke or screen-driven validation of the live Antigravity panel; code-only closes are ◐ by definition here)

Reconciled against what Cline/Kilo/Roo/Codex ship as standard vs Forge today. Priority order:
1. SETTINGS SURFACE: a real settings panel (native VS Code `contributes.configuration` + webview mirror) for: OpenRouter API key (secret storage, not env-only), default model/role bindings, budget caps (maxCostUsd/wall-clock/maxSteps presets), reflectionEnabled, architect model, auto-run-tests toggle. Today these are env vars and code defaults — the #1 clunk.
2. RUN CONTROL IN UI: Stop/Pause/Resume buttons wired to the EXISTING pauseGoal/resumeGoal/steerGoal commands (machinery built in Phase 43, has no buttons); live status line showing current step/task/budget spend; /goal contract preview card before Run.
3. STREAMING + HISTORY: stream tokens into the chat (provider layer has no streaming path — known Phase 0 gap); persist chat/run history across panel reloads (.forge/session-history.json); restore last run state on open (resumeFromDisk exists — surface it as a "Resume last run" banner).
4. DIFF REVIEW UX: proposed-change cards with per-file accept/reject BEFORE commit (firewall already gates; add the human gate as an optional mode), open-diff buttons per mutation in the run log.
5. CONTEXT ATTACH: @file / @folder mentions in the composer feeding state.files (bounded candidates for content addressing get better for free).
6. POLISH: error toasts instead of console-only errors, empty-state onboarding (the /goal syntax card), keyboard shortcuts, lane picker (solo/swarm/architect) in the Run flow surfacing the eval-proven modes to real use.
Validation protocol for whoever executes: every item closes ONLY with a screenshot of the live panel exercising it (x4-forge-confirm-style per-task visual close-out), plus the existing test:visual gate extended per surface.

## Lessons Adopted from Anthropic's "Plan Big, Execute Small" Cookbook (2026-07, reconciled against our live swarm data)

- RATE SPLIT IS THE ENGINE: multi-agent pays when the token-heavy leg bills at a cheaper rate than the judgment leg (their team: 84–98% of input at worker rate → 2.5× cheaper, 3× faster at matched rigor). Our 7B-coordinating-7B swarm has no split, which explains its measured 2.5× cost for no headline gain. → Next major experiment: ARCHITECT/EDITOR lane — mid-tier planner + 7B executors, fourth lane in the tier-2 A/B.
- BRIEF GRANULARITY HAS AN OPTIMUM: their "more, narrower briefs raised the bill" independently replicates our tiny-fixture overhead finding (1.08–1.18×). → Size-gate explorer granularity: no worker below a file-size threshold; batch small files into one brief.
- THE STANDARD ONLY COVERS WHAT'S IN IT: their premise error (wrong park list, perfectly verified) shows decomposition needs its own audit. Our held-out judge audits fixes, not plans. → Add a premise/plan verification pass (one cheap delegation or deterministic check) before execution.
- CHEAP READERS SUMMARIZE AWAY JUDGMENT: their third failure case is our seam-blindness, generalized. Deterministic extracts (interlocks) beat smarter summaries for structural facts; raw slices beat summaries when small. Banked as a swarm design law.
- METERING: adopt per-worker cost attribution in the swarm report (explorer vs implementer split), matching their per-thread usage discipline.

## Non-Goals / Scrapped IDE Assumptions

## Phase 69 - Structured Clarification Gates

Status: **Implemented and visually validated**.

Forge now closes a major autonomy/safety gap between blind assumption and manual pause. The proposal schema carries confidence and material uncertainty, and exposes `ask_user` as a non-mutating structured tool. Deterministic code rejects any ordinary proposal when the model reports material uncertainty, persists focused questions under `.forge/clarifications.json`, and holds the run in `awaiting_input` without additional provider calls. The normal bottom chat composer records the answer and resumes the same run with the answer in required prompt context.

Boundaries remain deliberate: the agent must investigate repository facts itself; questions are for user-owned intent, scope, authorization, behavior, cost, and acceptance decisions. Requests are capped at three and duplicate questions terminate rather than loop. This is not proof that every model calibrates uncertainty well; it is proof that self-reported uncertainty cannot silently mutate the workspace.

## Phase 70 - Typed Project Adapters and Composite Build Oracle

Status: **Implemented, extension-host validated, visually inspected, packaged, and installed in Antigravity**.

Reconciled defect: oracle command selection is spread across `oracles.ts`, is not represented as typed durable state, and terminal `lastOraclePass` currently mirrors only `runTest()`. A workspace can therefore have passing tests plus failing lint/typecheck and still acquire green test evidence. Build is also conflated with typecheck and has no independent status.

Bounded implementation:

- Add a deterministic project-adapter registry for Node, Python, Rust, Go, and unknown workspaces. Adapters record ecosystem, manifest, package manager, detection evidence, and exact test/lint/typecheck/build commands with required/skipped semantics.
- Persist the selected contract at `.forge/project-adapter.json`, include it in required model context, and expose it through native artifact access. Models cannot author adapter commands.
- Refactor verification oracles to execute the adapter contract and return command/provenance/skipped metadata.
- Add a distinct build oracle and define composite green as every required detected oracle passing, with tests always required. Evidence must record the composite command/result details, not a test-only claim.
- Add disposable Node, Python, Rust/Go detection, unknown/no-test, failing-build, and green-test/red-typecheck cases. Prove a red required oracle cannot yield `lastOraclePass`, green evidence, or success.

Non-goals: arbitrary plugin execution, framework-specific test generation, downloading missing toolchains, user-authored shell templates, monorepo package graph scheduling, or claiming support beyond deterministic root-level detection.

Implemented result: `projectAdapters.ts` deterministically selects Node, Python, Rust, Go, or unknown contracts and records manifest, package manager, evidence, fingerprint, and exact required/skipped oracle commands. `.forge/project-adapter.json` persists at initialization and opens through the native artifact command. `VerificationOracles.runAll()` executes separate lint/typecheck/build/test results and computes composite green only when every required result passes; `lastOraclePass` and green evidence use only that composite. Read/search/plan proposals no longer trigger wasteful full builds.

Proof: Node lockfile selection, Python Ruff/Mypy, Rust Cargo, Go module, and unknown/no-test detection pass in the extension host. An explicit passing-test/failing-build harness state remains red and writes no green evidence. The verification matrix now has 11 passing cases, including build failure and composite typecheck failure. `npm run compile`, `npm run test`, `npm run test:workers` (100/100), `npm run test:e2e` (102.7s), and `npm run test:visual` pass. Inspected `artifacts/visual-smoke-run.png` visibly shows `tests pass · build fail · proj node/npm`. `forge-agent-0.70.0.vsix` packages 36 files/212.5 KB; Antigravity lists `kennyg.forge-agent@0.70.0`, and installed runtime inspection confirms adapter and composite-oracle code.

## Phase 71 - Typed Oracle Failure Remediation Capsules

Status: **Implemented, causally extension-host tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled gap: composite oracle truth is now correct, but red-oracle retries receive a generic blocker plus raw log text. The harness does not preserve a typed history of which command failed, whether the same failure repeated unchanged, what repair policy applies, or when the failure was resolved. This weakens cheap-model convergence and evidence auditing.

Bounded implementation:

- Convert each failed required adapter result into a deterministic capsule containing kind, command, source, normalized signature, bounded output, occurrence count, task/role ownership, and category-specific remediation guidance.
- Persist `.forge/oracle-failures.json`; resolve open capsules only after the complete adapter oracle turns green.
- Inject the latest open capsules as required retry context. Models may consume but cannot author/resolve them.
- Count captures, repeated identical failures, guidance injections, and resolutions in run statistics and compact telemetry.
- Causally prove scripted weak workers repair build, typecheck, lint, test, and missing-test-contract failures only after the typed guidance is present. Repeated identical failures must increment one capsule rather than create unbounded duplicates.

Non-goals: model-generated truth classification, changing oracle commands, weakening tests/configuration to manufacture green, framework-specific test synthesis, or guaranteeing that arbitrary live models follow the guidance.

Implemented result: every failed required adapter oracle becomes a deterministic capsule with kind/category, exact command and source, normalized SHA-256 signature, bounded output, task/role ownership, occurrence count, and a fixed anti-cheating remediation policy. Repeated unchanged failures fold into the open capsule. `.forge/oracle-failures.json` persists the lifecycle and opens natively. Required model context always includes open capsules; only a fully green composite adapter run resolves them. Run statistics and the compact footer expose captured/repeated/resolved counts as `fix C/R/X`.

Causal proof: five scripted weak workers were constructed for test, lint, typecheck, build, and missing-test-contract failures. Each provider refused to repair unless its typed category and `Guidance:` appeared in the actual `AgentHarnessLoop` system prompt. After one validated `write_file`, every lane reached composite green and its capsule resolved. The build lane executed the same red oracle twice first and preserved one capsule with `occurrences: 2`. This proves guidance delivery and lifecycle causality, not live-model uplift.

Validation: final `npm run compile`, `npm run test`, `npm run test:workers` (100/100), `npm run test:e2e` (final combined gate 157.3s; host briefly recovered from unresponsive), `npm run test:visual`, and `git diff --check` pass. Inspected `artifacts/visual-smoke-run.png` visibly shows red build state plus `fix 1/1/0`. `forge-agent-0.71.0.vsix` contains 37 files/215.26 KB. Antigravity forced install succeeds, lists `kennyg.forge-agent@0.71.0`, and installed runtime contains occurrence and green-resolution logic.

## Phase 72 - Signature-Based Oracle Stagnation Control

Status: **Implemented, causally extension-host tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled defect: the generic progress signature includes timestamps and growing reflection/blocker records, so repeatedly executing the same red oracle can appear as progress until reflection exhaustion. That path ends `failed`, obscuring that the agent made no causal improvement. Phase 71 now supplies stable failure signatures suitable for a stricter rule.

Bounded implementation:

- Treat three occurrences of the same still-open required-oracle signature as deterministic stagnation.
- Preserve the existing second-reflection escalation opportunity, but stop before a fourth repeated attempt.
- Terminal as honest `gave_up`, classify a `no_progress` blocker, preserve the capsule/output/attempt evidence, and never create green evidence or success.
- A changed failure signature counts as a changed approach and must not trigger the old signature's threshold. Eventual composite green still resolves all capsules normally.
- Add causal fixtures for unchanged red repetition, changed red output, and red-red-green recovery. Surface the threshold compactly without a new panel.

Non-goals: semantic judgment that two different diagnostics share one root cause, infinite retries, automatic budget extension, or replacing the existing global progress detector.

Implemented result: after any oracle-bearing action, Forge checks the most-repeated open failure capsule before generic tool/red-oracle reflection handling. The second identical result remains retryable and reaches the existing escalation machinery. The third identical signature increments `oracleStagnationHalts`, records a `no_progress` blocker, preserves the capsule and diagnostics, and terminals as `gave_up` before a fourth provider call. Green evidence and success remain absent. Distinct signatures do not share occurrence counts.

Causal proof: an unchanged scripted worker emitted `run_tests` against the same diagnostic exactly three times; Forge stopped at three provider calls with `gave_up`, one capsule occurrence 3, one no-progress blocker, and no green evidence. A second fixture changed the diagnostic twice and retained two independent occurrence-1 capsules without stagnation. A third fixture produced the same red signature twice while changing source state, then reached green on its third edit; the capsule resolved and `oracleStagnationHalts` remained zero. The first host run exposed that failed `run_tests` enters tool-failure handling before the later red-oracle branch; enforcement was moved to the shared post-oracle/pre-tool-failure boundary and the full suite then passed.

Validation: final `npm run compile`, `npm run test`, `npm run test:workers` (100/100), `npm run test:e2e`, `npm run test:visual`, and `git diff --check` pass in the 170.8s combined gate. Inspected `artifacts/visual-smoke-run.png` shows the explicit honest-stop narration, `tests pass · build fail`, `fix 1/2/0`, and `stuck 1`. `forge-agent-0.72.0.vsix` packages 37 files/215.55 KB. Antigravity forced install succeeds and lists `kennyg.forge-agent@0.72.0`; installed runtime contains the threshold and halt logic.

## Kilo Parity Roadmap

Forge should preserve its deterministic harness advantage while closing the product gaps demonstrated by Kilo Code. Priority order:

1. **Checkpoint history and user restore — IMPLEMENTED (Phase 73):** compact timeline, full pre-mutation product snapshots, native diff, two-click restore, bounded history, and mandatory downstream-proof invalidation.
2. **Browser validation tool — IMPLEMENTED (Phase 74):** Reviewer/Escalation can validate loopback applications in detected host Edge/Chrome, with machine-readable diagnostics and screenshots opened through native IDE surfaces.
3. **Streaming progress — IMPLEMENTED (Phase 75):** stream ordered harness-authored transitions into one compact chat activity group while deterministic state remains authoritative.
4. **First-run onboarding/provider readiness — IMPLEMENTED (Phase 76):** two-step workspace/provider setup, secret-vault credentials, no-spend readiness probes, and explicit live/fallback catalog provenance.
5. **Custom user modes — IMPLEMENTED (Phase 77):** host-owned immutable built-ins and validated custom modes enforce bounded role presets through tool-permission intersection rather than prompt-only personas.
6. **Feedback/support entrypoint — IMPLEMENTED (Phase 78):** one compact global Help action generates a privacy-minimized diagnostic artifact, opens it natively, and hands off to GitHub without adding a permanent panel.

Kilo features are accepted only when they map onto native IDE surfaces and preserve `PROPOSE -> VALIDATE -> COMMIT -> NARRATE`, composite oracle truth, transaction safety, and evidence invalidation.

## Phase 73 - User-Facing Checkpoint History and Safe Restore

Status: **Implemented, causally extension-host tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled gap: Forge creates manifest-backed checkpoints but the installed extension exposes only a raw artifact. Product-loop edit checkpoints are targeted snapshots, so they are not equivalent to Kilo's “return to a previous step” behavior. No public restore API reconciles state, and restoring bytes without invalidating later evidence would permit stale success claims.

Bounded implementation:

- Product-loop mutations create full workspace snapshots while retaining targeted checkpoints as the lower-level/default API for narrow safety tests.
- Add an authenticated-by-state restore method and extension command/bridge. A checkpoint ID must exist in the current persisted run; arbitrary paths are rejected.
- Restore workspace bytes, then invalidate all evidence, diff reviews, terminal/AAR state, oracle/remediation truth, and workflow stages at or after implementation. Reopen Editor/Reviewer tasks and require fresh verification.
- Persist a checkpoint-restore ledger recording checkpoint, protected scope, invalidated proof counts, result, and timestamp.
- Add a compact history popover next to the composer with step/tool/scope/time, native diff inspection, and two-click restore confirmation. No permanent panel or cloned source-control UI.
- Prove complete workspace rollback across multiple files, deletion of files created after the checkpoint, missing/forged-ID rejection, stale-proof invalidation, and successful fresh verification after restore.

Non-goals: Git commit creation, arbitrary branch switching, restoring excluded dependency/build directories, retaining post-checkpoint evidence, or presenting targeted snapshots as full timeline restores.

Implemented result: product-loop mutations now call the existing firewall checkpoint API with `workspace-snapshot`, while direct/default checkpoint calls retain targeted semantics for narrow recovery. Snapshots exclude dependency/build/cache/runtime artifact directories, request filesystem clone-on-write where supported, and retain the latest 20 entries. `restoreCheckpoint` accepts only an ID present in current persisted state, restores bytes, removes post-checkpoint files, clears evidence/diff/reviewer/precommit/oracle/AAR truth, resolves open blockers as invalidated, reopens Editor/Reviewer tasks, resets workflow from implementation onward, and requires fresh composite verification. `.forge/checkpoint-restores.json` records successful and failed attempts.

Product UX: the bottom composer now has a standard history icon. Its compact popover shows up to eight recent action checkpoints with tool, step, snapshot strategy, scope, and time. Each entry offers native Diff and a two-click Restore → Confirm action; the confirmation copy states that workspace bytes return to the saved step and review/verification rerun. This adds no permanent panel and uses host-native diff surfaces.

Causal proof: a full snapshot restored two independently changed files and deleted a third file created later. A forged ID was rejected before filesystem access; a current-state ID with a missing manifest recorded one failed restore. Successful restore invalidated one green evidence entry, one approved diff, oracle lifecycle state, terminal success/AAR, and completed implementation-through-close workflow stages. A direct fresh composite oracle run then reacquired green evidence. Product-loop checkpoints are asserted to be full workspace snapshots; direct firewall checkpoints remain targeted and still restore correctly.

Validation: final post-bounding `npm run compile`, `npm run test`, `npm run test:workers` (100/100), `npm run test:e2e`, `npm run test:visual`, and `git diff --check` pass; final release gate took 182.7s. Inspected `artifacts/visual-smoke-run.png` visibly shows the checkpoint popover, `workspace-snapshot`, native Diff, red Confirm state, and fresh-verification warning. `forge-agent-0.73.0.vsix` packages 37 files/217.7 KB. Antigravity forced install succeeds and lists `kennyg.forge-agent@0.73.0`; installed runtime contains restore invalidation, retention 20, and copy-on-write snapshot logic.

## Phase 74 - Agent Browser Validation Tool

Status: **Implemented, causally agent-loop and extension-host tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled gap: Forge can run deterministic build/test oracles and its own test harness can take webview screenshots, but an installed agent cannot inspect the application it builds. Kilo's demonstrated workflow treats rendered application behavior as evidence. A test-only Playwright script does not close this product gap.

Contract:

- Add `browser_validate` as a structured, firewall-validated Reviewer/Escalation tool. The model supplies a URL and optional expected visible text; it never receives direct browser or filesystem authority.
- Default policy permits only `http`/`https` loopback targets (`localhost`, `127.0.0.1`, and `::1`), rejects credentials and unsafe schemes, and bounds timeout/output sizes. Remote browsing requires a future explicit permission design.
- The installed extension packages `playwright-core` but no browser binary. It launches a detected host Edge/Chrome executable, preserving a practical VSIX size and avoiding an embedded cloned browser.
- Persist each result as `.forge/browser-runs/<id>.json` plus PNG, and refresh `latest-browser-validation.json` / `.png`. Evidence includes requested/final URL, title, visible-text excerpt, expected-text match, console errors, page errors, failed requests, screenshot path, duration, and browser identity.
- A navigation failure, missing expected text, page error, or error-level console message is a failed tool result. Failed browser validation cannot create green evidence or terminal success. Passing validation adds a distinct browser evidence entry but does not replace required project oracles, diff review, or success gates.
- Add compact discoverability near the composer and open JSON/PNG through native editor surfaces. Do not add a permanent browser panel, fake browser, or embedded IDE surface.

Required proof:

- Causal fixtures cover a valid local page, expected-text miss, console/page error, unreachable target, forbidden remote URL, and a real agent-loop proposal.
- Extension-host proof opens the latest JSON and PNG artifacts through native commands.
- Visual smoke shows compact browser evidence discoverability without expanding the run console into another permanent panel.
- Compile, static tests, worker stress, extension-host tests, visual tests, package inspection, Antigravity install, and an inspected real browser-captured PNG must pass before this phase is marked implemented.

Non-goals: starting or supervising arbitrary development servers, remote-site browsing, browser form mutation, authenticated browsing, DOM scripting supplied by the model, replacing unit/integration tests, or bundling Chromium in the VSIX.

Implemented result: `browser_validate` is a typed Reviewer/Escalation tool with schema-declared `url`, `expectedText`, and bounded `timeoutMs`. The firewall accepts only credential-free HTTP(S) loopback targets. A short-lived role worker launches detected host Edge/Chrome through packaged `playwright-core`, captures title/final URL/body text, console and page errors, failed requests, and a full-page PNG, then writes immutable run artifacts plus `latest-browser-validation.json/png`. Passes create a distinct non-test evidence entry; failures enter normal tool-failure reflection. Browser evidence cannot set `lastOraclePass`, satisfy diff review, or terminal success.

Product/API surface: `forge-agent.runBrowserValidation` exposes direct deterministic testing; `forge-agent.openArtifact` opens latest JSON and PNG through native IDE surfaces. The composer adds one status-colored globe and the existing compact status line adds `web pass/fail` counts. No persistent browser panel, fake browser, or cloned host surface was added.

Causal proof: `npm run test:browser` served real loopback pages and proved successful text matching, expected-text failure, error-level console failure, unreachable navigation, remote/file/credential URL rejection, and a scripted Reviewer model proposal through the real `AgentHarnessLoop`. The agent-loop case recorded browser evidence and a PNG while preserving `lastOraclePass=false` and refusing terminal success. The final capture used Microsoft Edge at `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`; the inspected PNG visibly showed `Visible Forge application` and the `Run task` control.

Validation: final combined `npm run compile`, `npm run test`, `npm run test:browser`, `npm run test:workers` (100/100), `npm run test:e2e`, `npm run test:visual`, and `git diff --check` pass in 229.5s. Extension-host E2E ran the registered command against a real local page and opened both report and PNG natively. Inspected `artifacts/visual-smoke-run.png` shows the compact green browser icon and `web 1/0` without a new panel. `forge-agent-0.74.0.vsix` packages 144 files/3 MB, contains `playwright-core` but no browser binary, installs successfully, and Antigravity lists `kennyg.forge-agent@0.74.0`. The installed module resolves, accepts loopback, and rejects a remote URL.

## Phase 75 - Trustworthy Streaming Run Progress

Status: **Implemented, causally timing-tested, extension-host tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled gap: the installed webview receives a complete `state-update` only after `runStep` returns. Provider waits, validation, worker execution, commands, browser checks, and oracles therefore appear as an unexplained pause. Kilo-style UX requires continuous activity, but streaming unvalidated model prose would conflict with Forge's deterministic authority boundary.

Contract:

- Add ordered typed progress events for run start, step start, provider wait, proposal, deterministic validation, tool start/result, oracle result, reflection/retry, clarification wait, pause/resume, and terminal state.
- Every event carries immutable ID, monotonic session sequence, session ID, step index, role, active task, workflow/firewall phase, status, bounded summary/detail, optional tool name, and timestamp.
- Emit from the harness at authoritative transition points. Provider narration is shown only as a proposed action after parsing; no token stream may imply that an unvalidated action happened.
- Persist a bounded per-session event history in state plus `.forge/progress-events.jsonl`; state reload/resume rehydrates the same ordered timeline without duplicating events.
- Bridge events immediately through the extension host and render one compact `aria-live` activity block inside the existing chat timeline. Auto-scroll while active, retain the latest bounded events, and avoid a permanent activity panel or one large chat card per event.
- Full state snapshots remain authoritative and continue after each step. Progress events are observability, not mutable commands or success evidence.

Required proof:

- Causal delayed-provider/worker fixture proves events arrive before `runStep` resolves and ordering is monotonic.
- Rejected proposal, successful tool/oracle, reflection, ask gate, and terminal paths emit honest statuses without false commit/success events.
- Reload reconciliation deduplicates event IDs and restores ordered persisted history.
- Visual smoke shows a compact in-chat activity timeline with current role/tool/status and the composer still anchored at the bottom.
- Full compile, static, worker, extension-host, visual, package, Antigravity install, and screenshot inspection gates pass before implementation status is claimed.

Non-goals: token-by-token chain-of-thought, exposing hidden reasoning, treating progress as evidence, replacing persisted state snapshots, adding a log dashboard, or allowing webview messages to mutate harness event history.

Implemented result: `RunProgressEvent` records immutable ID, monotonic sequence, session/step identity, kind/status, bounded summary/detail, role/task, firewall phase, optional tool, and timestamp. The harness emits at run/step start, provider wait, parsed proposal, deterministic validation, tool start/result, composite oracle, reflection, clarification wait, pause/resume/checkpoint restore, and terminal state. A bounded 300-event history persists in session state and `.forge/progress-events.json`; every event also appends to `.forge/progress-events.jsonl`. Listener and persistence failures are isolated from harness correctness.

Product UX: the extension host forwards each event immediately as `run-progress` while retaining authoritative `state-update` snapshots after steps. The existing chat renders one `aria-live` run-activity group with status dots, role, concise summary, tool/phase, latest detail, and a 12-row viewport. Event IDs deduplicate live delivery against reload hydration. Direct chat-scroller `scrollTop` management keeps the composer anchored without moving the surrounding webview. No log panel or per-event card clutter was added.

Causal proof: `npm run test:progress` held a provider call behind a promise and observed `provider_wait` before `runStep` resolved, then proved strict provider-wait -> proposal -> validation -> tool-start -> tool-result ordering. Separate real-loop fixtures proved a green oracle event, remote-browser validation rejection plus reflection with no false tool-start, and an ask gate producing `awaiting_input`. Persisted event IDs were unique and sequences monotonic. Extension-host tests prove initialization creates persisted progress state/JSONL.

Validation: final full release gate `npm run compile`, `npm run test`, `npm run test:progress`, `npm run test:browser`, `npm run test:workers` (100/100), `npm run test:e2e`, `npm run test:visual`, `npm run package`, and `git diff --check` passed in 223.1s. Screenshot inspection caught whole-webview movement from `scrollIntoView`; it was replaced with chat-local scrolling, then compile/static/visual/package/diff/install gates passed again in 35.9s. The corrected `artifacts/visual-smoke-run.png` visibly shows eight ordered activity rows, explicit red oracle and terminal `gave_up`, usable checkpoint history, and the bottom composer. `forge-agent-0.75.0.vsix` packages 144 files/3.01 MB and Antigravity lists `kennyg.forge-agent@0.75.0`; installed harness/webview markers confirm JSONL, provider-wait, listener, and activity UI code.

## Phase 76 - First-Run Onboarding and Provider Readiness

Status: **Implemented, secret-vault and extension-host tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled gap: the settings copy claims API keys use secret storage, but `forge.openRouterApiKey` is a public string configuration and the provider reads it directly. Model refresh catches every error and silently returns a three-model fallback, so missing credentials, invalid credentials, network failure, and a healthy live catalog are visually ambiguous. A new user can press Run without knowing whether a workspace or usable provider exists.

Contract:

- Store OpenRouter credentials only in `ExtensionContext.secrets`; never send stored key bytes to the webview, logs, state, artifacts, diagnostics, errors, or configuration. Migrate a legacy plaintext setting once, clear it immediately, and report only configured/not-configured plus source category.
- Add typed provider readiness with provider, workspace status, credential status, authentication/connectivity probe, live catalog count, latency, checked time, sanitized blocker codes/messages, and final `ready` boolean.
- OpenRouter readiness uses authenticated key metadata plus catalog endpoints without spending completion tokens. OpenAI-compatible readiness probes its configured `/models` endpoint and requires no Forge-managed credential.
- Replace silent catalog fallback ambiguity with explicit `live`, `fallback`, or `error` provenance. Fallback models remain selectable offline but do not make an unavailable provider look ready.
- On first run, render one compact centered setup flow in the existing chat area: open workspace, enter/save key, verify provider/catalog, then start. Use a password field, clear it immediately after submission, and provide retry/change-key actions. Do not force advanced model-role or firewall settings into onboarding.
- Run controls refuse provider-backed work with an actionable readiness message, while native settings/proof artifacts remain available. No Forge account, hosted credits, OAuth proxy, or key escrow is introduced.

Required proof:

- Unit fixtures cover valid/invalid key, network failure, live/fallback catalog provenance, local-provider success/failure, and sanitized errors containing no submitted key.
- Extension-host tests prove readiness commands expose no secret, legacy configuration migration clears plaintext, and persisted Forge artifacts contain no test credential.
- Reload retains configured status through secret storage without returning key bytes to the webview.
- Visual smoke captures missing-key onboarding and ready-state normal chat separately at desktop/sidebar dimensions.
- Full compile, static, extension-host, visual, package, Antigravity install, and screenshot inspection gates pass before this phase is marked implemented.

Non-goals: Forge-managed billing or credits, storing keys in workspace files/localStorage, displaying masked key fragments, remote account login, automatic paid model calls during readiness, or replacing advanced settings.

Implemented result: `forge.openRouterApiKey` was removed from contributed configuration and `OpenRouterProvider` no longer reads configuration. Activation migrates a legacy global/workspace/workspace-folder value into `ExtensionContext.secrets`, clears every populated plaintext scope, and supplies the provider through an in-memory runtime bridge; environment fallback remains explicit. Save/get/clear commands and webview messages return only configured/source/valid metadata. A 30-second readiness cache coalesces initial UI/catalog probes and invalidates immediately after key changes.

Readiness API: `probeProviderReadiness` reports workspace, provider, credential requirement/source/validity, no-spend authentication latency, live/fallback/error catalog provenance/count, checked time, sanitized blocker codes/messages, and final readiness. OpenRouter uses authenticated `/api/v1/key` plus catalog access; missing/invalid/unreachable states remain distinct and anonymous catalog availability does not imply credential readiness. OpenAI-compatible mode probes configured `/models` and requires no Forge-managed key. The model picker labels live versus fallback provenance instead of silently presenting fallback as success.

Product UX: a red readiness state replaces only the empty chat body with a compact two-step card: workspace and provider. The OpenRouter key field is password-protected, never prefilled, clears immediately after submission, and offers save/check plus change-key. Local providers offer retry. Green readiness automatically returns to normal chat/activity. Advanced routing remains in Settings, which now includes a compact metadata-only readiness card. The UI run action and host bridge both refuse unready provider work with the first actionable blocker.

Causal proof: `npm run test:readiness` proved valid and invalid auth, network failure, anonymous live versus fallback provenance, missing workspace/key, local success/failure, migration without overwriting an existing secret, and serialization containing neither a recognizable submitted key nor authorization metadata. Extension-host E2E saved a disposable key through the real secret vault, read configured status on a later command, received no key bytes, cleared it, and found no key in workspace artifacts. A post-gate repository scan found neither test credential outside its defining test source.

Validation: final `npm run compile`, `npm run test`, `npm run test:readiness`, `npm run test:progress`, `npm run test:browser`, `npm run test:workers` (100/100), `npm run test:e2e`, `npm run test:visual`, `npm run package`, and `git diff --check` pass in 242.4s. Inspected `artifacts/visual-smoke-onboarding.png` shows one centered setup card, green workspace, empty password field, actionable Save & check, and no transient popover. High-detail inspection of `artifacts/visual-smoke-run.png` confirms explicit ready state returns to the complete normal chat/activity/composer UI. `forge-agent-0.76.0.vsix` packages 145 files/3.01 MB; Antigravity lists `kennyg.forge-agent@0.76.0`. Installed manifest lacks the plaintext setting and installed runtime contains secret, migration, readiness, and blocker markers. Existing user configuration migrates when the installed extension next activates; CLI installation alone does not claim that activation occurred.

## Phase 77 - Trusted Built-In and Custom Modes

Status: **Implemented, causally tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled gap: the composer advertises Code, Architect, Ask, Reviewer, Simplifier, Skeptic, Debug, Plan, and Test Engineer, but selection only prefixes the final chat message. The extension host does not resolve a mode, `/goal` always starts the same full harness, and the webview could claim arbitrary mode semantics without changing tool authority. This is cosmetic persona switching, not Kilo-style operational modes.

Implemented result: the extension host owns nine immutable built-ins and at most 20 validated custom modes in `globalState`. The webview sends only a mode ID; the host resolves intent, role/model defaults, instructions, and allowed tools. Advisory modes cannot start mutating runs. Code-mode permissions intersect the existing role ceiling and cannot add authority; required workflow/proof tools remain mandatory. Manual stepping resumes the host-owned state rather than trusting webview-supplied state. Custom-mode editing stays collapsed in Settings and the selected mode remains one composer chip.

Causal proof: `npm run test:modes` creates, persists, and deletes a custom mode; rejects malformed, duplicate, over-limit, and built-in mutation requests; proves a Reviewer custom mode can remove `run_command` while retaining green `run_tests`; and proves an attempted Editor grant cannot cross the Editor role ceiling. Extension-host tests cover persistence across command calls, caller-supplied trust metadata, advisory-run rejection, unknown-mode rejection, and webview-state tampering. `artifacts/visual-smoke-modes.png` shows the bounded editor only after expansion; `artifacts/visual-smoke-run.png` shows the selected custom mode without a new panel. `forge-agent-0.77.0.vsix` was installed and Antigravity reported `kennyg.forge-agent@0.77.0`.

## Phase 78 - Privacy-Minimized Feedback and Support

Status: **Implemented, privacy-tested, extension-host tested, visually inspected, packaged, and installed in Antigravity**.

Reconciled gap: Kilo offers an obvious product feedback route. Forge had no equivalent, leaving users to locate the repository manually and forcing bug reports to omit useful run context or disclose arbitrary workspace state. A large support panel would regress the compact extension UX, while dumping raw state would expose prompts, source, paths, commands, and credentials.

Contract:

- Add one global Help icon beside Settings, not another composer control or permanent panel.
- Generate host-owned `.forge/support/latest-support-report.json` and `.md` with extension/IDE/runtime versions, workspace basename, provider readiness categories, run phase/status, oracle statuses, and bounded numeric counters.
- Exclude source code, goal text, prompts, chat, command output, logs, credentials, authorization metadata, and full filesystem paths by construction rather than post-hoc text redaction.
- Open the Markdown report in the native editor, copy it to the native clipboard, and offer an explicit GitHub issue handoff. No model or provider call is needed beyond the existing no-spend readiness cache/probe.
- Expose `forge-agent.reportProblem` with side-effect controls for deterministic extension-host testing.

Non-goals: automatic issue submission, account login, telemetry upload, attaching source files, silently opening external URLs, collecting arbitrary logs, or claiming a public support community exists.

Implemented result: `supportBundle.ts` emits schema-versioned JSON and readable Markdown containing only extension/IDE/runtime versions, workspace basename/presence, categorical provider readiness, run status/phase/mode, oracle statuses, and an allowlisted set of numeric counters. Reports use workspace `.forge/support` when a folder is open and extension global storage before a folder opens. `forge-agent.reportProblem` can open the native document, copy it, and offer an explicit GitHub issue action; deterministic test options suppress all three UI effects. The Help icon lives in the global header beside Settings, not in the already-dense composer.

Causal/privacy proof: `npm run test:support` injects an OpenRouter-shaped secret, authorization metadata, private goal, private source, blocker message, and absolute fixture path, then proves none appear in JSON or Markdown. A second no-workspace case proves global-storage output. Extension-host E2E invokes the registered command through VS Code, verifies both artifacts, and rechecks secret-vault non-disclosure. `artifacts/visual-smoke-support.png` was inspected at high detail: the `?` action is visible beside Settings and the composer has no new control or panel.

Validation: extension-host E2E passed in 150.4s. The remaining release gate passed in 116.4s: compile, static invariants, trusted modes, support privacy, readiness, ordered progress, real Edge browser proof, worker stress 100/100, visual smoke, package, and diff checks. The final pre-workspace correction then passed static, support, visual, package, and diff checks in 31.2s. `forge-agent-0.78.0.vsix` contains 147 files, is 3,164,227 bytes, and installs successfully; Antigravity reports `kennyg.forge-agent@0.78.0`. Installed markers confirm the command, support runtime, Help UI, and global-storage fallback.

## Phase 79 - Durable Session Memory and Frictionless Resume

Status: **Implemented, adversarially tested, extension-host tested, visually inspected at desktop/sidebar widths, packaged, and installed in Antigravity**.

Reconciled gap: the harness already snapshots state under `.forge/sessions/<id>`, and the extension contains dormant list/load/pin/save-chat message cases. The webview never calls or renders them, chat is never saved, loading a snapshot does not rehydrate the host harness, and raw webview session IDs are joined directly into filesystem paths. A corrupt index can also erase the whole visible history. This is persistence plumbing, not trustworthy session memory UX.

Contract:

- Add a host-owned `SessionStore` that accepts only canonical Forge session IDs, refuses symlink directories, verifies state/session identity, bounds metadata and chat, reconstructs the index from actual session directories, and skips corrupt entries without hiding healthy sessions.
- Persist at most 500 bounded user/assistant chat messages per session with no arbitrary webview fields. Keep run state, evidence, progress, model/cost counters, and chat attached to the same session identity.
- Add one global recent-sessions icon and compact popover. Show title, terminal/resumable status, age, pin, Open/Resume, and two-step Delete without a permanent history page.
- Opening a session makes its validated snapshot the active compatibility copy and rehydrates the host harness. Resume continues the exact run with existing evidence/cost/mode and the standard bounded additional-step window; terminal sessions open read-only rather than resurrecting.
- On panel activation, load the active state into both webview and host authority. Refresh session metadata after state/chat changes and show corrupt-entry count without exposing raw parse errors.

Non-goals: cloud sync, cross-workspace session merging, semantic search over conversations, unbounded transcripts, deleting the currently active session, silently resuming provider spend, or treating chat text as verification evidence.

Implemented result: `SessionStore` reconstructs a 200-entry index from real directories, validates canonical Forge IDs, rejects symlink/session-identity mismatches and traversal, bounds state/meta/chat bytes, keeps only 500 schema-reduced user/assistant messages, and atomically writes index/chat/pointer changes. Run sessions retain state/evidence/progress/cost/mode; chat-only Ask/Architect conversations receive distinct `forge-chat-*` identities and never masquerade as resumable runs or evidence. `.forge/active-session.json` restores the last run or conversation on panel activation. Opening a run replaces harness `latestState`; opening chat-only memory clears stale run authority. Resume is explicit and only available for idle/running/paused runs; terminal and clarification-waiting sessions are Open-only. Active deletion is rejected.

Product UX: one global Recent Sessions icon opens a compact 20-row popover with title, status, step, cost, timestamp, active highlight, pin, explicit Open, conditional Resume, and two-step Delete. Corrupt entries are summarized as skipped without raw parse/path details. Chat autosaves after a bounded debounce and session metadata refreshes after state transitions. Screenshot inspection at 520px found an unrelated but user-visible composer overlap; controls now stack below mode/model chips below the `sm` breakpoint while desktop remains one row.

Causal proof: `npm run test:sessions` keeps three valid sessions visible despite a corrupt index and mismatched sibling, rejects traversal and missing-ID path disclosure, reduces arbitrary chat objects to the allowed schema, preserves a chat-only active pointer, distinguishes clarification/terminal/resumable states, proves pin/delete behavior, and blocks active deletion. Extension-host tests invoke list/open/resume/delete commands, prove Open replaces host authority, terminal Resume does not resurrect, chat-only Open clears stale authority, and a fresh loop rehydrates an interrupted run with prior cost/evidence and a bounded five-step extension. E2E passed in 143.7s.

Visual/package proof: `artifacts/visual-smoke-sessions.png` and `artifacts/visual-smoke-sessions-sidebar.png` were inspected. Both show explicit Open/Resume semantics, active/pinned state, skipped corruption, and no permanent panel; the 520px image confirms the repaired composer layout. The remaining release matrix passed in 130.8s: compile, static, sessions, modes, support, readiness, progress, real Edge browser validation, workers 100/100, visual, package, and diff checks. `forge-agent-0.79.0.vsix` contains 148 files and is 3,169,222 bytes. Antigravity reports `kennyg.forge-agent@0.79.0`; installed markers confirm four commands, store runtime, chat memory, and webview UI.

## Phase 80 - Installed Difficult Live Weak-Model Proof

Status: **Implemented, preflight/negative/visual/extension-host tested, packaged, and installed; paid live execution awaiting explicit user confirmation**.

Reconciled gap: Forge has historical live Tier-3/4 console results and strong scripted product-loop proofs, but the current installed product cannot launch a live difficult eval. The Proof button hard-codes `live:false`, the newest live archives were overwritten before immutable archives existed, and the shell cannot access the user’s secret-vault key. Therefore the installed `0.79.0` build still cannot produce the decisive fresh evidence demanded by the project objective.

Contract:

- Add `forge-agent.runDifficultWeakModelProof` and a collapsed Proof-panel control that runs through the extension host and secret storage. Never expose or copy the API key.
- Initial live model is strictly `qwen/qwen-2.5-7b-instruct`: currently served, 7B, older, inexpensive, and historically weak on symptom-only Tier-4 planning. Reject routers, frontier models, and unapproved substitutions rather than weakening the claim.
- Use the four Tier-4 leak-law tasks: goals contain no defect symbol/file/string, held-out judges remain immutable, bare and harness lanes see the same task, and call timeout/max-step/task limits remain enforced.
- Require an explicit provider-credit checkbox. Show model, task count, call/step bounds, current completed count, provider failures, and eventual cost; do not hide a multi-minute operation behind an unexplained spinner.
- Persist `.forge/evals/latest-difficult-live-proof.json` plus an immutable run archive. Record bare/harness solved counts, per-task model-driven flags, provider calls/failures, cost, archive paths, and `fallbackSolved: 0`; do not classify provider failure or deterministic fallback as model success.
- Honest outcome is `uplift_observed`, `model_capability_without_uplift`, or `no_uplift_observed`. Passing the measurement loop never implies the weak model passed the difficult capability gate.

Non-goals: silently spending credits, choosing a stronger model to manufacture success, using a frontier architect, weakening held-out tests, running against this repository, claiming industry-level capability from one task, or treating mocked/scripted results as live.

Implemented result: `difficultLiveProof.ts` owns a one-slug weak-model allowlist, request/spend/task/step/timeout validation, current-catalog presence and price guards, Tier-4 leak/solvability assertions, equal bare/harness execution through `Tier2EvalRunner`, and a separate classification wrapper. Capability requires at least two model-driven green harness solves across at least two tasks plus uplift over bare. Fallback solved is structurally zero. The wrapper writes a mutable convenience report only after an immutable `wx` archive succeeds. `forge-agent.runDifficultWeakModelProof` validates before readiness/provider activity and then uses the extension's secret-backed runtime; the webview polls only current-run partial reports by modification time.

Product UX: a collapsed Proof section exposes the approved model, 1-4 task selector, fixed 10-step/90-second bounds, explicit credit checkbox, disabled-until-consent Run action, native report open, partial completed/call/failure/cost line, and final outcome/bare/harness/model-driven/fallback/capability summary. Visual fixtures show both the spend control and an honest `model_capability_without_uplift` / `NOT PASSED` result rather than manufacturing green UI.

Validation: `npm run test:difficult-proof` proves allowlist/consent/bounds, two model-driven green solves classify as uplift, fallback remains zero, and duplicate archive writes reject. Extension-host E2E rejects frontier substitution and missing consent before readiness/provider calls; the full host suite exited 0 in 157.5s (one transient test-host unresponsive/responsive cycle). The remaining release matrix passed in 142.4s: compile, static, difficult proof, sessions, modes, support, readiness, progress, real Edge browser validation, workers 100/100, visual, package, and diff checks. `forge-agent-0.80.0.vsix` contains 149 files and is 3,173,610 bytes; Antigravity reports `kennyg.forge-agent@0.80.0` and installed runtime/command/UI markers pass.

Installed visual preflight: `artifacts/installed-difficult-proof-preflight.jpg` is captured from the actual Antigravity window after reload. It shows the installed panel, approved Qwen slug, four tasks, call/step bounds, unchecked consent, and disabled live action. Antigravity readiness reported OpenRouter ready with 345 live models. The paid run was deliberately not started because no explicit action-time credit-spend confirmation was received; therefore no fresh live capability/uplift claim is made yet.

Contract:

- Add a host-owned mode registry persisted in extension `globalState`: immutable built-ins plus at most 20 validated custom modes. Mode fields are ID, name, bounded description/instructions, base intent (`code`, `architect`, `ask`, `review`), model role, inference default, built-in flag, and explicit tool allowlist.
- The webview sends only a mode ID. The extension host resolves the trusted registry entry and ignores client-supplied descriptions/tools. IDs/names/instructions/tool arrays are schema-validated, unknown tools rejected, duplicates refused, and built-ins cannot be overwritten/deleted.
- Advisory architect/ask/review modes route through non-mutating chat. Their UI Run action is disabled with a clear “use Send” explanation. Code modes may start `/goal` and pass a host-resolved `ModePolicy` into persisted harness state.
- Harness role tools remain the upper capability ceiling. A code-mode allowlist can only intersect each role's existing deterministic permissions; it can never add authority. Required workflow/proof tools (`update_plan`, `run_tests`, `get_diff`, `record_evidence`, `declare_success`, `ask_user`) are mandatory for agentic custom modes so customization cannot silently make success structurally impossible.
- Built-in Code preserves current full behavior. Built-in advisory modes express their actual non-mutating contract. Custom settings live behind a collapsed Settings section with create/delete and concise permission checkboxes; no separate mode-management page.
- Chat system context is assembled by the host from the resolved mode. The mode ID/name is recorded in chat/run state for session rehydration, but mode instructions cannot replace the blueprint, firewall, workflow, oracle, or evidence contracts.

Required proof:

- Unit fixtures cover valid custom mode, unknown tool, missing required proof tool, overlong/duplicate/built-in mutation, max-count, persistence, and deletion.
- Causal harness fixture proves a mode can remove `run_command` from Reviewer while retained tools still work, and cannot grant Editor `run_command` or any role a tool outside its base ceiling.
- Extension-host tests prove webview/command callers cannot spoof tool arrays by mode ID and saved modes survive a later list call.
- Visual smoke shows built-ins/custom mode in the composer and the collapsed Settings editor without restoring a cluttered permanent panel.
- Full compile, static, worker, extension-host, visual, package, Antigravity install, and screenshot inspection gates pass before implementation status is claimed.

Non-goals: arbitrary system-prompt replacement, custom JavaScript/tools, weakening oracle success gates, making advisory modes claim coding success, cloud mode sync, marketplace sharing, or more than 20 custom modes.

- Do not build or fork a full IDE.
- Do not recreate editor tabs, terminals, file trees, diff viewers, or browser panes inside Forge.
- Do not treat visual agent personas as sub-agents unless they have isolated context and constrained tools.
- Do not claim full blueprint compliance from package/install success.
- Do not count deterministic fallback success as model-driven success.
- Do not expand UI complexity until the harness loop, verification, reflection, and evals are stronger.
# Phase 81 - Host-Owned Human Approval Gate (implemented; causal/host/visual/package/install gates PASS)

Reconciled gap: the composer shield claims to control auto-approval but is only React-local state. It sends no host message and cannot pause or authorize a harness action. This is misleading Kilo-style chrome without Kilo-style behavior.

Selected contract: native host-owned `ask` / `auto` policy; only `write_file`, `apply_patch`, and `run_command` require human approval; the pause occurs after deterministic validation and pre-commit review but before checkpoint or COMMIT. A pending record contains the exact structured proposal and digest. Approval commits only that persisted action without another model call; rejection mutates nothing and becomes durable context. Auto mode bypasses only the human pause and never bypasses workflow, role, firewall, review, checkpoint, transaction, oracle, or evidence gates.

Validation contract: causal no-mutation pause, exact-action approval, rejection/replay/forged-ID negatives, automatic-mode firewall rejection, extension-host anti-spoof proof, narrow visual screenshots, full release gate, package, and installed Antigravity marker check.

Implemented result: `ask` persists the exact validated `write_file`, `apply_patch`, or `run_command` plus SHA-256 digest, session/task/role identity, bounded summary, and timestamps, then enters `awaiting_approval` before checkpoint or COMMIT. Host-only approval validates ID, digest, session, task, and role, then executes the same proposal through the normal checkpoint/transaction/oracle continuation without another provider call. Rejection performs no mutation, cannot be replayed, and records bounded guidance. `auto` skips only this pause. Native configuration owns policy; the webview sends decision intent only.

Proof: `npm run test:approvals` proves unchanged pre-approval workspace, zero checkpoint/worker execution, exact one-call commit, rejection, replay, forged ID, tamper rejection, and automatic-mode path firewall. Extension-host E2E persisted policy and rejected forged authority in 192.1s. Full no-spend release matrix passed in 72s. Desktop/sidebar visual QA found and fixed both an impossible synthetic state and resize-hidden card; final screenshots keep the pending decision and bottom composer visible. `forge-agent-0.81.0.vsix` contains 149 files / 3,176,094 bytes; Antigravity lists `kennyg.forge-agent@0.81.0`. Installed screenshot `artifacts/installed-human-approval-081.jpg` visibly shows the compact extension and the accessibility tree confirms the real ask-policy shield.

Remaining boundary: this is per-action approval, not remembered per-path/per-command grants. Forge deliberately does not prompt for reads, planning, tests, evidence, or local browser validation. The paid difficult weak-model proof remains pending separate explicit spend confirmation.
# Phase 82 - Deterministic Workspace Index and Real Composer Control (implemented; causal/host/visual/package/install gates PASS)

Reconciled gap: the composer database icon is cosmetic and only changes local status text. Forge persists ten task-specific retrieval candidates, not a repository index. `repo_search` and `symbol_search` inspect at most 250 files, creating silent large-repository blindness that directly harms weak models.

Selected contract: host-owned, no-symlink, 5,000-file bounded text/source metadata index under `.forge/workspace-index.json`; source bodies are not serialized. Indexed paths drive bounded repository search and declaration metadata drives symbol search. Native watcher events mark stale; the existing icon opens one compact status/Refresh/Open popover. Search and UI disclose coverage, truncation, stale state, and fallback provenance.

Validation contract: >250-file causal find, exclusions/symlink/binary/path/cross-workspace/corruption negatives, indexed symbol search, host stale/refresh commands, desktop/sidebar screenshots, full release gates, package, and installed Antigravity proof.

Implemented result: a short-lived sanitized child builds an atomic workspace-bound index over at most 5,000 contained text/source files. Records contain path, stat, language, and normalized declaration identity only. Dependencies, build/cache/Git/Forge/artifact directories, binaries, oversized files, and symlinks are excluded. Load validation rejects schema/workspace/count/path/symbol mismatches and current junction escapes. `repo_search` consumes indexed paths under a 64 MB current-source read budget and reports coverage; `symbol_search` consumes declaration metadata. Native file events mark ready indexes stale; users explicitly Refresh/Open from the existing composer icon.

Proof: the causal fixture contains 320 decoys plus a target beyond the old cap and passes only through indexed search. It proves source-body non-serialization, exclusions, cross-workspace/corrupt rejection, junction and post-build pivot rejection, indexed symbol lookup, and stale/rebuild lifecycle. Extension-host E2E proves registered commands, child execution, native open, watcher stale event, and ready rebuild in 172.5s. Full release matrix passed in 68.6s. Desktop/sidebar screenshots were inspected. `forge-agent-0.82.0.vsix` contains 151 files / 3,182,659 bytes and Antigravity lists `0.82.0`.

Installed visual proof: a fresh actual Antigravity window was required because the pre-existing window retained old webview code across Reload Webviews and Restart Extension Host. The registered `0.82.0` window showed MISSING, Refresh, disabled Open, then built to READY with 276 files / 2,087 symbols / 824 ignored and enabled Open. Evidence: `artifacts/installed-workspace-index-082.jpg`.

Remaining boundary: this is deterministic metadata indexing, not semantic source embedding. It does not index more than 5,000 files, persist source contents, follow symlinks, or guarantee hot-reloaded webviews update inside an already-open Antigravity window.

## Phase 83 - Explicit Host-Owned Task Context

Status: **Implemented, adversarially tested, extension-host tested, visually inspected, packaged, installed, and interactively proven in Antigravity**.

Reconciled gap: context engineering existed inside the autonomous loop, but the user could not state “this file, this selection, these diagnostics” through the composer. Chat and `/goal` carried only text, while `/research` attachments were internal and unrelated to active editor context. This made task entry path-dependent and ambiguous despite the underlying retrieval machinery.

Implemented result: one compact Paperclip control delegates all capture to the extension host. Active files/selections, native-picked workspace files, and VS Code diagnostics become validated snapshots under `.forge/sessions/<id>/context.json`. Realpath containment, regular-file/no-symlink checks, binary and size rejection, attachment/total caps, and persisted-record revalidation are deterministic. Webview messages contain metadata only. Chat receives bounded system context; new runs snapshot the same context into `HarnessState.userContext`, persist it, and include it as a required named section under the existing prompt budget.

Proof: the focused causal test covers valid capture plus traversal, outside-workspace, binary, oversized, empty-selection, foreign-diagnostic, total-budget, and tampered-persistence negatives. It proves session reload and exact source/path appearance in the harness prompt while summaries exclude source. Extension-host E2E exercises the registered command API and native session reopen. Desktop and 520px screenshots prove temporary context controls do not restore a file tree or permanent panel. The full no-spend release matrix passes. The 152-file `0.83.0` VSIX installs in Antigravity; after reloading the window, the real installed panel exposed all three context actions and captured the active `server.ts` into a removable chip (`artifacts/installed-composer-context-083.jpg`).

Useful takeaway: context engineering must begin at task entry, not only after the first model call. Explicit host-owned snapshots reduce weak-model search burden without trusting webview-supplied paths or silently transmitting an entire workspace.

Remaining boundary: there is no inline `@symbol` parser, automatic active-editor attachment, image attachment, remote URI support, or semantic expansion from an attached symbol. Snapshots do not update after capture; users refresh intentionally. Live weak-model uplift attributable specifically to attachments remains unmeasured.

## Phase 84 - Indexed @File And @Folder Mentions

Status: **Implemented, causally tested, extension-host tested, visually inspected, packaged, installed, and corrected from installed-product evidence**.

Reconciled gap: the earlier research explicitly identified `@file / @folder` mentions as the direct task-entry route into bounded context. Phase 83 established the trusted attachment substrate but required leaving the text flow for a Paperclip menu and native picker.

Implemented result: token-boundary `@` queries call the host, which searches validated workspace-index metadata and returns at most 20 file/folder summaries. The webview owns only transient query/navigation state. Selection returns kind/path intent; the host proves membership in the current validated index and recaptures through `ComposerContextService`. Files snapshot bounded source. Folders snapshot at most 500 contained indexed paths without source bodies. The result persists as the same removable session context and feeds chat and run prompts through Phase 83.

UX result: the composer now says `Ask anything, @ to attach context, / for actions`. The compact picker supports mouse plus ArrowUp/ArrowDown/Enter/Tab/Escape, labels ready/stale/missing provenance, distinguishes files/folders, and removes the incomplete token after selection. `/` and `@` menus do not compete.

Proof: causal tests cover ranking, folders, caps, source exclusion, containment, tampering, persistence, and prompt delivery. Extension-host E2E proves registered APIs, exact live index selection, metadata-only results, and forged-path rejection. Visual automation proves keyboard selection and inspects desktop/sidebar screenshots. The full no-spend release matrix passes. Installed `@src` inspection exposed `.tmp` proof-workspace pollution; `.tmp` became an enforced index exclusion with a regression sentinel. The corrected installed index reports 140 files/2,105 symbols/zero `.tmp` paths, and Antigravity lists `0.84.0`.

Remaining boundary: no `@symbol`, diagnostics mention, image attachment, remote URI, or semantic-neighbor expansion. A missing index must be built first. Stale index results are labeled and filesystem-revalidated, but not silently rebuilt. Live weak-model uplift attributable specifically to mention-scoped context is not yet measured.
