# Phase 97 Oracle Calibration Implementation Plan

Lifecycle: `PLAN -> RECONCILE -> DOCUMENT -> IMPLEMENT -> VALIDATE -> REVIEW -> DOCUMENT CLOSE -> AAR`

## Implementation Order

1. Add bounded test-suite identity, source selection, lexical mutation generation and canonical report types.
2. Run baseline/mutants/final baseline only in a disposable copy with exact byte restoration and active-workspace drift detection.
3. Add immutable archives, latest status assessment and strict 80%/five-mutant Audited capability checks.
4. Bind current calibration status into harness state, execution-contract availability and terminal assurance gates.
5. Add native run/status/open commands and one row/action inside the existing collapsed Proof integrity surface.
6. Add causal green/weak/red/unsupported/stale/test-preservation/source-preservation/tamper/truncation fixtures.
7. Run affected regression, worker, extension-host, visual, package/install and actual Antigravity gates; review false-success and authority paths; close docs and AAR.

## Acceptance Evidence

- Strong fixture kills at least 80% of at least five one-token mutants and becomes available.
- Weak fixture survives below the floor and remains unavailable.
- Baseline-red, final-baseline-red, unsupported, too-few-candidates, symlink and bounded-scan conditions fail closed.
- Test files and active workspace remain byte-identical across every result path.
- Changing test/config bytes makes a previously valid report stale; changing ordinary source does not rewrite the report.
- Tampered report digest or policy/floor identity is rejected.
- Audited contracts cannot use a missing, stale or below-floor report.
- Compact installed UI reports calibration honestly and uses native artifacts.

## Document Close

Implementation and release validation complete.

- Focused causal proof: strong 8/8, weak 0/8, red baseline blocked, unsupported command execution blocked, stale/tampered reports rejected, active workspace preserved, lexical canaries excluded and `.forge` model writes rejected.
- Review correction: policy v1 initially treated the `>` in JavaScript arrows as a mutation and falsely counted syntax failures. Ambiguous standalone angle mutations were removed; invalid mutants cannot inflate sensitivity.
- Trust-boundary correction: `.forge` is host-owned for model file mutations. Command transactions already excluded it from merge.
- Adjacent regression: execution-contract, attestation, background, customization, static, worker 100/100, extension-host and visual suites pass.
- Installed proof: VS Code and Antigravity register 0.97.0; after reload the compact Proof disclosure exposes Calibration status plus Calibrate/Open actions. Clicking Calibrate produced an honest unsupported report in the current non-Node workspace and enabled native report access without provider use.
- AAR: sampled sensitivity must never count syntax failures; host-owned proof namespaces need explicit mutation denial; installed wording must be driven live because correct state with the wrong reason is still a product defect.
