# Forge Agent Build Log

Environment: Windows, workspace `F:\DEV_ENV\projects\Agent_Harness_Extension`
Git: not a repository, commit/hash unavailable.

## 2026-06-22 Slim Sidebar Refactor

- Phase 0 - `npm run compile` - PASS - no git repository / sha unavailable
  - `tsc -p ./tsconfig.json && vite build` exited 0.
  - Extension host and slim webview assets emitted under `out/`.
- Phase 1 - `npm run test` - PASS - no git repository / sha unavailable
  - Static invariants verified OpenRouter routing, JSON schema response format, firewall/patch safety, oracle-gated success, proof report fields, native host commands, and absence of cloned IDE webview selectors/components in `App.tsx`.
- Phase 2 - `npm run test:e2e` - PASS - no git repository / sha unavailable
  - VS Code Extension Development Host activated Forge Agent.
  - Fixture run produced `.forge` artifacts, green evidence, latest proof report, native artifact opens, native terminal creation, and diff artifact/native diff fallback.
- Phase 3 - `npm run test:visual` - PASS - no git repository / sha unavailable
  - Playwright clicked Run -> Proof -> Settings in the compact sidebar.
  - Screenshot artifact: `artifacts/visual-smoke.png`.
- Phase 4 - `npm run build` - PASS - no git repository / sha unavailable
  - `tsc -p ./tsconfig.json && vite build` exited 0.
- Phase 5 - `npm run package` - PASS - no git repository / sha unavailable
  - Output: `forge-agent-0.0.1.vsix`, 27 files, 115.02 KB.
  - VSIX includes runtime files only: `out/`, `media/icon.svg`, `package.json`, `BUILD_LOG.md`, and `scripts/proof-api-usage.md`.
- Phase 8 - OpenRouter catalog/search refactor - PASS - no git repository / sha unavailable
  - Removed the 75-model OpenRouter catalog cap.
  - Added refreshable searchable model pickers for coding, planning, and review roles.
  - Static invariants verify picker selectors and role-ranking logic.
- Phase 9 - Agent chat surface - PASS - no git repository / sha unavailable
  - Added `forge-agent.chat` and a compact Run-tab chat panel.
  - Chat uses the configured provider/model path and is advisory only; mutations still require the firewalled run loop.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run package` exited 0.
- Phase 10 - Default UI simplification - PASS - no git repository / sha unavailable
  - Reduced the Run tab to chat, compact goal/start controls, and one status line.
  - Moved run internals, firewall details, logs, evidence, and artifact links behind collapsed detail sections.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run package` exited 0.
- Phase 11 - Bottom composer UI - PASS - no git repository / sha unavailable
  - Removed the second visible goal input from the default Run view.
  - Moved chat to a bottom composer with role/model/inference chips and compact action icons for run, step, proof, settings, artifacts, and send.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run package` exited 0.
- Phase 12 - KLIO composer function gap pass - PASS - no git repository / sha unavailable
  - Added role menu, searchable model menu with favorites, inference menu, indexing status action, permission toggle, prompt enhancement, and voice input hook.
  - Composer chat now routes through the selected role/model/inference context.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run package` exited 0.
- Phase 13 - Settings dropdown cleanup - PASS - no git repository / sha unavailable
  - Collapsed settings model pickers by default.
  - Searchable model lists now open only when the user expands a picker.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run package` exited 0.
- Phase 14 - Weak-model harness eval - PASS - no git repository / sha unavailable
  - Added `forge-agent.runWeakModelEval`, `forge-agent.getWeakModelEvalReport`, `npm run eval`, and Proof-tab controls for the weak-model scorecard.
  - Weak selector prefers cheap/small legacy candidates such as `qwen/qwen2.5-coder-7b-instruct` and explicitly excludes routers, frontier models, and new agentic coding specialists.
  - `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct` exited 0.
  - Offline mocked scorecard: `bareSolved=1`, `harnessSolved=5`, `solveRateDelta=0.8`, `actuallyModelDriven=5`, `fallbackSolved=0`.
  - Scorecard artifact: `.forge/evals/latest-weak-model-eval.json`.
- Phase 15 - OpenRouter catalog sorting - PASS - no git repository / sha unavailable
  - Verified live OpenRouter `/api/v1/models` returned 340 models from this machine.
  - Added provider metadata for prompt price, completion price, created timestamp, and supported parameters.
  - Added model sort controls for recommended role fit, largest context, reasoning rank, coding rank, lowest cost, and newest in composer/settings model pickers.
  - Raised picker display windows from tiny slices to 80 visible ranked entries while search still scans the full loaded catalog.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
- Phase 16 - OpenRouter partial-catalog hardening - PASS - no git repository / sha unavailable
  - Verified the configured Antigravity OpenRouter key returns 340 models from `/api/v1/models` outside the extension host.
  - Updated provider catalog refresh to merge authenticated and anonymous OpenRouter catalog responses, preventing a partial authenticated response from shrinking the UI list.
  - Corrected cost sorting so actual free models sort as free while unknown/special pricing displays as `$?`.
  - Compiled provider check returned `count=340`.
  - `npm run compile`, `npm run test`, `npm run test:e2e` rerun, `npm run test:visual`, and `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct` exited 0.
- Phase 17 - Real agent loop agency - PASS - no git repository / sha unavailable
  - Replaced the test-only default task graph with inspect, plan, edit, verify, and evidence phases.
  - The run button now starts `run-agent-loop`, which drives firewalled steps to terminal success/failure instead of stopping after initialization.
  - Added `forge-agent.runAgentGoal` command API for end-to-end autonomous goal runs.
  - Tool results are captured into `SCRATCHPAD.md`, `.forge/state.json`, and `state.files` so later proposals have workspace memory.
  - Proof runner now exercises the full loop to terminal state with Explorer/Architect/Editor/Reviewer bindings.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct` exited 0.
- Phase 18 - Agency accountability hardening - PASS - no git repository / sha unavailable
  - Persisted normal-run stats in `.forge/state.json`: provider calls/failures, model-driven proposals, fallback proposals/actions, repair attempts, schema failures, validation failures, no-progress turns, and `actuallyModelDriven`.
  - Added bounded JSON/schema repair retries before deterministic fallback.
  - Added no-progress detection with honest `gave_up` after repeated unchanged state.
  - Added compact Run-console counters: tests, model proposals, fallback actions, and repair attempts.
  - Extension-host tests now force malformed provider output and verify the repaired response counts as model-driven rather than fallback.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Visual evidence: `artifacts/visual-smoke-run.png` and `artifacts/visual-smoke.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`, 20 files, 122.88 KB.
- Phase 19 - Bounded reflection retries - PASS - no git repository / sha unavailable
  - Added persisted reflection entries for firewall rejection, tool failure, and red-oracle failure.
  - Reflection entries are appended to `SCRATCHPAD.md`, persisted in `.forge/state.json`, exposed through run stats, and included in the next proposal prompt.
  - Added bounded reflection counters: total, firewall, tool-failure, and oracle reflections.
  - Extension-host tests now verify firewall reflection and failing-verification reflection avoid immediate terminal failure while the cap remains.
  - Visual smoke injects a reflected run state and captures the compact status line showing `reflect 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Visual evidence: `artifacts/visual-smoke-run.png` and `artifacts/visual-smoke.png`.
  - VSIX output: `forge-agent-0.0.1.vsix` from the final package command.
- Phase 20 - Reviewer diff gate - PASS - no git repository / sha unavailable
  - Added persisted diff review entries and `.forge/diff-reviews.json`.
  - Final success now requires green oracle evidence and a reviewer diff gate.
  - The deterministic fallback path requests `get_diff` before final evidence/success when review evidence is missing.
  - Evidence entries include the latest reviewed diff excerpt when relevant.
  - Extension-host tests verify successful runs include reviewer approval and persisted diff-review artifacts.
  - Visual smoke injects a reviewer-approved run state and captures the compact status line showing `review 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct` exited 0.
  - Visual evidence: `artifacts/visual-smoke-run.png` and `artifacts/visual-smoke.png`.
- Phase 21 - Verification fixture matrix - PASS - no git repository / sha unavailable
  - Split typecheck/build oracle handling from lint oracle handling.
  - Added `runVerificationFixtureMatrix` with disposable cases for passing tests, failing tests, missing test suite, typecheck failure, lint failure, malformed patch, out-of-workspace path, and blocked command rejection.
  - Added `forge-agent.runVerificationFixtureMatrix`, Proof-tab controls, and native artifact open path for `.forge/verification-fixture-matrix.json`.
  - Extension-host tests verify all 8 fixture cases and persisted matrix artifact.
  - Visual smoke captures the Proof tab showing verification fixture matrix `PASS` with 8 cases.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Visual evidence: `artifacts/visual-smoke-proof.png`.
- Phase 22 - Bounded escalation routing - PASS - no git repository / sha unavailable
  - Added escalation entries, `.forge/escalations.json`, escalation counters, and run status `esc`.
  - After repeated reflection-triggering failures, the next provider proposal routes to `Escalation`, `escalation`, `review`, or `Reviewer` model binding instead of the weak/code binding.
  - Extension-host tests force two firewall reflections and verify the third provider call uses the escalation model.
  - Visual smoke captures the Run console showing `esc 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct` exited 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
- Phase 23 - Unsolvable fixture and Node matrix portability - PASS - no git repository / sha unavailable
  - Added `unsolvable-step-cap` to the verification fixture matrix.
  - The fixture proves an exhausted run terminates as `gave_up` and does not record green evidence.
  - Fixed `AgentHarnessLoop` to lazy-load VS Code APIs so `verificationMatrix.js` can also run from plain Node.
  - Repo-local matrix artifact now reports 9 passing cases at `.forge/verification-fixture-matrix.json`.
  - Visual smoke captures the Proof tab showing verification fixture matrix `PASS` with 9 cases.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, `npm run package`, and a direct Node matrix run exited 0.
  - Visual evidence: `artifacts/visual-smoke-proof.png`.
- Phase 24 - Context bundle rehydration - PASS - no git repository / sha unavailable
  - Added persisted `.forge/context-bundle.json`.
  - Context bundle includes goal, active/open tasks, recent files, recent reflections, escalations, reviewer notes, scratchpad summary, retrieval policy, token estimate, and compaction flag.
  - Proposal prompts now consume the context bundle instead of assembling all context ad hoc.
  - Added native artifact open path `context`.
  - Extension-host tests verify context bundle persistence, goal rehydration, open task rehydration, retrieval policy, and native editor open.
  - Visual smoke captures the Run console showing `ctx 2`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct` exited 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
- Phase 25 - Role handoff artifacts - PASS - no git repository / sha unavailable
  - Added `RoleHandoff` state with active role, scoped allowed tools, responsibilities, role-open tasks, recent context, and handoff summary.
  - Proposal prompts now consume the active role's handoff slice.
  - Persisted `.forge/role-handoffs.json` and added native artifact open path `handoffs`.
  - Added compact Run-console counter `hand` instead of a new always-visible panel.
  - Extension-host tests verify role handoff persistence, allowed tool scopes, native artifact opening, and autonomous-run stats.
  - Visual smoke captures the Run console showing `hand 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 5-task run, bare solved 1/5, harness solved 5/5, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`, 21 files, 132.99 KB after final log-only repackage.
- Phase 26 - Deterministic retrieval index - PASS - no git repository / sha unavailable
  - Added `RetrievalCandidate` entries to the context bundle.
  - Added deterministic lexical ranking from goal/task text across workspace files while excluding dependencies, build output, `.git`, `.forge`, test runtime, and visual artifacts.
  - Proposal prompts now include ranked retrieval candidates before unrelated file reads.
  - Persisted `.forge/retrieval-index.json` and added native artifact open path `retrieval`.
  - Added compact Run-console counter `ret`.
  - Extension-host tests verify retrieval candidates persist in `.forge/context-bundle.json`, `.forge/retrieval-index.json` contains scored file candidates, native artifact opening works, and autonomous-run stats expose retrieval refreshes.
  - Visual smoke captures the Run console showing `ret 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 5-task run, bare solved 1/5, harness solved 5/5, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`.
- Phase 27 - Manifest-backed safety checkpoints - PASS - no git repository / sha unavailable
  - Replaced opaque checkpoint ids with persisted checkpoint records containing strategy, proposal name, protected paths, manifest path, and timestamp.
  - Added targeted file snapshots for path-scoped mutations and workspace snapshots for broader mutating proposals.
  - Checkpoint manifests persist under `.forge/checkpoints/<id>/manifest.json`; the run-level ledger persists under `.forge/safety-checkpoints.json`.
  - Added native artifact open path `safety` and compact Run-console counter `safe`.
  - Extension-host tests verify targeted checkpoint restore returns original file content, normal runs persist safety checkpoint manifests, native artifact opening works, and autonomous-run stats expose safety checkpoints.
  - Visual smoke captures the Run console showing `safe 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 5-task run, bare solved 1/5, harness solved 5/5, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`.
- Phase 28 - Reviewer critique artifacts - PASS - no git repository / sha unavailable
  - Added `ReviewerCritiqueEntry` state and `.forge/reviewer-critiques.json`.
  - `get_diff` now records both the deterministic diff-review gate and a reviewer critique artifact.
  - If `review` or `Reviewer` model binding is configured, the harness requests structured JSON critique from that model; otherwise it records an explicit deterministic fallback critique.
  - Added native artifact open path `critiques` and compact Run-console counter `crit`.
  - Extension-host tests verify deterministic critique persistence, configured reviewer-model critique with fake provider, native artifact opening, and autonomous-run critique stats.
  - Visual smoke captures the Run console showing `crit 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 5-task run, bare solved 1/5, harness solved 5/5, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`.
- Phase 29 - Blocking pre-commit review - PASS - no git repository / sha unavailable
  - Added `PreCommitReviewEntry` state and `.forge/precommit-reviews.json`.
  - Mutating proposals now create a pre-commit review after deterministic firewall validation and before COMMIT.
  - If `review` or `Reviewer` model binding is configured, the harness requests structured JSON pre-commit review from that model; otherwise it records deterministic approval after schema/scope/policy/applicability validation.
  - A model response of `blocked` prevents filesystem mutation, increments `preCommitBlocks`, records the review artifact, and queues bounded reflection.
  - Added native artifact open path `precommit` and compact Run-console counter `pre`.
  - Extension-host tests verify normal pre-commit review persistence, native artifact opening, autonomous-run stats, and a fake reviewer model blocking an `apply_patch` before the target file changes.
  - Visual smoke captures the Run console showing `pre 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 5-task run, bare solved 1/5, harness solved 5/5, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`.
- Phase 30 - Command side-effect capture - PASS - no git repository / sha unavailable
  - Added `CommandSideEffectEntry` state and `.forge/command-effects.json`.
  - `run_command` now snapshots workspace files before and after command execution and records created, modified, and deleted files plus command output excerpt.
  - Added native artifact open path `commandEffects` and compact Run-console counter `cmd`.
  - Extension-host tests verify a command-created file appears on disk, is recorded in state, and persists in `.forge/command-effects.json`.
  - Visual smoke captures the Run console showing `cmd 1`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 5-task run, bare solved 1/5, harness solved 5/5, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`.
- Phase 31 - Expanded weak-model eval benchmark - PASS - no git repository / sha unavailable
  - Expanded the mocked weak-model disposable benchmark from 5 to 15 fixture tasks.
  - Added fixture coverage for empty arrays, off-by-one slicing, case-insensitive checks, defaulting, parse validation, dedupe, clamping, promise returns, avoiding input mutation, and non-mutating sort behavior.
  - Updated the Proof-tab weak-eval action to run 15 tasks and display the scorecard task count.
  - Static invariant now requires at least 15 weak-eval fixture tasks.
  - Visual smoke captures the Proof tab showing `Tasks 15`, `Bare solved 1`, `Harness solved 15`, and `Fallback solved 0`.
  - `npm run compile`, `npm run build`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 15-task run, bare solved 1/15, harness solved 15/15, actually model-driven 15, fallback solved 0.
  - Scorecard artifact: `.forge/evals/latest-weak-model-eval.json`.
  - Visual evidence: `artifacts/visual-smoke-proof.png`.
  - VSIX output: `forge-agent-0.0.1.vsix`.
- Phase 32 - Explicit run budget caps - PASS - no git repository / sha unavailable
  - Added `RunBudget` state with `startedAt`, `maxWallClockMs`, `maxCostUsd`, `lastCheckedAt`, and halt reason.
  - Runs now persist `.forge/budget.json`.
  - Wall-clock caps halt as `gave_up` before provider calls or tool work.
  - Cost caps halt as `gave_up` after provider usage is recorded and before validation/commit can mutate files.
  - Added `budgetHalts` run stat and compact Run-console display for cost cap and halt count.
  - Added native artifact open path `budget`.
  - Extension-host tests verify wall-clock halt makes zero provider calls, cost halt blocks an over-budget patch before filesystem mutation, and `.forge/budget.json` persists the halt reason.
  - Visual smoke captures the Run console showing compact cost/cap status.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, and `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 15-task run, bare solved 1/15, harness solved 15/15, actually model-driven 15, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
- Phase 33 - Isolated workspace-copy runs - PASS - no git repository / sha unavailable
  - Added `runIsolatedAgentGoal` API in `src/harness/isolation.ts`.
  - Added extension command `forge-agent.runIsolatedAgentGoal` and webview bridge command `run-isolated-agent-goal`.
  - Isolated runs copy the source workspace to a temp root, execute the normal harness against that copy, compare source before/after, and persist `.forge/isolated-runs/latest-isolated-run.json` plus `.forge/isolated-runs/latest-isolated-run.diff`.
  - Added native artifact open paths `isolatedRun` and `isolatedDiff`.
  - Added Proof-tab Isolated Run controls and compact report summary.
  - Extension-host tests verify an isolated patch reaches success in the temp copy, records `src/math.js` as changed, leaves the source workspace file unchanged, and opens the isolated-run report through the native artifact command.
  - Local isolated smoke command wrote `.forge/isolated-runs/latest-isolated-run.json` with `sourceMutated: false`.
  - Visual smoke captures the Proof tab showing `Source mutated false`.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, local isolated smoke, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 15-task run, bare solved 1/15, harness solved 15/15, actually model-driven 15, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-proof.png`.
- Phase 34 - Command process sandbox metadata - PASS - no git repository / sha unavailable
  - `run_command` now executes with a curated environment allowlist instead of blindly inheriting the host process environment.
  - Command results carry sandbox metadata: cwd, timeout, duration, exit code, signal, sanitized-env flag, inherited env key count, allowed env key names, and blocked env key names.
  - `.forge/command-effects.json` now persists the sandbox metadata with file side effects.
  - Extension-host tests verify `FORGE_SANDBOX_SECRET` is blocked from command inheritance and appears only in blocked env key names, not allowed env key names.
  - Local command sandbox smoke wrote command-effects evidence where the child process printed `false`, `sanitizedEnv` was true, `blockedSecret` was true, and `allowedSecret` was false.
  - Added `.vscodeignore` excludes for generated root run artifacts so local proof runs do not contaminate VSIX contents.
  - `npm run compile`, `npm run test`, `npm run test:e2e`, `npm run test:visual`, `npm run eval -- --model qwen/qwen2.5-coder-7b-instruct`, local command sandbox smoke, and `npm run package` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 15-task run, bare solved 1/15, harness solved 15/15, actually model-driven 15, fallback solved 0.
  - Visual evidence: `artifacts/visual-smoke-run.png`.
- Phase 6 - VS Code install smoke - PASS - no git repository / sha unavailable
  - `code --install-extension .\forge-agent-0.0.1.vsix --force` exited 0.
- Phase 7 - Antigravity IDE install smoke - PASS at disk/registry level - no git repository / sha unavailable
  - Correct executable path checked: `C:\Users\Moshi\AppData\Local\Programs\Antigravity IDE\Antigravity IDE.exe`.
  - Refreshed extension from the same VSIX at `C:\Users\Moshi\.antigravity-ide\extensions\kennyg.forge-agent-0.0.1`.
  - Verified refreshed runtime files: `out\extension.js` and `out\webview\assets\index.js`.
  - Verified registry contains `kennyg.forge-agent` pointing at `kennyg.forge-agent-0.0.1`.
  - `Antigravity IDE.exe --list-extensions` produced no usable output in this session, so UI open/click proof remains manual.
- Phase 35 - True git worktree isolated execution - PASS (sandbox gates) / host gates pending - no git repository / sha unavailable
  - `runIsolatedAgentGoal` now executes git-backed sources in a real detached `git worktree` under a temp root instead of a plain workspace copy.
  - Dirty source state is overlaid into the worktree before the run: modified tracked files, untracked files, renames, and deletions, recorded as `dirtyFilesOverlaid`.
  - Report now records `requestedIsolationMode`, `isolationMode`, `isolationFallbackReason`, `baseCommit`, `dirtyFilesOverlaid`, and `sourceDirtyStatusPreserved`.
  - Non-git sources fall back to workspace-copy isolation with an explicit fallback reason; forcing `isolationMode: 'worktree'` on a non-git source fails honestly with a reasoned error.
  - Non-kept worktrees are removed with `git worktree remove --force`, temp roots deleted, and registrations pruned with `git worktree prune`.
  - Source dirty status is captured as normalized `git status --porcelain` before/after (excluding `.forge`) to prove uncommitted local edits and untracked files survive isolated runs.
  - Extension-host tests added: worktree mode selection, base commit hash, linked `.git` file proof, dirty overlay content, success inside worktree, source non-mutation, dirty-state preservation, registration cleanup after non-kept runs, copy-fallback fields on non-git sources, and forced-worktree rejection.
  - Static smoke invariants added for worktree add/remove/prune, isolation mode, fallback reason, base commit, dirty overlay, and dirty-status preservation.
  - Sandbox verification: `tsc -p ./tsconfig.json` exited 0; smoke invariants PASS; standalone behavioral proof of all Phase 35 worktree assertions PASS against `out/harness/isolation.js`; `node scripts/weak-model-eval.mjs --model qwen/qwen2.5-coder-7b-instruct` exited 0.
  - Weak eval summary: model `qwen/qwen2.5-coder-7b-instruct`, mocked 15-task run, bare solved 1/15, harness solved 15/15, actually model-driven 15, fallback solved 0.
  - HOST GATES PENDING (Linux sandbox cannot run them: npm registry 403 on Linux esbuild/rollup binaries; only win32 VS Code test archive cached): `npm run compile` (vite build step), `npm run test:e2e`, `npm run test:visual`, `npm run package`. Run these on the Windows host to close out Phase 35.
  - No UI change in this phase; no new screenshot required.
- Phase 36 - Reflection A/B eval (blueprint Phase 5 gate) - PASS (sandbox gates) / host gates pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 36: reflection A/B eval proves harness reflection carries recovery (+1.00 solve-rate delta)".
  - `HarnessState.reflectionEnabled` (default on) threads through `initializeHarness`; when off, `scheduleReflection` suppresses (counted in `runStats.reflectionSuppressed`) and existing red-oracle/tool-failure halt branches fire.
  - New `src/harness/reflectionAb.ts`: `runReflectionAbEval` runs the REAL `AgentHarnessLoop` per lane on disposable fixtures; the scripted provider emits a plausible-but-wrong patch first and only produces the correct patch after harness-injected reflection context (`red_oracle:` marker) appears in the prompt, so solving is causally dependent on reflection.
  - Scorecard persists at `.forge/evals/latest-reflection-ab.json`; CLI `scripts/reflection-ab-eval.mjs` + `npm run eval:reflection`; extension command `forge-agent.runReflectionAbEval`; artifact open path `reflectionAb`; smoke invariants and e2e assertions added.
  - Verified A/B number: reflection ON solved 5/5; reflection OFF solved 0/5 with 5/5 honest `failed` halts citing the red oracle; solveRateDelta +1.00; off lanes recorded `reflectionSuppressed: 1`, on lanes `reflectionAttempts: 1`.
  - Sandbox verification (shadow-tree due to mount staleness; see AAR): `tsc -p ./tsconfig.json` exited 0; smoke invariants PASS; `node scripts/reflection-ab-eval.mjs` exited 0 with `reflection_uplift_observed`; weak eval regression unchanged (bare 1/15, harness 15/15, model-driven 15).
  - HOST GATES PENDING (same environment limits as Phase 35): `npm run compile` (vite), `npm run test:e2e`, `npm run test:visual`, `npm run package`. Note e2e now also covers Phase 36 assertions.
  - No UI change; no screenshot required.
  - AAR: SUSTAIN scripted-causality A/B design (off-lane cannot accidentally solve) and running the real loop instead of a parallel mini-lane. WORK: plan was documented before build this time; keep that ordering. TOOLS (triggers fired): host->sandbox mount served stale-size truncated content for EDITED files (new files synced fine) - banked workaround is tail-splice: take truncated mount body, fetch missing tail via host-side reads, splice in /tmp shadow tree, validate there; second occurrence of this failure mode in one session, so treat mount reads of any host-edited file as suspect until size-verified.
- Phase 37 - Terminal-state AAR hook + durable lessons - PASS (sandbox gates) / host gates pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 37: terminal-state AAR banks durable lessons and rehydrates them into future run context".
  - `persistStateToDisk` is the single choke point: on any terminal status (`success`/`failed`/`gave_up`) it fires `recordAar` exactly once (guarded by `state.aar`), covering every halt path including budget and no-progress halts.
  - `.forge/aar.json` records deterministic trigger counts (reflections, suppressed reflections, escalations, budget halts, pre-commit blocks, validation/provider failures, repair attempts, safety reverts, no-progress turns), derived sustain/improve-work/improve-tools notes, `clean` flag, and banked lesson signatures. The AAR is embedded in `state.json`. The model never authors AAR content.
  - Non-clean runs bank signature-deduped lessons (`category:status:goal-slice`) to `.forge/lessons.json`, capped at 50, with occurrence counting.
  - `refreshContextBundle` now rehydrates `recentLessons` (last 3) into the context bundle, so past AARs inform future proposals - AAR feeds RECONCILE.
  - Extension artifact open paths `aar` and `lessons`; smoke invariants (7 new) and e2e assertions (AAR on both reflection A/B lanes, embedded state AAR, native artifact open) added.
  - Sandbox verification (shadow-tree round 2): `tsc` exited 0; smoke invariants + phase-37 delta asserts PASS; behavioral proof PASS on four paths - reflected success closes non-clean with banked lesson and sustain note; suppressed-reflection failure cites suppression in improve-tools; zero-trigger run closes `clean: true` with no lessons file (negative path - no lesson spam); second run in the same workspace rehydrates the banked lesson into `contextBundle.recentLessons`.
  - Regressions green: reflection A/B still `reflection_uplift_observed` (exit 0); weak eval unchanged (bare 1/15, harness 15/15).
  - Structural-only coverage (named honestly): `gave_up` AAR shares the identical proven choke point but was not behaviorally isolated; lesson dedupe-on-repeat is code-verified, not behaviorally exercised.
  - HOST GATES PENDING (same four): `npm run compile` (vite), `npm run test:e2e`, `npm run test:visual`, `npm run package`.
  - No UI change; no screenshot required.
  - AAR (meta): SUSTAIN tail-splice shadow validation now takes minutes (twice-proven technique) and behavioral negative-path proofs. WORK: plan documented before build; spec review caught two structural-only coverages and named them instead of overclaiming. TOOLS (trigger: mount staleness recurred, expected): treat it as the session norm - every host-edited file needs splicing before sandbox validation; consider asking the host to run gates earlier in future sessions to shrink the feedback loop.
- Phase 38 - Proposal schema fix + live-eval guards (live-run root cause) - PASS (sandbox gates) / live re-run pending fresh key - no git repository / sha unavailable
  - Suggested commit title: "Phase 38: enumerate tool-argument schema properties; live constrained decoders no longer empty arguments".
  - Root cause from first live runs (2026-07-08): `arguments: { type: 'object' }` with no properties inside a strict schema made live grammar engines force `arguments: {}` on all 75 calls; every proposal had an empty path and was correctly firewalled (live 0/15 both lanes, fallback 15/15, zero false successes - honest accounting held under total proposal failure). Also found `qwen/qwen2.5-coder-7b-instruct` and `mistralai/mistral-7b-instruct` delisted from OpenRouter; `qwen/qwen-2.5-7b-instruct` is live.
  - Fix: `TOOL_SCHEMA` (loop.ts) and `ACTION_SCHEMA` (weakEval.ts) enumerate path/patchContent/content/command/query/startLine/endLine/planMd/tasks/observation as optional properties; both exported for regression tests; inline comments document the failure mode.
  - Guards: `assertModelEndpointsLive` fails fast on delisted slugs (graceful on network failure); `runLiveSchemaCanary` makes one cheap call before the suite and aborts with an actionable error if arguments round-trip empty; canary result persists in the scorecard as `liveCanary`.
  - Verified in sandbox (shadow-tree round 3): tsc exit 0; strict-decoder simulation reproduces the empty-arguments bug on the OLD schema and proves path/patchContent survive the NEW schema; full harness loop reaches success behind simulated grammar enforcement; canary logic passes on healthy round-trip and fails on empty arguments; liveness probe (stubbed fetch) rejects dead slug, accepts live slug, degrades gracefully on network failure; smoke invariants incl. negative invariant (no property-less arguments object may ever return) PASS; regressions green (reflection A/B uplift observed; mocked weak eval 1/15 -> 15/15).
  - HOST GATES PENDING: `npm run test:e2e`, `npm run test:visual`, `npm run package` (compile already passed live on host during eval runs), plus the live eval re-run: `npm run eval -- --live --model qwen/qwen-2.5-7b-instruct` with a fresh OPENROUTER_API_KEY.
  - AAR: SUSTAIN honest fallback accounting (it converted a total failure into a precise diagnosis) and reading live per-task errors before theorizing. WORK: the live canary should have existed before the first live run - a one-call preflight would have saved 150 calls; lesson generalized as "canary every new live integration before batch runs". TOOLS (trigger: two hardcoded weak slugs were dead): model catalogs rot; probe liveness at run time, never trust a hardcoded slug list; banked into the eval itself as code, not just documentation.
- Phase 39 - Edit-format exemplar + firewall rejection feedback in eval lanes - PASS (sandbox gates) / live re-run pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 39: show the patch format and feed rejection reasons back - harness now beats bare on format errors".
  - Second live run diagnosis: canary passed and all 75 calls succeeded, but 0/15 both lanes with dominant error `Malformed patch: expected SEARCH/REPLACE hunk` - the model produced patches in the wrong format; the eval prompt named the format without showing it, and harness retries did not say WHY the previous attempt was rejected, leaving bare and harness lanes nearly identical on format errors.
  - Fix: `PATCH_FORMAT_EXEMPLAR` (real hunk, verbatim fence tokens, copy-character-for-character rule) added to eval task prompts; harness-lane retries inject `REJECTED by deterministic validation: <reason>` plus the exemplar; lane results persist `lastValidationError` and a 500-char `rejectedPatchSample` for future diagnosis without re-running.
  - Behavioral proof (shadow-tree round 4): a format-confused provider that only corrects itself after seeing the rejection feedback scores bare 0/1 vs harness 1/1 (`passed: true`, recovery at step >= 2); diagnostics captured in both lanes. Mocked 15-task regression unchanged (1/15 -> 15/15). tsc + smoke invariants + phase delta asserts PASS.
  - HOST GATES PENDING: `npm run test:e2e`, `npm run test:visual`, `npm run package`, and the live re-run `npm run eval -- --live --model qwen/qwen-2.5-7b-instruct`.
  - AAR: SUSTAIN diagnosing from captured per-task errors before theorizing (two live runs, two precise root causes) and adding diagnostics capture so the NEXT failure ships with its own evidence. WORK: the harness lane's silent-retry-on-rejection was a design gap violating our own verification-feedback thesis - when a validator rejects, the reason must reach the model; generalize this to every retry path. TOOLS (trigger: prompt named a format without an exemplar): weak models need the format SHOWN, not described - one-line lesson now encoded in the prompt itself.
- MILESTONE - First live weak-model baseline: THESIS CONFIRMED LIVE - 2026-07-08 - no git repository / sha unavailable
  - `npm run eval -- --live --model qwen/qwen-2.5-7b-instruct` (run 3, post Phase 38+39 fixes): bare 2/15, harness 13/15, solveRateDelta +0.733, actuallyModelDriven 13, fallbackSolved 1, providerCalls 58, providerFailures 0. Total cost across ALL live runs: under $0.01.
  - Same model, same tasks, one variable: the harness. All 13 harness solves were genuinely model-driven (fallback separately accounted). Zero false successes across three live runs.
  - Run-3 misses define the ratchet: `syntax-error` failed on exact SEARCH-block matching (whitespace drift -> next rung: lenient matching); `sort-copy` did not converge in 4 harness steps; diagnostics show early empty-path proposals still occur and are recovered by Phase 39 rejection feedback (candidate rung: deterministic path repair).
  - The three live runs also validated the failure ladder itself: run 1 caught dead slugs + schema-forced empty arguments; run 2 caught format-without-exemplar and silent retries; run 3 delivered uplift. Each failure produced one precise fix.
  - Baseline is now the ratchet floor: future harness changes must not regress bare-vs-harness delta below +0.73 on this suite.
- Phase 40 - Whitespace-lenient SEARCH matching + eval-lane path repair - PASS (sandbox gates) / live run 4 pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 40: lenient patch matching + visible path repair - both run-3 failure modes closed".
  - Shared `findLenientMatch` in tools.ts: exact substring match wins (legacy semantics preserved); otherwise sliding-window comparison of trimmed lines; a UNIQUE window applies with the replacement target rebuilt from the file's actual bytes (lenient application never invents content); multiple windows reject as ambiguous with a count and guidance; zero windows reject as not found. Used by both `firewall.validatePatchApplicability` and `tools.applyPatch`; lenient applications are named in tool output.
  - Eval harness lane: empty apply_patch paths are deterministically repaired to the task's single fixture file and counted in `pathRepairs` - harness assistance is allowed but always visible; the bare lane gets no repair by definition.
  - Verified in sandbox (shadow-tree round 5): tsc exit 0; matcher unit proofs (exact/lenient/ambiguous/not_found, file-bytes preservation); combined run-3 failure-mode e2e (drifted SEARCH + empty path): bare 0/1 vs harness 1/1 with pathRepairs=1 and lenient apply; reflection A/B and mocked 15-task regressions green; smoke invariants PASS with new asserts (shared matcher, visible lenient note, counted path repairs).
  - HOST GATES PENDING: `npm run test:e2e`, `npm run test:visual`, `npm run package`, live run 4 `npm run eval -- --live --model qwen/qwen-2.5-7b-instruct` (target: beat the +0.73 ratchet floor; stretch: 15/15).
  - AAR: SUSTAIN designing the behavioral proof around the exact live failure modes (drifted+empty-path provider reproduces run 3 in one test). WORK: one test-authoring stumble - my ambiguity case used a bare substring, which the exact path legitimately matches; lesson: when adding lenient layers, write tests that FAIL the strict layer first. TOOLS: legacy exact-substring semantics were deliberately preserved to avoid changing behavior for strong models; recorded here so a future phase does not "fix" it blind.
- Live run 4 (post Phase 40) - harness 13/15 held, efficiency up, variance observed - 2026-07-08
  - bare 4/15 (run-3: 2/15 - live variance ~+/-2 on this suite; single-run deltas are noisy), harness 13/15, delta +0.60, actuallyModelDriven 13, fallbackSolved 2, providerCalls 40 (run-3: 58 - Phase 40 cut retry burn ~30%).
  - Phase 40 machinery verified live: pathRepairs fired on 9+ tasks (1-4 each); lenient matching recovered failing-test mid-run.
  - Remaining misses (syntax-error, promise-return): model repeatedly emits STRUCTURALLY MALFORMED hunks on these shapes even after format feedback - not a matching problem; a diff-format-capability problem. Next rung per research (Aider): whole-file rewrite recovery for the weakest tier.
  - Ratchet note: floor metric revised - harness absolute (13/15) is stable across runs; bare-vs-harness delta fluctuates with bare-lane variance, so the floor is now "harness >= 13/15 AND delta > 0" pending a multi-run mean.
- Phase 41 - Whole-file rewrite recovery after repeated malformed patches - PASS (sandbox gates) / live run 5 pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 41: whole-file rewrite recovery - the harness stops asking for diffs a model cannot produce".
  - Run-4 diagnosis: the two persistent misses emit STRUCTURALLY malformed hunks on specific task shapes even after format feedback - a diff-format capability limit of the 7B, exactly the case Aider solves with whole-file format for weak models.
  - Eval harness lane: after 2 consecutive malformed-patch rejections, the retry prompt switches to "STOP emitting patches - emit write_file with the COMPLETE corrected content"; ACTION_SCHEMA gains `write_file` + `content`; recoveries counted in `wholeFileRecoveries` (assistance visible, never silent); step cap 4->6 (successful tasks exit early; cost grows only on failure paths); bare lane unaided by definition.
  - Verified in sandbox (shadow-tree round 6): tsc exit 0; fence-blind provider proof - bare 0/1 vs harness 1/1 with wholeFileRecoveries=1 kicking in at step 3; reflection A/B and mocked 15-task regressions green; smoke delta asserts PASS.
  - HOST GATES PENDING: e2e/visual/package, live run 5 `npm run eval -- --live --model qwen/qwen-2.5-7b-instruct` (target: 15/15; floor: harness >= 13 AND delta > 0).
  - AAR: SUSTAIN diagnosing failure CLASS before building (matching problem vs format-capability problem led to different rungs). WORK: none fired - clean phase. TOOLS: the ladder pattern is now visible in the code itself: exact match -> lenient match -> format feedback -> whole-file recovery, each rung visible in the scorecard; future rungs should preserve this gradient-of-assistance property.
- MILESTONE - Live run 5: 15/15, SUITE SATURATED - 2026-07-08
  - `qwen/qwen-2.5-7b-instruct` live: bare 0/15, harness 15/15, delta +1.00, actuallyModelDriven 15, fallbackSolved 0, providerCalls 46, providerFailures 0.
  - Control validity: taskPrompt improvements (format exemplar) are shared by BOTH lanes; bare received every prompt improvement and scored 0. The uplift is the loop, not the prompt.
  - Solve anatomy (gradient of assistance, all visible in the scorecard): 10 tasks via patches in 1-2 steps; 2 via rejection-feedback recovery (3 steps); 3 chronic diff-format failures (syntax-error, promise-return, object-copy) via whole-file recovery at steps 3-5 - exactly the tasks run-4 diagnosis predicted. pathRepairs fired on ~13 tasks (decoder empties path habitually; every repair counted).
  - Bare-lane variance across runs 3-5: 2, 4, 0 - single-run bare numbers are noise; harness absolute went 13 -> 13 -> 15 with each rung added.
  - Five live runs total cost: under $0.02.
  - CONSEQUENCE: this suite can no longer measure improvement. Next: tier-2 task suite (multi-file, no-test-suite, small features) and multi-agent context multiplication (blueprint Phase 6) measured against it.
- Phases 42-44 - Goal interface: /goal elicitation, mid-run steering, session-spanning resume - PASS (sandbox gates) / host gates pending - no git repository / sha unavailable
  - Suggested commit title: "Phases 42-44: /goal elicitation, pause/steer/resume, and session-spanning resume - Codex /goal machinery, firewalled".
  - Phase 42 (elicitation): `src/harness/goalContract.ts` parses `/goal <text>` with `done when:` / `constraints:` / `non-goals:` / `budget: $N` / `max steps: N` sections. User doneWhen criteria ADD to the mandatory oracle gates - they can never remove them; the model still cannot argue with the stop condition. `initializeHarness` accepts goalOverrides (doneWhen/constraints/nonGoals -> contract, budgetUsd -> runBudget.maxCostUsd, maxSteps -> state). Commands: `forge-agent.setGoal` (parse+preview); `runAgentGoal` accepts /goal text; the chat bridge intercepts `/goal` messages and returns the compiled contract instead of a chat completion.
  - Phase 43 (steering): `.forge/control.json` written by `pauseGoal`/`resumeGoal`/`steerGoal`; `runStep` applies control BEFORE proposing. Pause -> status `paused` with zero further provider calls; goal edits merge into the live contract without restarting (task graph and evidence retained, steer logged in state+scratchpad); all driving loops exit on `paused` as a resumable non-terminal state.
  - Phase 44 (resume): `loop.resumeFromDisk` rehydrates `.forge/state.json` across sessions - terminal success/failure returned as-is (no zombie resurrection), budget halts resume only via explicit `allowBudgetHaltResume`, resume grants a fresh wall-clock window and `currentStepIndex + N` step allowance while cost spent is retained. Command: `forge-agent.resumeAgentGoal`.
  - Behavioral proof (shadow-tree round 7), all PASS: parser (full syntax/partial/non-directive; gates always present); overrides flow into a live contract/budget/step-cap; pause after step 1 -> zero provider calls while paused; steer mid-run changes goal + step cap with task graph unrestarted; resume in a FRESH loop instance (session boundary) continues from the same step index with the steered goal intact and reaches success; terminal success refuses resume. Regressions green (reflection A/B, mocked 15/15 weak eval, smoke v2 + delta asserts).
  - Named ◐: extension-host e2e assertions for the five new commands are not yet written (behavioral proofs cover the underlying machinery; command-layer e2e rides with the next host-gate run). Webview UI surfaces deliberately deferred (no visual claims made).
  - HOST GATES PENDING: e2e/visual/package (now covering Phases 35-44).
  - AAR: SUSTAIN one-pass trilogy build with a single splice round (7th) - the shadow validation technique is now routine at ~5 minutes per round. WORK: spec'd all three phases in the gap doc before touching code and the build followed the spec 1:1; keep batching tightly-coupled phases. TOOLS: none fired beyond the known mount staleness (session norm, handled).
- Phase 45 - Tier-2 task suite: multi-file, missing-test, features, HELD-OUT judge - PASS (sandbox gates) / live tier-2 baseline pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 45: tier-2 suite with held-out judging - the model can never grade its own homework".
  - New `src/harness/weakEvalTier2.ts`: 7 tasks - 3 multi-file bugs (bug behind a re-export, ignored config default, misnamed import), 2 missing-test tasks (no oracle exists; the harness must AUTHOR test.js first - authored oracles counted in `authoredTest`), 2 features (new function + export wiring, inherently multi-step/multi-file).
  - Integrity property: final `solved` is decided by a runner-owned held-out judge executed after the run and never shown to the model; the model-visible oracle is tracked separately (`workspaceOracleGreen`). Full gradient of assistance carried over; single-file path repair disabled where multiple files make the target ambiguous.
  - CLI `scripts/weak-model-eval-tier2.mjs` + `npm run eval:tier2` (reuses endpoint liveness probe + schema canary for --live). Report at `.forge/evals/latest-weak-model-eval-tier2.json` with per-kind breakdown.
  - Verified in sandbox (shadow round 8): tsc exit 0; mock mechanics run - bare 0/7 vs harness 7/7 with held-out judging, oracle authoring, and multi-step features all asserted; tier-1 mocked regression and reflection A/B green. Smoke asserts added (host `npm run test` validates the package script - shadow package.json predates it, named honestly).
  - HOST GATES PENDING: e2e/visual/package (Phases 35-45), and the LIVE tier-2 baseline: `npm run eval:tier2 -- --live --model qwen/qwen-2.5-7b-instruct`.
  - AAR: SUSTAIN the held-out-judge design (borrowed from eval research; prevents the self-grading failure mode BEFORE it could occur, unlike phases 38-41 which fixed failures after). WORK: one design flaw caught in review-before-validate (mock feature fixes needed prompt-awareness for multi-step) - catching it pre-validation beats catching it in the run; keep reviewing mocks as adversarially as product code. TOOLS: mount synced same-session-created files reliably all day - staleness applies to files predating the session; refined lesson banked.
- MILESTONE - First live TIER-2 baseline - 2026-07-08
  - `npm run eval:tier2 -- --live --model qwen/qwen-2.5-7b-instruct`: bare 1/7, harness 5/7, delta +0.571. By kind: multi-file bugs 2/3, missing-test 1/2, features 2/2. providerFailures 0.
  - Notable behaviors: BOTH features landed (multi-step, multi-file work); the harness forced genuine oracle authoring (authoredTest fired, including on the solved clamp task, which also needed 3 whole-file recoveries); bare's single solve was a trivial single-file shape passing the held-out judge without a test.
  - Loss analysis: t2-broken-import burned all 8 steps on empty-path proposals (multi-file fixtures disable single-file path repair, so the known decoder quirk went unrepaired - dominant fixable loss); t2-missing-test-dedupe authored an oracle but could not land the fix in 8 steps.
  - Next rung identified: content-addressed path repair - empty path + SEARCH block matching exactly ONE workspace file = deterministically unambiguous target, valid for multi-file fixtures. Honest and counted like all assistance.
  - TIER-2 RATCHET FLOOR: harness >= 5/7 AND delta > 0. This is the number the multi-agent (Phase 6) swarm must beat.
- Phase 46 - Content-addressed path repair for multi-file fixtures - PASS (sandbox gates) / live tier-2 re-run pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 46: content-addressed path repair - SEARCH blocks identify their target when exactly one file matches".
  - `resolvePatchTargetByContent` (tools.ts): fires only for apply_patch with an empty path; EVERY hunk must match (exact or lenient) in EXACTLY ONE candidate source file; zero or multiple matches refuse resolution - ambiguity is never guessed away; write_file gets no content guessing, ever. Candidates are the task's declared source files read from current disk state; the model-authored test is never a candidate.
  - Tier-2 lane wiring: successful resolutions counted in `contentAddressedRepairs` (+pathRepairs); failed resolutions append "matched N candidate files - emit the path argument explicitly" to the retry feedback, giving the model an actionable recovery channel.
  - Verified in sandbox (shadow round 9, edits applied programmatically after full mount staleness): tsc exit 0; resolver units (unique exact, unique lenient, ambiguous-2 refused, zero-match, malformed); e2e reproducing the exact live t2-broken-import failure mode (correct patches, always-empty paths) - now solved with contentAddressedRepairs=1, bare still fails; ambiguity-feedback round-trip proven; tier-1 and tier-2 mock regressions green.
  - HOST GATES PENDING: e2e/visual/package, live tier-2 re-run `npm run eval:tier2 -- --live --model qwen/qwen-2.5-7b-instruct` (floor 5/7; target 6/7).
  - AAR: SUSTAIN designing the e2e proof as an exact replay of the live failure mode. WORK: one smoke-assert ordering bug (TDZ - asserted a const before its declaration) caught immediately; lesson: place new asserts inside the section that declares their subject. TOOLS (trigger: mount fully stale even for same-session files): refined again - staleness is universal and lag-based, not age-based; the programmatic-edit shadow technique (apply known replacements to last-good copies, assert anchor counts) is faster and safer than tail-splicing when edits are exact strings; prefer it going forward.
- Live tier-2 run 2 (post Phase 46) - rung verified at kind level - 2026-07-08
  - bare 1/7, harness 5/7, delta +0.571, providerCalls 38 (run 1: 48). MULTI-FILE BUGS 3/3 (was 2/3) - content-addressed path repair closed t2-broken-import exactly as designed. Features 2/2 stable. Missing-test 0/2 (was 1/2) - the hardest kind is live-variance dominated; noted as the primary axis for the swarm A/B and/or a step-budget rung.
  - Pattern holding across both tiers: headline numbers are noisy; kind-level movement tracks each rung precisely.
- Phase 47 - Swarm lane: explorer workers, handoff artifact, focused implementer - PASS (sandbox gates) / live swarm A/B pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 47: the swarm - explorer workers with fresh contexts feed a focused implementer through a handoff artifact".
  - Third A/B lane in the tier-2 runner (`--swarm`): one explorer worker per source file, each a FRESH session seeing only its file + the goal, returning schema-constrained {summary, suspicionScore, keyLines}; findings compile into `.forge/swarm-handoff.json` (context partitioned via artifacts, never transcripts); a single harnessed implementer (full gradient of assistance) works from the handoff - full text of the top suspect only, summaries of the rest, rotating suspects on red oracles. Depth-1, non-nesting, sequential this slice.
  - Measured, not assumed: explorerCalls, handoffChars, implementerPromptChars vs soloPromptChars, suspectRotations, all per task; report sums now include swarm-lane calls (bug caught in validation: the sum initially omitted them).
  - HONEST FINDING from the mechanics proof: on tiny fixtures the swarm is context OVERHEAD (impl/solo prompt ratios 1.08-1.18) - compression requires corpora big enough to compress. On this suite the live A/B tests the swarm's OTHER value: suspicion-guided focus. True compression needs a large-file tier (tier-3 candidate: fixtures with several-hundred-line files).
  - Verified (shadow round 10, deterministic replay after full mount staleness; one structural brace error caught by anchor asserts and fixed): tsc exit 0; mock 3-lane run bare 0/7, harness 7/7, swarm 7/7; one explorer per file; handoff artifacts persisted; prompt metrics recorded; suspect rotation on features; tier-1, tier-2-no-swarm, and CLI regressions green.
  - HOST GATES PENDING: e2e/visual/package, and the LIVE swarm A/B: `npm run eval:tier2 -- --live --swarm --model qwen/qwen-2.5-7b-instruct` (question: does swarm beat harness 5/7, especially on multi-file and missing-test kinds, at ~2x calls?).
  - AAR: SUSTAIN metrics-before-belief (the compression assertion FAILING was the phase's most valuable output - it converted an assumption into a scoping law: swarm pays on big contexts, taxes small ones). WORK: the class-brace insertion error recurred (second structural-anchor mistake this session) - lesson: when inserting class members, anchor INSIDE the class on the preceding method, never on module-level code after it. TOOLS: deterministic-replay shadow validation held up under heavy multi-edit phases; anchor-count asserts caught both the brace error and the truncated CLI copy.
- MILESTONE - First live swarm A/B: kind-level tradeoff, not a blanket win - 2026-07-08
  - Three lanes, same tasks, held-out judge: bare 1/7, solo harness 4/7, swarm 3/7. providerCalls 99 total (swarm lane ~2.5x solo for a lower headline) - the research's multi-agent token tax, measured.
  - MISSING-TEST: swarm 2/2 vs solo 0/2 (solo was 1-for-4 across all prior live runs). Explorer digest + focused implementer wins where the task is noisy and single-file: author oracle, then fix. The swarm's first real earned keep.
  - MULTI-FILE BUGS: swarm 1/3 vs solo 3/3. Seam blindness, exactly as predicted in the arcs discussion: the implementer sees only the prime suspect in full, so relationship defects (misnamed import across two files) vanish when suspicion points at the wrong side of the seam. FEATURES: swarm 0/2 - two-file coordinated edits, same seam problem. Solo features 1/2 this run (2/2 prior - variance).
  - Interpretation discipline: every cell is n=1-2; kind-level PATTERN (swarm wins noisy single-file, loses cross-file) is consistent with mechanism, but means need 2-3 repeat runs (pennies) before building dispatch logic on them.
  - Next rungs, in order: (1) repeat live swarm runs for kind-level means; (2) INTERLOCKING ARCS - each explorer and the implementer get the exported signatures of the suspect's direct dependencies (deterministic, zero extra calls) - targets the multi-file/feature losses; (3) if means hold, terrain-based lane dispatch (deterministic routing by task kind: swarm for missing-test shapes, solo for cross-file), the orchestrator picking the formation by terrain, not a model picking by vibes.
- Phase 48 - Interlocking arcs: seam interfaces across explorer/implementer sectors - PASS (sandbox gates) / live re-run pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 48: interlocking arcs - sectors overlap at the seams so relationship bugs are visible from either side".
  - `extractInterlocks` builds the require-graph deterministically (relative-require resolution, zero provider calls); each file gets deps, dependents, and an interface signature (declarations + module.exports lines). `renderInterlocks` shows every explorer its file's seams (neighbor exports + dependent import lines) and shows the swarm implementer the prime suspect's seams, recomputed from fresh disk state each step. Interlocks persist in the handoff artifact.
  - Verified (shadow round 11): unit - on the t2-broken-import fixture BOTH sides of the seam see the mismatch (index.js arc sees `function formatName`; format.js arc sees the dependent's misnamed `formatname` import); CAUSAL e2e - a seam-blind provider that can only emit the correct cross-file fix when the neighbor signature appears in its prompt now solves the relationship bug through the swarm lane; explorers verified to receive seam context.
- Phase 49 - Terrain dispatch: deterministic lane routing by task kind - PASS (sandbox gates) / live dispatch run pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 49: terrain dispatch - a lookup table picks the formation, solo for cross-file, swarm for oracle-authoring".
  - `--dispatch` routes each task to ONE lane via `DEFAULT_TERRAIN_ROUTING` (data-derived from the 2026-07-08 live A/B: multi-file->harness, missing-test->swarm, feature->harness), overridable via options.routing for when repeat-run means revise the map. Per-task `dispatchLane` recorded; report gains `dispatchSolved`; headline/delta computed from the routed lane. The router is a lookup, not a model.
  - Verified (shadow round 11): mock dispatch 7/7 with per-table routing and only the routed lane executed per task; routing override respected; no-dispatch, 3-lane, and tier-1 regressions green.
  - HOST GATES PENDING (35-49): e2e/visual/package, plus live: swarm A/B re-run with interlocks `npm run eval:tier2 -- --live --swarm --model qwen/qwen-2.5-7b-instruct` (does seam-sight fix multi-file 1/3?), then dispatch `npm run eval:tier2 -- --live --dispatch --model qwen/qwen-2.5-7b-instruct` (composite target: >5/7 at lower cost than 3 lanes).
  - AAR (48+49): SUSTAIN causal proof design (fix gated on the exact context the phase adds) and data-derived defaults with explicit revision paths. WORK: clean phases, spec followed 1:1; batching two small coupled phases in one validation round halved overhead. TOOLS: deterministic-replay shadow validation now the fully standard path; anchor asserts caught zero errors this round (the technique is stabilizing the process, not just detecting failures).
- MILESTONE - TIER-2 SATURATED VIA TERRAIN DISPATCH: 7/7 LIVE - 2026-07-08
  - Dispatch run: bare 0/7, dispatchSolved 7/7, delta +1.00, providerCalls 43 (vs 99-101 for 3-lane runs - the composite is CHEAPER than measuring, and comparable to single-lane runs). Routing performed exactly as designed: multi-file 3/3 via solo harness, missing-test 2/2 via swarm, features 2/2 via solo harness. Every kind routed to its winning formation, every task solved, zero false successes, all model-driven.
  - Swarm+interlocks run (same session): swarm multi-file improved 1/3 -> 2/3 (seam-sight helped; not yet at solo parity), missing-test 2/2 AGAIN (now 4/4 across two runs - the swarm's dominance on oracle-authoring shapes is no longer noise), features 0/2. Solo lane 4/7 this run (variance).
  - Caveats named: dispatch n=1; new tier-2 ratchet floor set at dispatch >= 6/7 (allowing one variance flip) with the kind-level routing table as the protected artifact. Missing-test-via-swarm is the best-replicated cell in the whole suite.
  - The session's arc, complete: same 7B model - bare 0-13% at every tier; tier-1 harness 15/15; tier-2 solo 5/7; tier-2 dispatched 7/7. The slide now picks the tube per rider, deterministically, and the whole day of live experimentation cost well under a dollar.
- Phase 50 - Gradient of assistance ported into the MAIN AgentHarnessLoop - PASS (sandbox gates) / package+reinstall pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 50: the product loop now carries everything the evals proved - path repair, format exemplar, whole-file recovery".
  - Ported from the proven eval lanes into `AgentHarnessLoop.runStep`/`systemPrompt`: (a) content-addressed path repair pre-validation - empty-path apply_patch resolved via `resolvePatchTargetByContent` over files the agent has READ this run (bounded deterministic candidates, unique-or-refuse, ambiguity logged with candidate count); (b) malformed-patch streak tracking with the format exemplar embedded in firewall-rejection reflections; (c) at streak >= 2 the proposal prompt gains whole-file guidance (STOP apply_patch, write_file COMPLETE content); (d) streak clears on ANY successful file mutation. All counted in runStats: `pathRepairs`, `malformedPatchStreak`, `wholeFileGuidanceInjections` - assistance visible in the product exactly as in the evals.
  - Verified (shadow round 12): tsc exit 0; PROOF 1 - main-loop run solves with an empty-path patch after read_file, pathRepairs=1, repair logged; PROOF 2 - a fence-blind provider recovers to success via injected whole-file guidance, exemplar present in reflection details, streak cleared after the successful write_file. Proof 2 initially FAILED and caught a real port flaw (streak reset only on apply_patch, so guidance never cleared after whole-file recovery) - fixed to match eval-lane semantics before close. Regressions green (reflection A/B, tier-1 mock 1/15->15/15, tier-2 dispatch mock 7/7). Smoke asserts added for all four port markers.
  - DEPLOYMENT: the Antigravity-installed extension is the Phase-34 build. To ship everything from Phases 35-50 into the running agent: `npm run package` then reinstall `forge-agent-0.0.1.vsix` (plus the still-pending e2e/visual host gates).
  - AAR: SUSTAIN behavioral proofs that FAIL first - proof 2's failure was the phase's key catch, the third time this session a proof caught a subtle state-lifecycle flaw before live. WORK: when porting between architectures (stateless mini-lane loop variables vs persistent state fields), enumerate every lifecycle event of the source variable (set, read, CLEAR) - the clear is the one that gets missed. TOOLS: none fired beyond session norms.
- Phase 51 - Tier-3 large-corpus suite: truncation-honest solo vs swarm - PASS (sandbox gates) / live tier-3 pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 51: tier-3 - where context stops being free, the swarm earns its keep and the oracle pierces truncation".
  - Suite: 5 deterministically generated large-corpus tasks (no network): 2 large-file-bug (defect ~24k chars deep in a 34k file), 2 haystack (defect deep in 1 of 12 files, ~100k corpus), 1 large-seam (misnamed cross-file call deep in a big consumer). Generation-time PROOF asserts every defect sits beyond the solo lane's per-file truncation horizon. Runner reuse via `options.tasks` + `tier`; `taskPrompt` gains `SOLO_PROMPT_CHAR_BUDGET` (24k) with VISIBLE truncation markers; tier-3 mock is honest-by-construction (fixes only defects visible in its prompt). CLI `npm run eval:tier3` (--swarm/--dispatch).
  - STRUCTURAL RESULTS (mock, information-gated): bare 0/5; truncated solo harness 1/5; swarm 5/5.
  - FINDING 1 - THE ORACLE PIERCES TRUNCATION FOR CRASH-CLASS DEFECTS: the solo lane solved the seam because Node's ReferenceError output QUOTES the offending source line, and test output flows into the next prompt - verification feedback is a context channel that truncation cannot close. Silent wrong-value defects stay hidden (solo 0/4 on those). Confirmed by bare/harness contrast on identical prompts minus the oracle channel.
  - FINDING 2 - COMPRESSION IS A HAYSTACK PROPERTY: implementer/solo prompt ratios - haystack 0.36 (strong compression, many files one suspect); large-file 1.33-1.41 (a single file bigger than the whole budget cannot compress - the swarm wins on INFORMATION COMPLETENESS at higher prompt cost; chunked explorers are the future rung); large-seam 1.14 (two-file corpora barely compress, and interlock signatures for 180-function files add ~5k - signature capping is a cheap future rung).
  - FINDING 3 - two of my own artifacts failed honest validation before the suite could pass: a held-out judge with an undeclared identifier (the judge itself was buggy; the swarm had actually solved the task), and mock regexes silently broken by the padding change (swarm 5/5 -> 1/5 with identical lane code). Both caught by the proofs, both fixed; fixture-sizing math was also wrong once (defects visible pre-padding) and caught by the generation-time assertion.
  - Regressions green: tier-2 dispatch mock 7/7, tier-1 mocked 15/15, reflection A/B, smoke invariants (incl. new tier-3 asserts).
  - HOST GATES PENDING: e2e/visual/package + reinstall; live tier-3: `npm run eval:tier3 -- --live --swarm --model qwen/qwen-2.5-7b-instruct` (the first real large-corpus measurement; expect swarm >> solo on haystack, and watch whether the real model can use oracle leakthrough on the seam like the mock did).
  - AAR: SUSTAIN generation-time assertions (they caught the sizing error instantly) and information-gated mocks (they turn context claims into physics). WORK: two self-inflicted validator bugs in one phase - new rule: JUDGES AND MOCKS GET THE SAME REVIEW RIGOR AS PRODUCT CODE, and any fixture-content change requires re-running every proof that pattern-matches fixture content. TOOLS: the eval infrastructure absorbed an entire new tier via options injection with zero runner-logic changes - the Phase 45 architecture paid off; leftover diagnostic fixtures (~680 dirs) should be cleaned by keepFixtures defaults in future diagnostic runs.
- Phase 52 - Architect/editor lane: one planning call points the implementer - PASS (sandbox gates) / live rate-split run pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 52: architect lane - plan big, execute small, meter the split".
  - Fifth lane in the shared runner (`--architect`, `--architect-model <slug>`): ONE schema-constrained planning call (PLAN_SCHEMA: premiseCheck, targetFile, approach, doneWhen) against the SAME truncated view the solo lane gets - planning is judgment over goal + structure, not reading. The standard 7B implementer then runs the full-gradient mini-loop with the plan injected and the planned target file shown in full; empty paths repair to the planned target; the prompt instructs "verify the premise before trusting the plan" (cookbook caveat 3, slice 1). Architect model and cost metered SEPARATELY (`architectCalls`, `architectCost`) - the rate split is measured, never assumed.
  - STRUCTURAL PROOF (tier-3, information-gated mock): architect 5/5 (vs bare 0/5, truncated solo 1/5, swarm 5/5) with the goal->file mapping correct on every task and exactly one planning call each. ECONOMICS: architect lane 10 total calls vs swarm 35 for the same 5/5 - on this suite, one good plan replaces twelve explorers. Regressions green (tier-2 dispatch 7/7, tier-1 15/15, reflection A/B).
  - The lane matrix is now complete for the cookbook comparison: bare / solo / swarm / architect / dispatch, all held-out judged, all separately metered.
  - HOST GATES PENDING: e2e/visual/package + reinstall; LIVE architect runs: same-model ablation `npm run eval:tier3 -- --live --architect --model qwen/qwen-2.5-7b-instruct`, then the true rate split with a stronger planner, e.g. `--architect-model <mid-tier slug>` (check structured_outputs support on its endpoints first).
  - AAR: SUSTAIN reusing the shared runner - a whole new lane cost ~150 lines and zero regressions; the Phase 45 architecture keeps compounding. WORK: clean phase, spec followed 1:1, proofs passed first run - the validator-rigor lesson from Phase 51 was applied (mock planner reviewed like product code before validation). TOOLS: none fired; deterministic-replay shadow validation handled an 11-anchor replay without error.
- Version stamp for deployment verification - 2026-07-09
  - package.json version bumped 0.0.1 -> 0.52.0 (Phase-52 build) and description gains a phase tag. VERIFICATION AFTER INSTALL: (1) VSIX filename is now `forge-agent-0.52.0.vsix`; (2) Antigravity Extensions panel shows Forge Agent v0.52.0 (old build shows 0.0.1); (3) extension folder becomes `.antigravity-ide\extensions\kennyg.forge-agent-0.52.0`. Any of the three proves the update landed. Future phases: bump minor to the closing phase number at each package.
- Phase 54 slice 1 - UX: native settings, config-driven runs, pause/resume buttons, /goal in the composer - ◐ CODE-COMPLETE, VISUAL VALIDATION DELEGATED TO USER - no git repository / sha unavailable
  - Suggested commit title: "Phase 54.1: settings surface + run control buttons - the proven machinery gets an interface".
  - Native settings (contributes.configuration): forge.maxCostUsd, maxWallClockMinutes, maxSteps, reflectionEnabled, architectModel, defaultCodingModel/defaultMixedModel join the existing apiKey/provider keys. UI runs now READ them: the webview 'run-agent-loop' path applies config budget caps, step cap, and reflection toggle, and parses /goal directives typed in the composer (full elicitation in the panel, not just the command API).
  - Run control: Pause (testId pause-run) and Resume (resume-run) buttons in the composer icon row; bridge cases 'pause-goal'/'resume-goal' write .forge/control.json; resume drives the loop onward from the paused state (one step consumes the cleared flag - the applyControl lifecycle). FIXED A REAL PHASE-43 GAP FOUND IN RECONCILE: the webview bridge run loop did not exit on 'paused', so UI-started runs could never pause; all bridge loops are now paused-aware.
  - Sandbox verification (shadow round 15): tsc exit 0 on all extension-side changes; package.json valid with new properties; static proofs (bridge cases, paused-aware loops x3, config reads). App.tsx changes (2 anchored edits: Pause import + two IconButtons mirroring existing patterns) are vite-built on HOST only - named ◐.
  - USER VISUAL VALIDATION CHECKLIST (after `npm run package` + reinstall v0.52.0+): (1) Settings -> search "forge" -> new keys visible with descriptions; (2) set forge.maxCostUsd=0.05, run a goal, confirm budget halt honors it; (3) type "/goal fix X\ndone when: Y" in the composer, hit Run, confirm the goal contract reflects Y in .forge/goal-contract.json; (4) start a run, click Pause -> status shows paused and NO further steps; click Resume -> run continues; (5) pause/resume buttons render next to Play/Step.
  - AAR: SUSTAIN reconcile-before-build (found the bridge pause gap - the button would have "worked" while the loop ignored it). WORK: UX phases need the visual gate planned INTO the close from the start - this ◐-with-checklist pattern is the honest shape when the builder has no screen. TOOLS: webview tsx is outside tsc's gate - a future rung should add `tsc --noEmit` coverage or vitest for src/webview to give sandbox sessions any static signal on UI code.
- Phase 55 SPEC - Forge Studio UI: sessions, memory, right rail (Claude-desktop layout) - DOCUMENTED, build next session - see FORGE_UI_SPEC.md
  - Key reconcile insight: every requested panel already has a Forge data source - taskGraph = progress checklist (cross-out = task status), state.files = context documents, lessons/AAR = memory, runStats = capabilities counters, sessionId already keys every run. The gap is STORAGE (state.json is an overwritten singleton -> .forge/sessions/<id>/ with meta/chat/index) and SURFACING. Six build slices, storage first; UI slices close ◐ pending screenshots per the Phase 54 protocol.
- Phase 55.1 - Session storage foundation + session bridge - PASS (sandbox behavioral) / UI slices 55.2-55.6 next session per FORGE_UI_SPEC.md - no git repository / sha unavailable
  - Suggested commit title: "Phase 55.1: durable sessions - every run persists under .forge/sessions/<id> with contextual titles, a pinnable index, and per-session memory".
  - `persistSession` in loop.ts: every persist also writes .forge/sessions/<sessionId>/{state.json, meta.json} and maintains sessions/index.json (pinned-first, newest-first, capped 200). autoTitle = goal cleaned of /goal syntax, 60 chars; user title preserved once set; titles cosmetic, sessionId is identity; persistence failures can never break a run. Active .forge/state.json copy kept for backcompat (resumeFromDisk, artifact paths).
  - Bridge cases: list-sessions, load-session (loads meta+state+chat, makes it the active session), pin-session (re-sorts index), save-chat (per-session chat.json, capped 500 msgs - chat was NEVER persisted before).
  - Behavioral proof (shadow round 16): two live-loop sessions -> both durable with correct metas; /goal prefix cleaned from title; index tracks both newest-first; pinned sorts above newer; resumeFromDisk still resolves the latest session (backcompat); per-session state carries its own AAR (memory per session). tsc exit 0; tier-1 and reflection A/B regressions green; smoke asserts added (7).
  - Remaining slices (specced in FORGE_UI_SPEC.md, UI = screenshot-gated): 55.2 left session rail, 55.3 right-rail progress checklist + steering, 55.4 context/memory/connectors groups, 55.5 chat restore + title polish, 55.6 polish. The webview can already call the new bridge messages.
  - AAR: SUSTAIN storage-before-UI ordering (every rail now has a queryable backend) and never-break-the-run guards on persistence. WORK: clean slice, spec followed. TOOLS: mount served the fresh loop.ts edit intact this round (sync is intermittent, not absent - probe before replaying).
- Phase 54.2 hotfix - chat grounding + selectable text - ◐ CODE-COMPLETE, user validates after rebuild - no git repository / sha unavailable
  - Suggested commit title: "Phase 54.2: chat grounding with hard UI facts - the model may not invent workflows; chat text selectable".
  - FIELD BUG (first real-user session, deepseek-v4-pro): the chat model hallucinated an elaborate fake workflow ("Forge: Propose command", manual paste-and-commit steps) because our own chat system prompt mentioned PROPOSE/VALIDATE/COMMIT/NARRATE without stating the real controls. Chat has no tools; the model filled the vacuum with plausible UI fiction - the exact trust failure the firewall exists to prevent, occurring in the one ungated lane.
  - Fix 1: runChatCompletion system prompt rewritten as HARD UI FACTS: chat is advisory-only with no tools; the only execution path is goal-in-composer + Run button; the real /goal syntax; pause/resume exist; explicitly states there is NO "Forge: Propose" command and NO manual commit step; instructs the model to hand users a concrete /goal line + "click Run", and to admit uncertainty about UI rather than invent steps.
  - Fix 2: chat log container gains select-text + explicit userSelect:text (webview default blocked text selection).
  - Validation: extension.ts change is a string constant (host tsc gates it); App.tsx className/style edit mirrors existing patterns (host vite gates). USER CHECKLIST after rebuild+reinstall: (1) ask chat to "make me a desktop organizer" - it must answer with a suggested /goal line and "click Run", NOT fake commands; (2) select/copy chat text.
  - AAR: SUSTAIN real-user sessions as the ultimate eval - one user question exposed a lane every mock passed. WORK: any string the model sees IS prompt surface - our own grounding text seeded the hallucination vocabulary; audit all model-visible strings for undefined referents. TOOLS: queue a "chat grounding" static smoke assert (system prompt must name the Run button and deny fake commands) - added next session with the 55.x slices.
- Phase 54.3 - Command menu: "+" button, "/" autocomplete, GOAL MODE pill - ◐ CODE-COMPLETE, user validates after rebuild - no git repository / sha unavailable
  - Suggested commit title: "Phase 54.3: discoverable commands - + menu, slash autocomplete with Tab-complete, and a GOAL MODE pill so /goal visibly arms".
  - Composer gains: a "+" toggle (testId command-menu-toggle) opening a command menu; the same menu auto-opens while input starts with "/" until a known command+space is typed; click inserts, Tab completes a partial /goal, Escape closes. SLASH_COMMANDS registry (extensible) with hints including the full /goal sub-syntax. When input starts with "/goal ", a GOAL MODE pill renders above the box: the user SEES the command armed and is told Run executes it (Enter still chats) - directly answers "so I know it's working".
  - All UI-only App.tsx changes mirroring existing Tailwind/JSX patterns; vite gates syntax on host build. USER CHECKLIST after rebuild: (1) click + -> menu lists /goal with hint; (2) type "/go" -> menu filters, Tab completes to "/goal "; (3) GOAL MODE pill appears; (4) press Run -> goal contract in .forge reflects the directive; Enter -> chat responds per the 54.2 grounding.
  - AAR: SUSTAIN answering UX asks with visible STATE (the pill is truth from the input, not decoration). WORK: command registry deliberately extensible - future /pause /resume /session entries are one line each. TOOLS: none.
- Phase 56.1 - Deep research mode: /research -> web-grounded workers -> cited artifact attached to the conversation - ◐ CODE-COMPLETE, user validates live after rebuild - no git repository / sha unavailable
  - Suggested commit title: "Phase 56.1: /research - plan, web-grounded workers via OpenRouter :online, cited artifact that grounds the chat".
  - `src/harness/research.ts` runDeepResearch (ChatGPT-deep-research shaped, firewalled): (1) one planning call decomposes the question into 3-5 sub-questions (schema-constrained); (2) one WEB-GROUNDED worker per sub-question via the OpenRouter ':online' suffix (any model slug + live web search + citations; zero scraping infra); workers are instructed to cite URLs for every claim and to report unanswered rather than answer from memory; failed workers surface as UNANSWERED, never papered over; (3) synthesis composes ## Answer / ## Key findings (claims with source URLs) / ## Conflicts & uncertainty / ## Sources, with raw worker findings appended. Artifact persists to .forge/research/<ts>-<slug>.md.
  - ATTACHMENT: the chat bridge intercepts "/research <question>" (progress message, then the report + save path posted to chat); the artifact path joins the session's attached-research list, and runChatCompletion injects the last 2 artifacts (8k cap each) as system context on EVERY subsequent chat turn - the conversation is grounded by its research from then on. Chat grounding prompt documents /research; SLASH_COMMANDS menu gains the /research entry with hint (Enter executes - research IS a chat-lane action, unlike /goal).
  - Design law kept: research is read-only by construction (produces a document, never mutations) and claims carry sources - the artifact is evidence-shaped, not narration.
  - Validation honesty: web-grounded calls require the live OpenRouter key -> no sandbox behavioral proof possible; extension/module changes are tsc-gated on host build; webview edit is one registry line. USER CHECKLIST after rebuild: (1) type "/" -> menu shows /research; (2) "/research current best 7B coding models" -> progress message, then cited report in chat; (3) .forge/research/ contains the .md; (4) ask a follow-up chat question -> answer reflects the artifact (attachment working); (5) a bogus topic yields uncertainty, not confident fiction.
  - Known limits for next slice (56.2): attachment list is per-extension-host-session (not yet persisted into session meta - wire into Phase 55.1 meta.researchArtifacts); artifacts attach to CHAT context only, not yet to /goal runs; ':online' pricing adds search cost per worker call - consider a forge.researchModel setting.
  - AAR: SUSTAIN reuse of proven shapes (plan-big/execute-small became the research pipeline verbatim). WORK: first draft misread the ask as repo-research - one clarifying screenshot corrected it; lesson: when a user names a feature by a competitor's product name, match THAT product's shape first. TOOLS: OpenRouter :online suffix is the cheapest web-tool on-ramp - banked for future web-capable phases.
- MILESTONE - LIVE TIER-3: ARCHITECT 5/5 WHERE SOLO GOT 0/5 - 2026-07-09
  - Run 1 (swarm): bare 0/5, truncated solo 0/5 (structural blindness CONFIRMED LIVE - including the seam: the real 7B could not exploit oracle leakthrough the way the mock did), swarm 3/5 @106 calls (large-file 2/2, haystack 1/2, seam 0/1).
  - Run 2 (architect, SAME-MODEL ablation): architect 5/5 @62 calls. The same 7B that is structurally blind solo solved EVERYTHING when it planned first - one truncated-overview planning call naming the target, then focused execution. Architect beats swarm on solve rate AND cost, live, with the weak model in every seat. The plan-big-execute-small thesis holds even when "big" is the same small model given a planning turn.
  - Terrain map update: tier-3 kinds should route to ARCHITECT (revise DEFAULT_TERRAIN_ROUTING + add architect as a routable dispatch lane - next session).
  - REPORT BUGS found by these runs (fix next session): headline status/passed ignores swarm/architect lanes (printed no_uplift while architect swept); byKind lacks architectSolved outside dispatch. Duplicate package.json config keys (Phase 54 re-added existing defaultCodingModel/defaultMixedModel) FIXED now.
  - Cumulative live proof across tiers, one 7B model, total spend still ~a dollar: t1 15/15, t2 dispatch 7/7, t3 architect 5/5. The slide holds at every tier once the right formation is picked - and picking formations is deterministic.
- Phase 57.1 - Tier-4: tasks that cannot be pattern-matched - PASS (sandbox gates) / live tier-4 pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 57.1: tier-4 - the leak law, symptom-only goals, a causal chain, a spec-gap feature, and a judge-proven suite".
  - THE LEAK LAW enforced in code: `assertNoLeak` tokenizes the goal and refuses any suite where a goal token appears within 5 lines of the defect (the CLI runs it before every eval). It immediately caught ITS OWN AUTHOR ("settings" in a goal sat beside the require line at the defect) - goal reworded; the negative case (a deliberately leaking goal) is asserted refused.
  - 4 tasks, each a different wall: t4-symptom-pricing (symptom-only cross-file cause + a reviewer-bait distractor comment on CORRECT code), t4-symptom-pipeline (8-stage haystack, cause mid-chain, goal describes user-visible symptom only), t4-causal-timeout (TWO coordinated edits, either alone stays red - PLAN_SCHEMA gains optional ordered subtasks), t4-spec-limiter (implement per SPEC.md; the held-out judge tests non-invocation past the limit, an edge the goal never states).
  - SOLVABILITY proven without any model: every task carries a hidden `provenFix` (+ PROVEN_EXTRA for the causal chain) used ONLY by validation - pristine fixtures FAIL the judge, proven-fixed fixtures PASS judge and workspace tests. Unsolvable-task and self-leaking-suite failure modes are now structurally impossible.
  - The tier-4 mock is deliberately pattern-blind and solves NOTHING (0 across all 4 lanes) - the live-vs-mock gap IS the measurement: any live solve is reasoning, not plumbing. All regressions green (t1 15/15, t2 dispatch 7/7, t3 architect 5/5).
  - LIVE (user): `npm run eval:tier4 -- --live --architect --model qwen/qwen-2.5-7b-instruct` then the DeepSeek-planner variant - on symptom-only goals the regex-planner and the reasoner finally produce different numbers. Expect the 7B to stop acing; zeros that persist are the true model ceiling, measured at last.
  - AAR: SUSTAIN laws-as-assertions (the leak law catching my own task 60 seconds after I wrote it is the whole methodology in one moment). WORK: goals need adversarial wording review like any model-visible string. TOOLS: the pattern-blind mock inverts the usual mock role - from proving success possible to proving success MEANINGFUL; banked as the standard for capability (vs plumbing) tiers.
- MILESTONE - LIVE TIER-4: THE PLANNERS SEPARATE; THE CAUSAL CHAIN IS THE MEASURED WALL - 2026-07-09
  - Same executor (qwen-2.5-7b), same symptom-only leak-law suite, two planners:
    - 7B-as-architect (same-model): architect 1/4 @59 calls - the regex-planner collapses when goals stop naming targets, exactly as tier-3 saturation predicted.
    - DeepSeek-v4-pro-as-architect + 7B executor: architect 3/4 @34 calls - the reasoning planner recovers the solve rate at nearly HALF the calls. First live rate-split win: better plans are cheaper, not just smarter.
  - Solo harness: 3/4 in both runs (small fixtures fit in context; full visibility + oracle feedback lets the 7B reason passably alone - solo remains competitive when context is not starved). Bare: 0/4 and 1/4 (the self-contained spec-limiter one-shot, variance).
  - THE RESIDUE: t4-causal-timeout (two coordinated edits, either alone stays red) went 0 across EVERY lane in BOTH runs - the designed choke point is the measured frontier: coordinated multi-edit reasoning. Next rung is already scaffolded: PLAN_SCHEMA carries ordered subtasks; Phase 57.2 = an implementer that executes plan subtasks sequentially with per-subtask oracle checks.
  - Standing results table, one weak executor, all live, all held-out judged, total project spend ~a dollar: T1 15/15 (gradient) | T2 7/7 (dispatch) | T3 5/5 (architect-focus) | T4 3/4 (reasoning planner + weak executor), residue = causal chains.
  - The instrument did its job at every tier: saturation forced a harder question; the harder question found the real wall. Calibration record across the session: predictions bounded every live result except one undershoot (run 5 tier-1).
- Phase 57.1 hotfix - eval report headline and by-kind architect accounting - PASS (host smoke + compile + mocked evals) - no git repository / sha unavailable
  - Suggested commit title: "Phase 57.1 hotfix: architect solves count in eval headlines and by-kind reports".
  - Fixed the report bugs found by live tier-3: the shared tier runner now includes `architectSolved` in `byKind`, and headline `status`/`passed` uses the best active non-dispatch lane (`harness`, `swarm`, or `architect`) instead of always using solo harness. Dispatch still owns the headline when `--dispatch` is active.
  - Fixed an existing smoke-test ordering bug that referenced `webview` before initialization, which made `npm run test` fail before reaching the new invariants.
  - Validation: `npm run test` PASS; `npm run compile` PASS; `node scripts/weak-model-eval-tier4.mjs --architect` confirms tier-4 by-kind output includes `architectSolved`; `node scripts/weak-model-eval-tier3.mjs --architect` now reports `status: "uplift_observed"`, `architectSolved: 5`, and per-kind architect counts.
  - Remaining known issue: tier-4's `PROVEN_EXTRA` is declared for the coordinated two-file causal task but is not wired into any validation helper in the current repo. The live measurement is still meaningful because the held-out judge tests the actual model output, but the documented "proven-fixed passes" claim for that one task needs a follow-up validation helper before being treated as mechanically proven.
- Phase 57.2 - Coordinated-subtask execution: ordered plan steps for causal chains - PASS (host smoke + compile + focused behavioral proof) / live rerun pending - no git repository / sha unavailable
  - Suggested commit title: "Phase 57.2: coordinated subtasks - the architect plan executes file-by-file with oracle checks".
  - Reconciled root cause: `PLAN_SCHEMA` had `subtasks`, but the architect lane ignored them and treated every task as a single-target-file repair. That fit tier-3 focus tasks and failed the tier-4 causal chain, where `src/settings.js` and `src/service.js` both need edits and either single edit leaves tests red.
  - Built: architect plans now persist `planSubtasks`; architect results record `subtaskChecks`; the implementer prompt shows the full ordered plan plus one `CURRENT SUBTASK`; files named by the current subtask are shown in full; prior subtask oracle output feeds into the next turn; empty-path repair targets the current subtask's single referenced file before falling back to the primary target.
  - Built: Tier-4 now has `applyProvenFixes`, `proveTier4TaskSolvable`, and `proveTier4SuiteSolvable`; the Tier-4 CLI refuses to run if pristine fixtures do not fail or proven-fixed fixtures do not pass. This wires `PROVEN_EXTRA` for `t4-causal-timeout` into real validation.
  - Validation: `npm run test` PASS; `npm run compile` PASS; `node scripts/weak-model-eval-tier3.mjs --architect` remains PASS with architect 5/5; `node scripts/weak-model-eval-tier4.mjs --architect` PASS with default pattern-blind mock still 0/4 after the new solvability gate; focused coordinated-provider proof on only `t4-causal-timeout` solved 1/1 with two subtask checks: subtask 1 red (settings edit alone) and subtask 2 green (service edit completes the chain), with `pathRepairs: 2`.
  - Validation caught a real proof bug first: the initial solvability helper ran `npm run test --silent`, which made proven workspace tests appear false while held-out tests passed. Fixed by running `node test.js` directly in the proof fixture, matching the held-out runner's direct execution model.
  - AAR: SUSTAIN failed-first validation and focused synthetic providers for capability rungs; they prove runner mechanics without pretending a pattern-blind mock can reason. WORK: if a schema field exists but the runner ignores it, live success can hide the dead field until a harder tier exposes it. TOOLS: direct `node test.js` is the right low-noise proof surface for generated fixture tests; npm flag semantics add avoidable uncertainty.
- Phase 57.2a - Live subtask normalization hotfix: object subtasks preserved - PASS (host smoke + compile + focused proof + one live rerun) - no git repository / sha unavailable
  - Suggested commit title: "Phase 57.2a: normalize real planner subtasks - preserve object-shaped file/action plans".
  - Live trigger: DeepSeek planner returned `subtasks` as objects on `t4-causal-timeout`; the runner converted them to `"[object Object]"`, erasing `src/settings.js` and `src/service.js`, so focused file context and empty-path repair could not work.
  - Fix: `normalizePlanSubtask` now accepts strings and common object shapes (`path`/`file`/`targetFile`, `action`/`description`/`instructions`), producing readable `file: action` strings and JSON fallback instead of `[object Object]`.
  - Validation: `npm run test` PASS; `npm run compile` PASS; focused object-subtask provider solves `t4-causal-timeout` with normalized subtasks `src/settings.js: ...` then `src/service.js: ...`, subtask 1 red and subtask 2 green, `pathRepairs: 2`.
  - Live rerun after fix (DeepSeek planner + Qwen 7B executor): bare 1/4, solo harness 2/4, architect 3/4 @45 calls, providerFailures 1, cost ~$0.011. `t4-causal-timeout` still red, but the failure became diagnostic: planSubtasks were now usable (`src/settings.js`, `src/service.js`), and subtask checks showed partial progress (`1000 -> 250 -> 5000 vs 42`) before the model started trying to edit `test.js`.
- Phase 57.3 - Eval oracle immutability: protect provided workspace tests - PASS (host smoke + compile + focused proof) / live rerun inconclusive - no git repository / sha unavailable
  - Suggested commit title: "Phase 57.3: protect visible eval tests - the model may not edit the oracle".
  - Built: all shared eval lanes reject `apply_patch`/`write_file` to `test.js` when a task already supplies `workspaceTest`, except `missing-test` tasks where authoring the oracle is the point. Rejection reason: `Protected workspace oracle...`, fed back through the normal reflection loop.
  - Validation: `npm run test` PASS; `npm run compile` PASS; focused provider proof first attempted `write_file test.js`, then fixed `src/limiter.js`; the kept fixture confirmed `test.js` stayed original and the source file changed.
  - Live rerun attempt after this guard exceeded 10 minutes and was stopped; no live report was written, so this phase does NOT claim live Tier-4 improvement. The last valid live Tier-4 result remains the 57.2a rerun above.
  - AAR: SUSTAIN held-out judging plus visible-oracle immutability; held-out tests prevented false success, and the new guard prevents wasted test-tampering loops earlier. WORK: a model attempting to edit tests in a provided-test eval is a harness smell, not a model result. TOOLS: long live evals need per-call timeout/report flushing; current CLI can hang past useful observation windows.
- Packaging/install proof - Phase 57 build installed in Antigravity - PASS at CLI/disk-marker level / live UI proof pending - no git repository / sha unavailable
  - package.json bumped `0.52.0` -> `0.57.3` and description updated so Antigravity install state is unambiguous. Same-version `0.52.0` VSIX installs exited 0 but did not refresh the installed folder.
  - Packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.57.3.vsix`; `bin\antigravity-ide.cmd --install-extension ... --force` reported success; `bin\antigravity-ide.cmd --list-extensions --show-versions` lists `kennyg.forge-agent@0.57.3`.
  - Disk verification: installed `C:\Users\Moshi\.antigravity-ide\extensions\kennyg.forge-agent-0.57.3` contains package version marker, Phase 57 build log marker, and compiled `weakEvalTier2.js` markers for `normalizePlanSubtask` / `Protected workspace oracle`.
  - Remaining goal gap: this proves installability/current bits, not that a live Antigravity panel run has executed an agentic `/goal` under the harness. That is the next acceptance proof for the thread objective.
- Phase 57.4 - Live eval robustness: provider timeouts + partial report flushing - PASS (host compile/smoke + mocked evals + focused timeout proofs) - no git repository / sha unavailable
  - Suggested commit title: "Phase 57.4: live eval robustness - per-call timeouts and partial reports".
  - Built: shared tier eval runner accepts `providerCallTimeoutMs` and wraps provider calls with `TimeoutProvider`; tier-2/3/4 CLIs expose `--call-timeout-ms`; tier-4 live defaults to 90000ms; reports flush after every completed task with `partial`, `completedTaskCount`, `lastUpdatedAt`, and full scheduled `taskCount`, then final overwrite marks `partial: false`.
  - Validation: `npm run compile` PASS; `npm run test` PASS; `node scripts/weak-model-eval-tier3.mjs --architect` PASS with architect 5/5; `node scripts/weak-model-eval-tier4.mjs --architect` PASS with expected pattern-blind 0/4; stalled-provider proof returned in 2322ms with providerFailures 2 and persisted report; two-task write-interception proof captured 3 report writes and confirmed first partial write = `partial:true`, `completedTaskCount:1`, `taskCount:2`, final completed 2.
  - Packaging/install proof: packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.57.4.vsix`; `bin\antigravity-ide.cmd --install-extension ... --force` reported success; `bin\antigravity-ide.cmd --list-extensions --show-versions` lists `kennyg.forge-agent@0.57.4`; installed `C:\Users\Moshi\.antigravity-ide\extensions\kennyg.forge-agent-0.57.4\out\harness\weakEvalTier2.js` contains `TimeoutProvider`, `completedTaskCount`, and `persistTierReport` markers.
  - AAR: SUSTAIN measurement infrastructure as first-class product work; an eval that hangs without a partial artifact destroys the evidence chain. WORK: final reports must distinguish scheduled `taskCount` from completed count; otherwise partial reports can make a stalled suite look smaller rather than incomplete. TOOLS: monkeypatching `fs.writeFileSync` around the compiled runner was the lowest-noise way to prove mid-suite persistence without adding test-only exports.
- Phase 57.5 / 57.5a - Installed Antigravity webview product proof + reviewer-gate hotfix - PASS (installed-webview live proof) - no git repository / sha unavailable
  - Suggested commit title: "Phase 57.5: installed Antigravity proof - live /goal succeeds under harness gates".
  - Product proof route: Playwright/Electron launched `C:\Users\Moshi\AppData\Local\Programs\Antigravity IDE\Antigravity IDE.exe` with installed extension dir `C:\Users\Moshi\.antigravity-ide\extensions`, opened Forge Studio through the command palette, found the installed `kennyg.forge-agent` webview frame, entered `/goal ...`, and captured UI/artifacts. Fresh Antigravity onboarding overlays intercept pointer clicks, so the final proof used DOM activation for the Forge run button; the webview frame and resulting UI state are still from the installed Antigravity app.
  - Bug found first: installed webview + live model fixed `src/math.js` and tests passed, but terminaled `failed` because no diff review was recorded (`diffReviewAttempts: 0`). Root cause: a working provider could skip `get_diff`; the deterministic fallback only forced review after provider failure.
  - Hotfix built: `reviewerGateProposal` runs before provider proposal. Reviewer/verification tasks with green tests force `get_diff`; with review but missing evidence force `record_evidence`; with green evidence plus review force `declare_success`. The terminal diff-review guard was not weakened.
  - Validation before reinstall: `npm run compile` PASS; `npm run test` PASS; focused reviewer-gate proof PASS with providerCalls 0, `diffReviewAttempts: 1`, `reviewerApprovals: 1`, terminal `success`.
  - Packaged/installed `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.57.5.vsix`; Antigravity CLI lists `kennyg.forge-agent@0.57.5`; installed `out\harness\loop.js` contains `reviewerGateProposal`, `green tests require diff inspection`, and terminal declare-gate markers.
  - Final installed-webview proof (git-backed fixture): UI text showed `success · Run success: Declared success after green oracle evidence.`; persisted state `success`; `src/math.js` changed `a - b` -> `a + b`; tests pass; `providerCalls: 3`, `providerFailures: 0`, `fallbackProposals: 0`, `actuallyModelDriven: true`; `preCommitReviews: 3`, `safetyCheckpoints: 3`; `diffReviewAttempts: 1`, `reviewerApprovals: 1`, `reviewerCritiques: 1`; diff review status `approved` and excerpt contains the math change. Screenshot: `F:\DEV_ENV\projects\Agent_Harness_Extension\artifacts\antigravity-live-goal-0575-git-final.png`; state: `F:\DEV_ENV\projects\Agent_Harness_Extension\.tmp\antigravity-live-goal-0575-git\.forge\state.json`.
  - AAR: SUSTAIN installed-product proof before declaring the thread done; it found a completion-gate bug no eval exposed. WORK: fallback policy is not enforcement if the provider is healthy; hard harness gates must preempt model proposals. TOOLS: git-backed fixtures are required when diff-review quality is part of the claim; non-git fixtures can prove success plumbing but make diff review read as `no_changes`.
- Phase 58 - Immutable evaluation evidence - ◐ LOCAL/NEGATIVE/MOCK GATES PASS; fresh live archive pending credential
  - Suggested commit title: "Phase 58: preserve every eval run - latest is a pointer, archives are evidence".
  - Reconciled trigger: the live Qwen Tier-3 5/5 architect result and DeepSeek-planned Tier-4 3/4 result were no longer present as structured reports because later mock regressions overwrote the shared `latest-weak-model-eval-tier<n>.json` files. The console summaries and prose remained, but the machine-verifiable task-level evidence was gone.
  - Built: `Tier2EvalReport` now carries `runId`, `startedAt`, and `archivePath`; the shared Tier-2/3/4 runner allocates one collision-resistant identity before task execution; every partial and final write updates both the backward-compatible latest path and `.forge/evals/runs/tier-<n>/<runId>.json`; all three CLIs print the run ID and archive path. Version bumped to 0.58.0.
  - Validation: failing-first smoke invariant observed; `npm run compile` PASS; `npm run test` PASS; two sequential Tier-2 runs produced distinct archives and left run one byte-identical after latest advanced to run two; delayed two-task Tier-3 proof observed `partial:true`, count 1 and then `partial:false`, count 2 in the same archive; mocked Tier-3 architect remained 5/5 at archive `.forge/evals/runs/tier-3/tier3-mock-20260710T024346287Z-14215338.json`; mocked Tier-4 remained expected pattern-blind 0/4 at `.forge/evals/runs/tier-4/tier4-mock-20260710T024347516Z-a8151f40.json`; `npm run test:visual` PASS; `npm run test:e2e` exited 0 in 101 seconds without the prior unresponsive-host warnings; `git diff --check` found no whitespace errors.
  - Named missing gate: no fresh live Qwen or DeepSeek-planned run was possible because `OPENROUTER_API_KEY` is absent from the current shell. Phase remains ◐ and the thread goal remains active.
  - Review: no prompt, fixture, judge, lane routing, solve accounting, or model behavior changed. `latest` remains compatible for the UI; archives are tier-separated and unique across sequential or parallel runs.
  - AAR: SUSTAIN artifact-first reconciliation before spending more live calls. WORK: a mutable `latest` file is a convenience pointer, never durable experimental evidence. TOOLS: a delayed provider plus polling the real archive gave a stronger partial-write proof than a static source assertion.
  - Delivery: `.vscodeignore` now excludes source-only research, handoff, build-log, plan, test, and repository metadata. Repackaged `forge-agent-0.58.0.vsix` contains 24 functional entries and is 167.16 KB versus the first Phase-58 package's 32 entries and 246.51 KB. Antigravity install succeeded; `--list-extensions --show-versions` reports `kennyg.forge-agent@0.58.0`; installed runner markers confirm archive support.
- Phase 53.1 - Deterministic context budget scheduler - ◐ LOCAL/NEGATIVE/MOCK/INSTALL GATES PASS; live Qwen uplift pending credential
  - Suggested commit title: "Phase 53.1: make compaction real - bound prompts, preserve goals, clear stale outputs".
  - Reconciled trigger: `ContextBundle.compacted` was only a threshold flag and did not alter provider prompts. Its `tokenEstimate` omitted role handoff, tool contract, logs, reflections, and escalations, so Forge claimed compaction without performing or measuring it.
  - Built: new pure `contextBudget.ts` scheduler; 96,000-character product prompt budget; named required/optional sections; required fair-share truncation; priority admission for optional sections; explicit included/cleared/truncated/dropped accounting; prompt-only clearing leaves filesystem artifacts untouched. Main loop persists actual prompt budget/chars/tokens and reason in `ContextBundle`, plus `contextCompactions` and `toolResultSectionsCleared` in `RunStats`. Version bumped to 0.58.1.
  - Failing-first validation: smoke failed on the missing scheduler before implementation. Pure 1,024-character negative proof retained separate goal and open-task markers, truncated both required sections, removed a 5k stale tool result, and returned exactly 1,024 characters.
  - Product-loop proof: disposable failing math fixture with a 140k-character goal used a provider that throws on any prompt above budget. Three model proposal prompts were each exactly 96,000 characters; every prompt retained goal, active task, and open-task markers; the loop read, planned, patched `a - b` to `a + b`, passed tests, completed required review/evidence gates, and terminaled `success`. Persisted `.forge/context-bundle.json` recorded `goal-contract` truncation, six cleared optional sections, 49,041 dropped characters, and the budget reason.
  - Proof caught a real scheduler bug: the first fair-share algorithm marked every required section truncated and wasted unused shares from short sections. Replaced with deterministic water-filling; short required sections now remain complete and unused capacity flows to the oversized section. Both pure and full-loop proofs passed after the fix.
  - Regression/delivery: `npm run compile` PASS; `npm run test` PASS; mocked Tier-3 architect 5/5 at `.forge/evals/runs/tier-3/tier3-mock-20260710T030433035Z-4e5e6d49.json`; mocked Tier-4 expected 0/4 at `.forge/evals/runs/tier-4/tier4-mock-20260710T030434365Z-53c09bd4.json`; `npm run test:e2e` PASS in 96.8s; `npm run test:visual` PASS; package PASS at 25 VSIX entries/169.57 KB. Antigravity install/list/compiled-marker checks PASS for `kennyg.forge-agent@0.58.1`.
  - Named missing gate: no live Qwen 7B context-limited A/B or DeepSeek-planned rerun was possible because `OPENROUTER_API_KEY` is absent. This phase proves deterministic context safety and product completion, not live solve-rate uplift.
  - AAR: SUSTAIN required-state separation and overflow-first tests. WORK: a compaction Boolean is not compaction, and equal shares need redistribution or short sections waste the context window. TOOLS: a provider that throws above the budget made overflow prevention causal rather than observational.
- Phase 59 - Product architect/editor rate split - ◐ LOCAL/CAUSAL/E2E/INSTALL GATES PASS; live DeepSeek/Qwen product run pending
  - Suggested commit title: "Phase 59: port plan-big execute-small into /goal - DeepSeek hands exact context to Qwen".
  - Reconciled trigger: the eval architect lane gave its executor premise, ordered subtasks, and focused full files. Product `/goal` only routed role names; it did not inject `PLAN.md` into Editor context or focus plan-named files. Generic progress completion also allowed Architect/Editor tasks to finish after read-only inspection. Initial webview bindings use `plan`/`code`, while product selection checked only `Architect`/`Editor`, so untouched defaults could route both roles to the coding model.
  - Built: `ArchitectHandoff` state and `.forge/architect-handoff.json`; deterministic exact-existing-path extraction (10k-file scan, six focus files, order preserved, Windows case handling, no basename guessing); premise/ordered-step extraction; Architect guidance requiring `## Premise Checks`, `## Focus Files`, `## Ordered Steps`; required Editor prompt sections for committed plan and each current focus file under Phase 53.1 budgeting; native artifact opening.
  - Built: role-specific completion policy. Explorer completes on search/read; Architect only on `update_plan`; Editor only on `apply_patch|write_file`; Reviewer diff only after green tests. Special `run_tests`/`record_evidence` branches are Reviewer-only so they cannot bypass planning/editing. Product routing honors `Architect|plan|forge.architectModel` and `Editor|code` aliases.
  - Causal product proof: UI-shape bindings only (`plan=deepseek/deepseek-v4-pro`, `code=qwen/qwen-2.5-7b-instruct`). Architect read stayed running; Architect plan completed planning; Editor read stayed running; Editor received exact committed plan plus full `src/math.js`; Editor patch completed editing; tests, diff review, evidence, and terminal success passed. Two Architect calls used DeepSeek, two Editor calls used Qwen; five provider calls total, zero fallback, `actuallyModelDriven:true`. Artifact persisted exact focus, premise, and two ordered steps.
  - Negative focus proof: basename-only `math.js`, lookalike `src/math.js.bak`, and nonexistent paths were rejected; exact `lib/math.js` then `src/math.js` preserved plan order.
  - Regression found/fixed: E2E initially failed reflection A/B because its scripted provider was role-blind and had relied on generic completion (`update_plan` while Explorer). Root fix made the fixture read as Explorer, plan as Architect, and perform wrong/reflected-correct patches as Editor. Focused A/B returned 2/2 vs 0/2; full A/B returned 5/5 vs 0/5 with five honest off-lane halts. Product completion policy was not weakened.
  - Full validation: compile PASS; smoke PASS; E2E PASS in 93.4s with durable product rate-split assertions; visual PASS; reflection A/B +1.00; mocked Tier-3 architect 5/5 archive `.forge/evals/runs/tier-3/tier3-mock-20260710T210716776Z-e5f07b47.json`; mocked Tier-4 expected 0/4 archive `.forge/evals/runs/tier-4/tier4-mock-20260710T210718186Z-5a986c73.json`; package PASS at 25 VSIX entries/171.05 KB. Antigravity install/list/compiled-marker checks PASS for `kennyg.forge-agent@0.59.0`.
  - Named missing gate: no credential-backed installed DeepSeek-planner/Qwen-editor `/goal` run was available. Scripted proof establishes product routing, handoff, focus, and completion mechanics; it does not establish live model solve rate.
  - AAR: SUSTAIN diffing benchmark mechanics against product mechanics, not assuming shared role names mean shared capability. WORK: task completion is part of the agent-computer interface; a read cannot mean “planning finished” or “edit finished.” TOOLS: the extension-host suite caught a legacy fixture whose green result depended on the exact bug being removed.
- Phase 60 - Outbound network intent governance - PASS (local behavioral, extension-host, visual, package) / OS containment still open - no new commit created
  - Suggested commit title: "Phase 60: govern outbound command intent and preserve network evidence".
  - Reconciled trigger: command execution sanitized environment inheritance and captured filesystem effects, but treated read-only downloads and outbound writes identically and persisted no network intent or endpoints.
  - Built: shared pure `classifyCommandNetworkIntent`; explicit read-only curl/wget/PowerShell web requests, package downloads, registry pulls, and git reads are allowed with risk/operation/endpoint evidence; explicit HTTP uploads, `git push`, package publication, remote file transfer/shell, cluster writes, and infrastructure applies are rejected before execution with `[network_intent_blocked]`.
  - Behavioral proof: curl GET classified `read/allowed` with exact endpoint; curl POST classified `write/blocked`; `git push` and `npm publish` rejected; a model-proposed `git push` incremented `networkWriteBlocks` and produced zero command-effect entries; `curl.exe --version` executed without a request and returned `network.detected:true`, `risk:read` metadata.
  - UI proof: existing compact status line now renders `net allowed/blocked`; inspected `artifacts/visual-smoke-run.png` visibly shows `net 1/2` without adding a panel. Proof surface remained intact in `artifacts/visual-smoke-proof.png`. Stale copy-only isolation wording was corrected to worktree with non-git copy fallback.
  - Validation: `npm run compile` PASS; `npm run test` PASS; `npm run test:e2e` PASS in 156.2s; `npm run test:visual` PASS; `git diff --check` found no whitespace errors. Final `npm run package` PASS at 26 VSIX entries/173.14 KB.
  - Delivery: packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.60.0.vsix`; Antigravity CLI install with `--force` succeeded; `--list-extensions --show-versions` reports `kennyg.forge-agent@0.60.0`.
  - Honest boundary: this is deterministic command-intent governance and evidence, not socket interception. An unclassified executable or child process can still open network connections; OS/container sandboxing and socket-level observation remain open.
- Phase 61 - Enforced role worker boundaries - PASS (local behavioral, extension-host, visual, package, Antigravity install) / separate worker processes still open - no new commit created
  - Suggested commit title: "Phase 61: enforce role capabilities and isolate provider sessions".
  - Reconciled trigger: `.forge/role-handoffs.json` advertised allowed tools, but no validator enforced them and all roles reused the run-level provider session ID.
  - Built: typed authoritative role capability matrix checked before generic firewall validation; explicit `[role_capability_blocked]` rejection; `roleCapabilityBlocks`; stable `<run>:worker:<role>` provider sessions; `.forge/worker-contexts.json` with per-role allowed tools, calls, accepted/rejected proposals, recent tools, and current task; native artifact open path; compact `perm` footer counter.
  - Capability policy: Explorer is read/search only; Architect is read/search/plan/task update; Editor is read/search/edit plus diagnostic `run_tests`; Reviewer is command/test/diff/evidence/success without direct file mutation. Editor `run_tests` completes the phase only when green and never bypasses Reviewer diff/evidence/success gates.
  - Behavioral proof: Editor `declare_success` rejected and counted; Reviewer `apply_patch` rejected before `src/guard.txt` changed; Editor/Reviewer provider session IDs are distinct; rejection counts persist in the worker-context artifact. The default provider-unavailable no-op workspace still reaches success through Editor green-test proof followed by Reviewer gates.
  - Regression corrections: network-policy and command-effect tests now assign `run_command` to Reviewer, ensuring the new role boundary does not mask the lower policy layer each fixture is intended to prove.
  - Validation: `npm run compile` PASS; `npm run test` PASS; `npm run test:e2e` PASS in 146.7s; `npm run test:visual` PASS; `git diff --check` found no whitespace errors; final `npm run package` PASS at 26 VSIX entries/174.02 KB.
  - Visual proof: inspected `F:\DEV_ENV\projects\Agent_Harness_Extension\artifacts\visual-smoke-run.png`; footer visibly renders `perm 1` beside existing network and harness counters without adding a panel.
  - Delivery: packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.61.0.vsix`; Antigravity CLI forced install succeeded; extension list reports `kennyg.forge-agent@0.61.0`.
  - Honest boundary: these are enforceable tool and provider-context boundaries, not separate OS processes or worktrees. Roles still share one harness state and workspace; process isolation, worktree-per-role execution, deterministic merge, independent memory, and bounded parallelism remain open.
- Phase 62 - Process-isolated workspace tools - PASS (local behavioral, extension-host, visual, package, Antigravity install) / persistent autonomous workers and container isolation still open - no new commit created
  - Suggested commit title: "Phase 62: execute validated workspace tools in role-tagged child processes".
  - Reconciled trigger: Phase 61 isolated role permissions and provider session identity, but `WorkspaceTools.dispatch` still ran inside the extension-host process with its process and environment boundary.
  - Built: `ProcessWorkerExecutor` forks one short-lived child per validated workspace tool; `workerHost` receives only request ID, workspace root, role, and proposal over IPC; inherited environment is allowlisted; structured result/PID returns to the parent; child exits after one request. Startup error, timeout, early exit, and tool failure become honest failed tool results.
  - Persisted evidence: `.forge/worker-contexts.json` now records `processExecutions`, `processFailures`, `lastWorkerPid`, duration, and blocked environment keys per role. Run stats add `workerProcessExecutions`/`workerProcessFailures`; compact UI footer renders `proc executions/failures`.
  - Behavioral proof: Editor child wrote `src/worker.txt`; worker PID differed from extension-host PID; deliberate `FORGE_WORKER_SECRET` appeared in blocked keys and not allowed keys; descendant `node` command printed `false`; invalid tool returned `success:false` with `Unknown tool` rather than false success.
  - First E2E found an evidence regression: the parent stripped the secret before worker launch, so the inner command sandbox could not list a key it never received. Fix unions upstream worker denied keys into command metadata and carries the parent inherited-key count, preserving transitive evidence without reintroducing the secret. Rerun PASS.
  - Full product-loop E2E terminaled success with five process executions across external Explorer/Editor/Reviewer PIDs; one deliberate failure remained counted. `npm run test:e2e` PASS in 146.6s.
  - Validation: `npm run compile` PASS; `npm run test` PASS; `npm run test:visual` PASS; `git diff --check` found no whitespace errors; `npm run package` PASS at 28 VSIX entries/177.01 KB, including compiled `workerExecutor.js` and `workerHost.js`.
  - Visual proof: inspected `F:\DEV_ENV\projects\Agent_Harness_Extension\artifacts\visual-smoke-run.png`; footer visibly renders `proc 4/0` without adding another panel.
  - Delivery: packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.62.0.vsix`; Antigravity forced install succeeded; extension list reports `kennyg.forge-agent@0.62.0`.
  - Honest boundary: tool execution now has a real process boundary, but proposal generation/orchestration remains in the extension host and roles share one state/workspace. This is not kernel/container sandboxing, worktree-per-role isolation, persistent autonomous workers, deterministic merge, or parallel execution.
- Phase 63 - Deterministic blocker taxonomy - PASS (behavioral, stress, extension-host, visual, package, Antigravity install) - no new commit created
  - Suggested commit title: "Phase 63: classify blockers and drive retries from durable failure state".
  - Reconciled trigger: failures existed as logs/reflections/counters/prose, but no durable object distinguished provider, schema, role, scope, command, network, patch, review, process, tool, oracle, budget, progress, or step-cap blockers.
  - Built: pure deterministic `classifyBlocker`; `.forge/blockers.json`; open/resolved/terminal lifecycle; retryability; role/task ownership; occurrence deduplication; timestamps; summary; suggested action; resolution; native artifact opening. Model output never assigns category or status.
  - Loop wiring: provider/schema/firewall/pre-commit/tool/oracle/budget/no-progress/step-cap paths record blockers; corrected validation/tool/oracle responses resolve matching task blockers; failed/gave-up runs terminalize unresolved blockers; successful runs close goal-level blockers with explicit resolution. Open blockers are required context under the existing prompt budget.
  - AAR wiring: blocker event and terminal-blocker counts enter deterministic triggers; terminal categories enter improve-work and lesson banking. Run stats expose `blockerEvents`, unresolved count, resolved count; compact footer renders `blk unresolved/events`.
  - Behavioral proof: role denial -> `role_capability/retryable`; network mutation -> `network_policy/retryable`; patch drift -> `patch_applicability`; budget cap -> `budget/non-retryable/terminal`; role/network blockers remain open during bounded reflection; native artifact command opens the ledger.
  - Strategic pivot / worker lifecycle root cause: blocker evidence exposed false `worker_process` failures. Failing-first `node scripts/worker-process-stress.mjs 10` proved dispatch returned while PID lived and temp root stayed locked. Fixed delays and child-owned ACK were rejected as timing/lifecycle designs; parent now stores response, disconnects IPC, waits for child exit, then resolves.
  - Stress proof: `node scripts/worker-process-stress.mjs 100` PASS 100/100, every external child exited before dispatch returned. Corrected one invalid test assumption after Windows legally reused an exited PID; uniqueness is not the invariant, confirmed exit is.
  - Regression proof: final `npm run test:e2e` PASS in 130.7s with no unresponsive-host cycle; persisted successful state records workerProcessExecutions > 0, workerProcessFailures 0, blocker history resolved, and unresolved 0. `npm run compile`, `npm run test`, `npm run test:visual`, and `git diff --check` PASS.
  - Visual proof: inspected `F:\DEV_ENV\projects\Agent_Harness_Extension\artifacts\visual-smoke-run.png`; footer visibly renders `blk 1/3` without another panel.
  - Delivery: `npm run package` PASS at 29 VSIX entries/179.75 KB including `blockers.js`; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.63.0.vsix`; Antigravity forced install succeeded and lists `kennyg.forge-agent@0.63.0`.
  - Honest boundary: taxonomy improves diagnosis and retry context but does not yet implement category-specific executable strategy policies beyond deterministic suggested actions and existing reflection/escalation routing.
- Phase 64 - Opt-in hybrid semantic retrieval - PASS (mock causal/cache/fallback, extension-host, visual, package, Antigravity install) / live uplift pending explicit opt-in + credential - no new commit created
  - Suggested commit title: "Phase 64: add cached opt-in semantic retrieval with explicit provenance".
  - Official contract checked before build: OpenRouter documents `POST /api/v1/embeddings`, batched string inputs, `openai/text-embedding-3-small`, cosine comparison, and caching. Source: `https://openrouter.ai/docs/api/reference/embeddings` and create endpoint reference.
  - Privacy boundary: `forge.semanticRetrievalEnabled` defaults false. Repository text is never sent externally without explicit opt-in; `forge.embeddingModel` defaults `openai/text-embedding-3-small`. Disabled/missing-key/API-failure modes retain deterministic retrieval.
  - Built: `EmbeddingProvider`, `OpenRouterEmbeddingProvider`, 32-input batching, normalized vectors, cosine similarity, model/path/content-addressed `.forge/embedding-cache.json`, bounded 80-document scan at 6k chars/document, and hybrid score blend before bounded prompt assembly.
  - Provenance: `.forge/semantic-retrieval.json` records status/provider/model/query/cache hits/embedded document count/candidates/error; retrieval candidates record `source`, `semanticScore`, and visible `semantic cosine`; native artifact opening and run stats (`semanticRefreshes`, failures, cache hits, embedded docs) exist; compact footer renders `sem refreshes/failures`.
  - Causal proof: mock query “repair login flow” and credential-verification source share a vector despite no lexical overlap; hybrid ranking places `src/auth.ts` first and prompt includes semantic provenance. Repeating identical query/docs made zero new embedding calls and reported three cache hits.
  - Failure proof: throwing embedding provider persisted `status:failed`, incremented semantic failure count, retained deterministic candidates, and did not fail the agent step.
  - Retrieval contamination found/fixed: root `.forge` was not excluded by the old descendant-only check and state files repeated goal tokens. The directory is now explicitly excluded; harness-owned root `PLAN.md`, `SCRATCHPAD.md`, `todos.json`, and `evidence_ledger.json` are also excluded because dedicated prompt sections already carry them.
  - Validation: `npm run compile`, `npm run test`, 100/100 worker stress, `npm run test:e2e` (128.5s), `npm run test:visual`, `npm run package`, and `git diff --check` PASS. Visual inspection shows `sem 1/0` in `artifacts/visual-smoke-run.png` without adding a panel.
  - Delivery: package PASS at 30 VSIX entries/183.61 KB including `semanticRetrieval.js`; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.64.0.vsix`; Antigravity forced install succeeded and lists `kennyg.forge-agent@0.64.0`.
  - Honest boundary: mocked mechanics prove ranking, caching, integration, and fallback, not real-model retrieval quality or solve-rate uplift. No live embedding request was made in this phase.
- Phase 65 - Transactional role file edits - PASS (sparse/worktree/conflict, worker stress, extension-host, visual, package, Antigravity install) - no new commit created
  - Suggested commit title: "Phase 65: stage role file edits transactionally and reject concurrent merges".
  - Reconciled trigger: role-tagged child processes still applied validated edits directly to the active workspace, so process isolation did not prevent stale edit proposals from overwriting user changes made between validation and commit.
  - Built: `TransactionalEditExecutor` routes `apply_patch`/`write_file` into a detached temporary Git worktree or non-git sparse staging root; overlays current dirty target bytes; runs the role child there; compares source SHA-256 before merge; copies back only the one validated target if unchanged; removes staging before dispatch resolves.
  - Evidence: `.forge/worker-edit-transactions.json` records role/proposal/target, mode, source/staged hashes, base commit, worker PID, committed/conflict/cleanup result, duration, and error. `forge-agent.openArtifact` opens it natively; run stats and compact footer expose `txn transactions/conflicts`.
  - Negative proof: a fake worker changed the active target after staging; transaction returned `success:false`, `conflict:true`, `committed:false`, and preserved `concurrent-user-change`. Sparse non-git staging committed successfully. Git proof patched dirty bytes unavailable at `HEAD`, committed the staged result, and left `git worktree list --porcelain` byte-identical before/after.
  - Validation so far: `npm run compile` PASS; `npm run test` PASS; `npm run test:workers` PASS 100/100 with every child exited before dispatch resolved; `npm run test:e2e` PASS in 182.4s; `npm run test:visual` PASS. Inspected `F:\DEV_ENV\projects\Agent_Harness_Extension\artifacts\visual-smoke-run.png` visibly shows `txn 1/0` in the existing compact footer. E2E now writes `.tmp/e2e-failure.log` with the exact assertion stack on future suite failures.
  - Delivery: `git diff --check` PASS; final `npm run package` PASS at 31 VSIX entries/186.64 KB; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.65.0.vsix`. Antigravity CLI forced install succeeded and lists `kennyg.forge-agent@0.65.0`; installed `C:\Users\Moshi\.antigravity-ide\extensions\kennyg.forge-agent-0.65.0\out\harness\transactionalEdits.js` contains the worktree-add and source-hash conflict markers.
  - Honest boundary: only explicit one-target file-edit proposals are transactionally staged. Commands still run against the active workspace under policy/effect capture. No persistent autonomous role worktrees, multi-file atomic merge, parallel workers, socket containment, or OS/container sandbox is claimed.
- Phase 66 - Transactional multi-file command execution - PASS (copy/worktree/conflict/rollback, worker stress, extension-host, visual, package, Antigravity install) - no new commit created
  - Suggested commit title: "Phase 66: stage command effects and merge with conflict-safe rollback".
  - Reconciled trigger: `run_command` executed directly in the active root and captured effects only after mutation; process isolation did not prevent partial writes, stale overwrites, or shared Git metadata mutation.
  - Built: `TransactionalCommandExecutor` stages commands in detached Git worktrees or filtered workspace copies; overlays dirty source state; computes created/modified/deleted files; rejects symlinks, explicit source-root paths, more than 200 changed files, or more than 20 MiB; validates every source hash before merge; and restores byte backups if any merge operation fails. Failed commands never merge.
  - Git boundary: firewall now rejects agent-issued Git index/history/config/worktree mutation commands including add/commit/checkout/switch/branch/merge/rebase/tag/config/worktree/gc/update-ref. `git status`, diff, and log-style reads remain available.
  - Evidence/UI: `.forge/worker-command-transactions.json` records staging, changed paths, merge bytes/count, conflicts, rollback, cleanup, PID, and errors; command-effect entries link transaction ID/mode; native artifact opening exists. Compact footer combines counts as `txn edit+command/conflicts`; inspected `artifacts/visual-smoke-run.png` visibly shows `txn 1+1/0` without a new panel.
  - Behavioral proof: non-Git create+modify+delete commits all three paths; nonzero command leaves its staged file unmerged; explicit active-root command is rejected before a worker starts; concurrent source mutation rejects the whole set; injected second-file merge failure restores the first file and reports rollback success; Git transaction consumes dirty source bytes, commits two effects, and leaves worktree registry unchanged.
  - Validation: `npm run compile` PASS; `npm run test` PASS; `npm run test:workers` PASS 100/100; final `npm run test:e2e` PASS in 166.9s without an unresponsive-host cycle; `npm run test:visual` PASS; visual inspected; `git diff --check` PASS.
  - Delivery: final `npm run package` PASS at 32 VSIX entries/191.23 KB including compiled `transactionalCommands.js`; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.66.0.vsix`. Antigravity forced install succeeded, lists `kennyg.forge-agent@0.66.0`, and its installed runtime contains both transaction modules.
  - Honest boundary: staging governs normal cwd-relative effects and explicit active-root references, not arbitrary absolute OS paths, process exploits, sockets, or kernel facilities. Dependency/build directories are intentionally excluded from staging. Persistent autonomous role worktrees, parallelism, socket containment, and OS/container sandboxing remain open.
- Phase 67 - Verified procedural skill memory - PASS (pure causal, real AAR, prompt rehydration, extension-host, visual, package, Antigravity install) - no new commit created
  - Suggested commit title: "Phase 67: bank and retrieve verified procedural recovery skills".
  - Reconciled trigger: `skills: []` and `.forge/skill-registry.json` were presence-only. AAR lessons described failures but supplied no bounded executable workflow to later runs.
  - Built: deterministic templates for proposal repair, oracle recovery, reviewer recovery, and escalation; success-only banking; stable category/language IDs; verified-run/session provenance; confidence and occurrence strengthening; token/blocker retrieval; max-three bounded prompt injection; session-deduplicated use accounting. Models cannot author or approve skills.
  - Negative proof: failed run banks zero; clean success invents none; unrelated CSS goal retrieves none; generic firewall reflection cannot teach an oracle skill. Positive proof: repeated verified TypeScript recovery strengthens one ID; matching later run receives the exact SEARCH-block workflow and persists last-use provenance.
  - Product proof: real reflection A/B fixture's reflection-on terminal success persists `skillsBanked` and a provenance-bearing registry; reflection-off failed fixture persists an empty registry. Native `skills` artifact opening passes. Compact footer renders `skill applications/retrievals`; inspected `artifacts/visual-smoke-run.png` visibly shows `skill 1/2` without adding a panel.
  - Validation: final post-causal-label `npm run compile`, `npm run test`, and `npm run test:e2e` PASS; `npm run test:workers` PASS 100/100; `npm run test:visual` PASS and screenshot inspected; `git diff --check` PASS.
  - Delivery: `npm run package` PASS at 33 VSIX entries/195.02 KB including compiled `proceduralSkills.js`; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.67.0.vsix`. Antigravity forced install succeeded, lists `kennyg.forge-agent@0.67.0`, and the installed runtime contains the procedural-skill module.
  - Honest boundary: this proves deterministic skill lifecycle and causal prompt delivery, not weak-model solve-rate uplift. No model writes skill state. Cross-repository sharing, independent worker memories, and live/held-out skill A/B remain open.
- Phase 68 - Enforced universal task workflow - PASS (lane/baseline/lifecycle/bypass, extension-host, visual, package, Antigravity install) - no new commit created
  - Suggested commit title: "Phase 68: enforce universal task workflow gates in every agent run".
  - Reconciled trigger: the user workflow was prose while Forge had only a five-task graph. Role-valid actions could skip baseline/reconcile/plan state, and terminal artifacts did not prove the complete universal lifecycle.
  - Built: deterministic Full/Light classifier; baseline capture; structured acceptance contract; 11-stage ledger; predecessor mutation/evidence/success gates; weak-plan augmentation; workflow blocker category; required prompt section; `.forge/workflow-governance.json`; `.forge/workflow-task-record.md`; native artifact commands; compact `flow stage/blocks` telemetry.
  - Negative proof: ambiguous goals default Full; “fix README typo” is Light; manually completing tasks cannot bypass planning; early write leaves source bytes unchanged and records a workflow violation; early close evidence and premature success reject; red/no-evidence states cannot become `VERIFIED`.
  - Positive proof: accepted exploration/plan/edit/oracle/diff/evidence events advance exact stages; automatic post-edit oracle and harness-authored green evidence satisfy their real existing gates; no-op green test can satisfy implementation; terminal AAR closes all 11 stages. Persisted product rate-split fixture is `success`, workflow `VERIFIED`, current `complete`, 11 stages completed, zero violations.
  - Compatibility corrections: layered network/precommit/command tests now explicitly satisfy workflow planning before testing lower policy layers. Validation exposed and fixed two real integration mismatches: automatic post-edit oracles and harness-authored evidence were authoritative but initially unrecognized by workflow state.
  - Validation: final 0.68.0 `npm run compile`, `npm run test`, and `npm run test:e2e` PASS (combined gate 235.7s; Electron briefly reported then recovered from an unresponsive-host cycle); `npm run test:workers` PASS 100/100; `npm run test:visual` PASS; `git diff --check` PASS. Screenshot inspection shows `flow review/0` with no added panel.
  - Delivery: `npm run package` PASS at 35 VSIX entries/207.39 KB including compiled `workflowGovernance.js` and `UNIVERSAL_AI_TASK_WORKFLOW.md`; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.68.0.vsix`. Antigravity forced install succeeded, lists `kennyg.forge-agent@0.68.0`, and installed-marker inspection found workflow bypass and terminal-completion logic.
  - Honest boundary: core governance is machine-enforced, not model-authored. Typed project-adapter registries, OS/socket containment, persistent autonomous role worktrees, and live weak-model workflow uplift remain open.
- Phase 69 - Structured uncertainty ask gates - PASS (schema/pause/persistence/answer/resume/negative mutation/extension-host/visual) - no new commit created
  - Suggested commit title: "Phase 69: pause ambiguous runs for structured user clarification".
  - Built: typed `ask_user`; optional confidence/material-uncertainty envelope; deterministic rejection of non-question proposals under material uncertainty; `awaiting_input`; three-question cap and duplicate rejection; durable `.forge/clarifications.json`; authoritative answer injection; native command/API; composer answer-and-resume flow; compact `ask answers/requests` telemetry.
  - Policy: ask only when user-owned intent, scope, authorization, cost, behavior, or acceptance could change the work. Workspace-discoverable facts must use read/search tools. Pending clarification prevents provider calls and all mutation.
  - Proof: an uncertain `write_file` was blocked before commit; a valid question persisted and paused; an extra run step made zero provider calls; answering persisted the exact response and returned the same run to idle. `npm run compile`, `npm run test`, and `npm run test:e2e` PASS (175.9s); `npm run test:workers` PASS 100/100; `npm run test:visual` PASS. Inspected `artifacts/visual-smoke-run.png` visibly shows the question/options/recommendation above the bottom composer, `awaiting_input`, and `ask 0/1`.
  - Delivery: `git diff --check` PASS; `npm run package` PASS at 35 files/209.74 KB; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.69.0.vsix`. Antigravity forced install succeeded and lists `kennyg.forge-agent@0.69.0`; installed runtime marker inspection found `awaiting_input`, `[clarification_required]`, and `clarifications.json` persistence logic.
- Phase 70 - Typed project adapters and composite build oracle - PASS (detection/composite truth/negative evidence/extension-host/visual/package/Antigravity install) - no new commit created
  - Suggested commit title: "Phase 70: require typed project adapter composite verification".
  - Reconciled defect: adapter selection was implicit; `lastOraclePass` mirrored tests alone, so green tests could mask red lint/typecheck, and build had no independent status.
  - Built: deterministic Node/Python/Rust/Go/unknown registry; lockfile-aware Node package manager selection; exact command/source/required/skipped contracts; stable fingerprint; `.forge/project-adapter.json`; required prompt injection; native artifact access; distinct build status; all-required composite green; composite evidence details. Full oracles now run only after edits/commands/explicit tests, not reads or planning.
  - Negative proof: passing tests plus failing build leaves `tests=pass`, `build=fail`, `lastOraclePass=false`, and zero green ledger entries. Missing test contract remains hard red. Verification matrix expanded 9→11 with build and composite-typecheck failures.
  - Detection proof: Node/pnpm, Python/Ruff/Mypy, Rust/Cargo, Go, and unknown contracts pass host assertions. One initial Python fixture accidentally contained the shared helper's package.json and correctly selected Node; the fixture was repaired to a truly empty root. A stale compiled-test rerun was also identified; host tests require compile first.
  - Validation: `npm run compile`, `npm run test`, `npm run test:workers` PASS 100/100, `npm run test:e2e` PASS in 102.7s (one recovered unresponsive-host cycle), `npm run test:visual` PASS, screenshot inspected, and `git diff --check` PASS with only Windows line-ending warnings.
  - Delivery: `npm run package` PASS at 36 files/212.5 KB; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.70.0.vsix`. Antigravity forced install succeeded and lists `kennyg.forge-agent@0.70.0`; installed runtime contains `projectAdapters.js` and required composite `runAll` logic.
  - Honest boundary: adapters are deterministic root-level contracts. No monorepo package graph, custom user adapter plugin, toolchain installation, framework-specific test generation, or automated missing-oracle remediation is claimed.
- Phase 71 - Typed oracle failure remediation capsules - PASS (five causal lanes/duplicate folding/green resolution/extension-host/visual/package/Antigravity install) - no new commit created
  - Suggested commit title: "Phase 71: persist typed oracle remediation capsules".
  - Reconciled gap: red composite results had no durable per-kind signature, occurrence lifecycle, category-specific repair contract, or resolution evidence; retries received generic raw output.
  - Built: deterministic test/lint/typecheck/build/missing-test categories; normalized SHA-256 failure signatures; bounded command/output/source evidence; task/role ownership; fixed anti-cheating remediation guidance; duplicate folding; green-only lifecycle resolution; required prompt section; `.forge/oracle-failures.json`; native artifact access; `fix captured/repeated/resolved` telemetry.
  - Causal proof: five scripted weak providers refuse to act without the exact typed guidance in the real product-loop prompt, then each repairs its disposable workspace via validated `write_file` and reaches composite green. Build repeats red twice first: one capsule, occurrence 2, then resolved. Missing-test creates a repository-native test script before green. No lane weakens or bypasses its oracle.
  - Validation: final `npm run compile`, `npm run test`, `npm run test:workers` PASS 100/100, `npm run test:e2e` PASS in the 157.3s combined gate, `npm run test:visual` PASS, screenshot inspected, `git diff --check` PASS with Windows line-ending warnings only.
  - Delivery: `npm run package` PASS at 37 files/215.26 KB; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.71.0.vsix`. Antigravity forced install succeeds and lists `kennyg.forge-agent@0.71.0`; installed runtime inspection confirms occurrence and resolution code.
  - Honest boundary: this proves deterministic diagnosis, prompt delivery, and scripted causal recovery. It does not prove live weak-model uplift, arbitrary framework test synthesis, or that a model cannot choose a bad source fix while staying inside the firewall.
- Phase 72 - Signature-based oracle stagnation control - PASS (unchanged/changed/red-red-green/extension-host/visual/package/Antigravity install) - no new commit created
  - Suggested commit title: "Phase 72: halt unchanged oracle loops as honest no progress".
  - Reconciled defect: repeated identical red oracles looked like progress because logs/reflections grew; failed `run_tests` also entered generic tool-failure handling before the red-oracle branch.
  - Built: threshold 3 over stable open-capsule signatures at the shared post-oracle boundary; second occurrence remains eligible for escalation; third records `no_progress`, increments `oracleStagnationHalts`, preserves evidence, and returns `gave_up` before call four. Distinct signatures remain independent.
  - Causal proof: unchanged fixture = exactly 3 calls, occurrence 3, `gave_up`, no green evidence; changing diagnostic = two occurrence-1 capsules, no stagnation; constant-red/changed-source/third-green = occurrence 2 then resolved, no halt. Initial test caught the tool-failure ordering bug; moving ownership earlier made the proof pass.
  - Validation: final combined gate 170.8s: `npm run compile`, `npm run test`, `npm run test:workers` PASS 100/100, `npm run test:e2e`, `npm run test:visual`, and `git diff --check` PASS. Inspected screenshot visibly narrates the honest stop and shows `fix 1/2/0 · stuck 1` while build remains red.
  - Delivery: `npm run package` PASS at 37 files/215.55 KB; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.72.0.vsix`. Antigravity forced install succeeds and lists `kennyg.forge-agent@0.72.0`; installed runtime marker inspection confirms threshold 3 and stagnation halt code.
  - Honest boundary: signature identity is deterministic textual identity after normalization, not semantic root-cause equivalence. Changed diagnostics can still represent lateral thrashing and remain governed by the broader step/budget caps.
- Phase 73 - User-facing checkpoint history and safe restore - PASS (full snapshot/ID rejection/proof invalidation/fresh verification/UI/package/Antigravity install) - no new commit created
  - Suggested commit title: "Phase 73: add Kilo-style checkpoint history with safe proof-invalidating restore".
  - Kilo parity target: visible incremental checkpoint history with real return-to-step behavior, built over Forge's stronger transaction/evidence system.
  - Built: full workspace product checkpoints; targeted low-level mode retained; clone-on-write snapshot hints; dependency/build/cache exclusions; 20-entry retention; state-owned ID validation; full-byte restore; post-checkpoint file deletion; evidence/review/oracle/AAR/workflow invalidation; task reopening; `.forge/checkpoint-restores.json`; command and webview bridge; compact history popover; native Diff; two-click Restore/Confirm; `back success/failure` telemetry.
  - Causal proof: two changed files restored, one later-created file removed; forged ID rejected pre-access; missing manifest records failure; stale green evidence, approved diff, oracle state, success/AAR, and downstream workflow completion all cleared; fresh verification reacquires composite green only after restore. Direct targeted checkpoint regression remains green; product checkpoints are all full snapshots.
  - UX proof: visual smoke opens the composer history, selects Restore once, and captures the red Confirm state. Inspected screenshot shows action/step, workspace snapshot scope, timestamp, native Diff, confirmation, and explicit fresh-review warning without a permanent panel.
  - Validation: final post-bounding `npm run compile`, `npm run test`, `npm run test:workers` PASS 100/100, `npm run test:e2e`, `npm run test:visual`, and `git diff --check` PASS; release gate 182.7s.
  - Delivery: `npm run package` PASS at 37 files/217.7 KB; packaged `F:\DEV_ENV\projects\Agent_Harness_Extension\forge-agent-0.73.0.vsix`. Antigravity forced install succeeds and lists `kennyg.forge-agent@0.73.0`; installed marker inspection confirms restore invalidation, retention 20, and clone-on-write copy paths.
  - Honest boundary: snapshots intentionally exclude dependencies, build outputs, caches, Git metadata, Forge artifacts, and visual artifacts. This is workspace return-to-step, not Git history or branch management. Snapshot cost is bounded by retention but still scales with non-excluded workspace size.
# Phase 74 - Agent Browser Validation Tool (PASS)

- Reconciled that `scripts/visual-smoke.mjs` proves Forge's webview only; it is not an agent tool and is excluded from the installed VSIX.
- Selected installed-runtime design: packaged `playwright-core`, detected host Edge/Chrome, loopback-only URL policy, bounded screenshot and diagnostic evidence under `.forge/browser-runs`.
- Implemented typed `browser_validate` for Reviewer/Escalation with loopback-only URL firewall, bounded timeout/output, detected host Edge/Chrome, persisted JSON/PNG diagnostics, distinct evidence accounting, and no authority to satisfy the composite code oracle.
- Added `forge-agent.runBrowserValidation`, native report/PNG artifact opening, a compact composer globe, and `web pass/fail` status counts. No permanent browser surface was added.
- Causal `npm run test:browser` PASS: real rendered page, expected-text miss, console error, unreachable target, forbidden URL, and scripted model proposal through `AgentHarnessLoop`; browser-only proof retained `lastOraclePass=false` and could not terminal success.
- Final release gate PASS in 229.5s: `compile`, `test`, `test:browser`, `test:workers` 100/100, `test:e2e`, `test:visual`, and `git diff --check`.
- Visual inspection PASS: real Edge-captured PNG shows `Visible Forge application` plus `Run task`; `artifacts/visual-smoke-run.png` shows one green globe and `web 1/0`, with no new panel.
- Packaging/install PASS: 144 files/3 MB; VSIX contains `node_modules/playwright-core/package.json` and no browser binary. Antigravity forced install succeeds and lists `kennyg.forge-agent@0.74.0`; installed runtime resolves the runner, accepts loopback, and rejects remote hosts.
# Phase 75 - Trustworthy Streaming Run Progress (PASS)

- Reconciled current behavior: `runStep` sends no webview message until provider, validation, worker/tool, and oracle work has fully returned; users see an unexplained busy interval.
- Selected contract: harness-authored typed events, monotonic per-session ordering, bounded state plus JSONL persistence, immediate extension bridge, and one compact in-chat activity block. Unvalidated token/prose streaming is explicitly excluded.
- Implemented typed ordered events for run/step/provider/proposal/validation/tool/oracle/reflection/ask/pause/resume/terminal transitions. State retains 300 events; JSON snapshot and append-only JSONL persist reload evidence; observer failures cannot alter the run.
- Extension host forwards `run-progress` immediately. Chat deduplicates live/reloaded IDs and renders one bounded `aria-live` activity group with status, role, tool/phase, and latest detail.
- `npm run test:progress` PASS: delayed provider emitted before step resolution; accepted sequence ordered correctly; green oracle, rejected remote browser plus reflection/no-tool, ask gate, unique IDs, and monotonic persistence all proved.
- Full release gate PASS in 223.1s: compile, static, progress, browser, workers 100/100, extension-host, visual, package, and diff checks.
- Visual inspection found `scrollIntoView` moving the entire webview. Replaced it with chat-local `scrollTop`; corrected post-fix compile/static/visual/package/diff/install gate PASS in 35.9s.
- Corrected screenshot visibly shows all eight activity rows, red oracle, terminal `gave_up`, checkpoint controls, and bottom composer without clipping.
- Packaging/install PASS: 144 files/3.01 MB; Antigravity lists `kennyg.forge-agent@0.75.0`; installed JSONL/provider/listener/webview markers are present.
# Phase 76 - First-Run Onboarding and Provider Readiness (PASS)

- Reconciled a security/product contradiction: `forge.openRouterApiKey` is a public plaintext setting despite secret-storage copy, and catalog errors silently collapse to fallback models with no readiness explanation.
- Selected contract: migrate/clear legacy plaintext, store only in `ExtensionContext.secrets`, typed no-spend provider/catalog probes, sanitized blocker codes, explicit catalog provenance, and one compact workspace/key/verify onboarding flow.
- Removed the public `forge.openRouterApiKey` setting. Activation migrates legacy global/workspace/folder values into `ExtensionContext.secrets`, clears every plaintext scope, and supplies only an in-memory provider key. Commands/webview never receive stored bytes.
- Added typed no-spend OpenRouter/local readiness: workspace, credential source/validity, authentication latency, live/fallback/error catalog provenance/count, sanitized blockers, and ready boolean. Initial probes coalesce for 30 seconds and invalidate on key changes.
- Added one compact two-step first-run card, empty password input, save/check/change/retry controls, host-side unready run guard, explicit model-catalog provenance, and a metadata-only Settings readiness card.
- `test:readiness` PASS for valid/invalid/network/missing/local/migration/redaction cases. Extension-host secret-vault save/later-read/clear PASS with no returned or workspace-persisted bytes. Repository leak scan PASS.
- Full release gate PASS in 242.4s: compile, static, readiness, progress, browser, workers 100/100, extension-host, visual, package, and diff checks.
- Visual inspection PASS: onboarding is one centered card with green workspace and empty password field; ready state returns to normal activity/chat/composer. No setup panel remains after readiness.
- Packaging/install PASS: 145 files/3.01 MB; Antigravity lists `kennyg.forge-agent@0.76.0`; installed manifest has no plaintext key property and runtime markers confirm secret/migration/readiness behavior. User-key migration occurs on next extension activation, not at CLI install time.
# Phase 77 - Trusted Built-In and Custom Modes - PASS

- Reconciled current role menu as cosmetic: role text is inserted client-side, but host and harness do not resolve/enforce a mode and Run always starts the full agent.
- Selected contract: immutable built-ins plus validated global-state customs, host-only mode resolution, advisory chat routing, code-mode allowlist intersection below existing role ceilings, and mandatory proof tools.
- Built host-owned immutable built-ins plus bounded persisted custom modes, advisory/code intent routing, role-ceiling tool intersection, required proof tools, and host-state-only manual stepping.
- `npm run test:modes` PASS: nine built-ins, custom persistence, Reviewer command removal with retained green oracle, and blocked cross-role Editor grant. Extension-host tamper/advisory/unknown-mode tests and visual mode/settings smoke pass.
- `forge-agent-0.77.0.vsix` is 3,160,914 bytes; forced Antigravity install succeeded and `--list-extensions --show-versions` reported `kennyg.forge-agent@0.77.0`.

# Phase 78 - Privacy-Minimized Feedback and Support - PASS

- Reconciled Kilo's feedback entrypoint against Forge's compact UX and stronger privacy boundary. Selected one global header Help action, native artifacts/editor/clipboard, explicit GitHub handoff, and no permanent support panel.
- Added host command `forge-agent.reportProblem`, `.forge/support/latest-support-report.json|md`, source/prompt/chat/credential/path exclusion by construction, and a deterministic no-UI command mode for tests.
- `npm run test:support`, static invariants, compile, and visual smoke pass. The dedicated leak fixture proves a submitted OpenRouter-shaped secret, private goal, source text, blocker message, and absolute fixture path do not serialize. Inspected `artifacts/visual-smoke-support.png`: Help is one global icon beside Settings and composer density is unchanged.
- Extension-host E2E PASS in 150.4s through the real registered command, including native artifact persistence and secret-vault non-disclosure.
- Release gate PASS in 116.4s: compile, static, modes, support, readiness, progress, browser, workers 100/100, visual, package, and diff. Final no-workspace global-storage correction passed static/support/visual/package/diff in 31.2s.
- Delivery PASS: `forge-agent-0.78.0.vsix`, 147 files, 3,164,227 bytes. Forced Antigravity install reports `kennyg.forge-agent@0.78.0`; installed package/runtime/webview markers confirm command registration, support generator, Help label, and `fallbackRoot` behavior.

# Phase 79 - Durable Session Memory and Frictionless Resume - PASS

- Reconciled dormant session persistence against actual UX: the webview never lists sessions or saves chat, snapshot load does not rehydrate `latestState`, raw session IDs reach path joins, and one corrupt index can hide healthy history.
- Selected contract: host-owned validated store, index reconstruction, bounded chat, compact header popover, explicit Open/Resume, pin, two-step delete, active-session protection, and no permanent history panel.
- Built validated `SessionStore`, run/chat-only session identities, active-session pointer, bounded schema-reduced chat autosave, host rehydration, explicit bounded Resume, pin, active-safe two-step delete, corrupt-index reconstruction, and compact header popover.
- Causal store proof PASS: three healthy sessions survive one corrupt sibling/index; traversal, identity mismatch, missing-path disclosure, and active delete reject; clarification/terminal sessions cannot auto-resume; chat-only memory persists without run state.
- Extension-host E2E PASS in 143.7s: real list/open/resume/delete commands, host authority replacement, terminal non-resurrection, chat-only stale-authority clearing, and interrupted-run rehydration retaining cost/evidence with bounded additional steps.
- Visual inspection PASS at 1440px and 520px. `visual-smoke-sessions-sidebar.png` exposed composer icon/chip overlap; responsive stacking fixed it and the recaptured image is clean. Open and Resume are explicit; corruption is summarized; no permanent panel exists.
- Release matrix PASS in 130.8s: compile/static/sessions/modes/support/readiness/progress/browser/workers 100/100/visual/package/diff. `forge-agent-0.79.0.vsix`: 148 files, 3,169,222 bytes. Forced Antigravity install reports `kennyg.forge-agent@0.79.0`; installed runtime markers confirm commands/store/chat/UI.

# Phase 80 - Installed Difficult Live Weak-Model Proof (in progress)

- Reconciled decisive evidence gap: installed Proof UX can only run `live:false`; current shell has no key; historical Tier-4 live results predate immutable archives.
- Current anonymous OpenRouter catalog confirms `qwen/qwen-2.5-7b-instruct` is served at 131,072 context with advertised prompt/completion prices approximately $0.04/$0.10 per million tokens. It is the sole initial approved weak slug.
- Selected contract: extension-secret execution, explicit spend consent, four symptom-only Tier-4 tasks, equal bare/harness inputs, held-out immutable judges, bounded calls/steps/timeouts, partial progress, immutable wrapper report, and honest no-uplift outcome.
- Built approved-model/price/consent/bounds preflight, Tier-4 live runner, model-driven/fallback-zero wrapper, immutable archive, extension command, partial progress polling, native report access, and collapsed spend-aware Proof UX.
- Offline causal proof PASS: uplift classification requires two model-driven green solves; fallback is zero; duplicate archive write rejects. Frontier model and missing-consent extension commands reject before readiness/provider work.
- Visual PASS: control screenshot plus result-focused screenshot. Honest visual result is `model_capability_without_uplift`, fallback 0, capability NOT PASSED.
- Release PASS: E2E 157.5s (transient unresponsive/responsive test-host cycle, exit 0); remaining matrix 142.4s; package 149 files/3,173,610 bytes. Antigravity forced install reports `kennyg.forge-agent@0.80.0`; installed runtime/command/UI markers pass.
- Installed preflight PASS: Antigravity reports OpenRouter ready with 345 models. `artifacts/installed-difficult-proof-preflight.jpg` visibly shows Qwen 2.5 7B, 4/4 tasks, 10-step/90s bounds, unchecked consent, and disabled Run. Paid live execution remains pending explicit action-time confirmation; no live result is claimed.
# Phase 81 - Host-Owned Human Approval Gate - PASS

- Reconciled defect: the composer auto-approve shield is webview-local and has no effect on harness authority.
- Design and implementation plans: `docs/plans/2026-07-13-human-approval-gate-design.md` and `docs/plans/2026-07-13-human-approval-gate-implementation.md`.
- Selected boundary: ask after deterministic validation/pre-commit review and before checkpoint/COMMIT for file writes, patches, and commands; auto skips only this human pause.
- Built: persisted `ask` / `auto` policy; exact proposal/digest/task/session approval records; `awaiting_approval`; approval and rejection commands; `.forge/human-approvals.json`; compact in-chat decision card; native configuration; installed shield state. Only `write_file`, `apply_patch`, and `run_command` pause. Auto mode bypasses only the human prompt.
- Causal proof PASS: ask paused with unchanged file, zero checkpoint, and zero worker execution; forged ID rejected; approval committed the exact persisted write with one total provider call; replay rejected; rejection remained non-mutating and became scratchpad guidance; tampered proposal failed digest validation; auto mode still rejected a path escape.
- Extension-host E2E PASS in 192.1s after correcting an over-specific safe-rejection assertion. Native policy persisted; forged approval could not manufacture authority. Existing heavy host fixtures briefly reported unresponsive/responsive and exited 0.
- Visual PASS after two QA corrections. The first fixture contradicted itself (`gave_up` plus pending action) and was made coherent. The first narrow capture hid the card after resize; pending-state resize scrolling was fixed. Inspected `artifacts/visual-smoke-human-approval.png` and `artifacts/visual-smoke-human-approval-sidebar.png`: card and bottom composer remain visible at 1440px/520px with no overlap.
- Release matrix PASS in 72s: compile, static, approvals, modes, sessions, support, readiness, progress, real Edge browser validation, difficult-proof offline contract, workers 100/100, visual, and diff check. `git diff --check` emitted line-ending warnings only.
- Delivery PASS: `forge-agent-0.81.0.vsix`, 149 packaged files, 3,176,094 bytes. Forced Antigravity install succeeded and lists `kennyg.forge-agent@0.81.0`; installed runtime/webview markers include `awaiting_approval`, digest validation, `human-approvals.json`, host resolution, and the compact card.
- Installed visual PASS after Antigravity reload: accessibility exposes `Ask before file changes and commands. Click to auto approve validated actions.` and OpenRouter ready with 345 models. Screenshot: `artifacts/installed-human-approval-081.jpg`.
- Honest boundary: approval is per consequential proposal, not a granular path/command allowlist or session-scoped "always allow this category" policy. `run_tests`, planning, evidence, reads, and browser validation do not prompt. Paid Phase-80 difficult-model execution remains separately unrun.
- Suggested commit title: `Phase 81: make human approval a real pre-commit boundary`.
# Phase 82 - Deterministic Workspace Index and Real Composer Control - PASS

- Reconciled defect: database icon is local-only chrome; retrieval artifact is task-specific top-ten candidates; search silently caps at 250 files.
- Plans: `docs/plans/2026-07-13-workspace-index-design.md` and `docs/plans/2026-07-13-workspace-index-implementation.md`.
- Selected boundary: 5,000 contained text/source files, no symlinks/source bodies, explicit exclusions/truncation/staleness, search-tool consumption, compact Refresh/Open popover.
- Built: `WorkspaceIndexService`, short-lived sanitized index worker, 5,000-file/2 MB-per-file bounds, dependency/build/cache/Git/Forge/artifact exclusions, no symlinks or binary files, workspace-bound artifact validation, normalized symbol metadata without source bodies, stale marker lifecycle, indexed `repo_search`/`symbol_search` with coverage provenance, native commands/watcher, and compact status/Refresh/Open popover.
- First causal proof correctly failed because one-line declaration excerpts leaked a source-body sentinel. Storage was tightened to normalized `kind name` metadata. Final `npm run test:index` PASS: 321 files/321 symbols, target beyond legacy 250 cap found, source body absent, dependency/build/binary/junction excluded, cross-workspace/corrupt artifacts rejected, post-build junction pivot invalidated, and stale/rebuild lifecycle passed.
- Extension-host E2E PASS in 172.5s: registered build/status/open commands, out-of-host child build, native artifact open, real file event changed ready -> stale, rebuild returned ready, and watcher/child cleanup allowed host exit 0. One existing heavy-fixture unresponsive/responsive cycle recovered.
- Visual PASS: inspected `artifacts/visual-smoke-workspace-index.png` and `artifacts/visual-smoke-workspace-index-sidebar.png`; ready counts and Refresh/Open remain legible and contained at desktop/520px, with no permanent panel.
- Release matrix PASS in 68.6s: compile, static, index, approval, modes, sessions, support, readiness, progress, real Edge browser validation, difficult-proof offline contract, workers 100/100, visual, and diff check.
- Delivery PASS: `forge-agent-0.82.0.vsix`, 151 files, 3,182,659 bytes. Antigravity forced install lists `kennyg.forge-agent@0.82.0`; installed hashes/markers confirm the builder, watcher, UI, service, and 5,000-file cap.
- Installed product proof PASS in a fresh real Antigravity window. The existing window retained its old webview despite Reload Webviews/Restart Extension Host; a new window selected registered `0.82.0`. Installed popover moved MISSING -> READY after Refresh and reported 276 files, 2,087 symbols, 824 ignored, not truncated; Open Index enabled. Screenshot: `artifacts/installed-workspace-index-082.jpg`.
- Honest boundary: index contains path/stat/language/declaration identity, not semantic embeddings or source bodies. `repo_search` still reads current source under a 64 MB bound. File count is capped at 5,000 and visibly marks truncation. Antigravity hot-installed webviews may require a new IDE window.
- Suggested commit title: `Phase 82: add a real bounded workspace index`.
# Phase 83 - Host-Owned Composer Context - PASS

- Reconciled defect: Forge could search only after a run started; the composer could not attach the active editor, selection, diagnostics, or chosen files. Users had to manually describe paths, and chat/run requests carried only text.
- Plans: `docs/plans/2026-07-13-composer-context-design.md` and `docs/plans/2026-07-13-composer-context-implementation.md`.
- Selected boundary: host-owned snapshots for contained files, active selections, and workspace diagnostics; 12 attachments, 64 KiB each, 192 KiB total, no symlinks/binaries/traversal; metadata-only webview messages; native Quick Pick; one temporary popover and removable chips.
- Built: deterministic `ComposerContextService`; atomic per-session `context.json`; session reload; active editor/selection capture; native workspace file selection; diagnostic capture; add/remove/clear bridge; command API; metadata-only webview boundary; chat system-context injection; required budgeted `/goal` prompt section; compact Paperclip control and chips.
- First static gate correctly failed because `openSession` returned stored source snapshots to the webview even though normal updates were metadata-only. The host response now reduces reloaded context to summaries before posting; React no longer strips content it should never receive.
- `npm run test:context` PASS: valid file/selection/diagnostics, outside/binary/oversized/empty negatives, diagnostics filtering, count/byte bounds, tampered persistence rejection, metadata-only summaries, session reload, and exact required prompt injection.
- Extension-host E2E PASS in 166.1s: three context commands registered; host attachment created a durable conversation, returned metadata only, survived native session reopen, cleared cleanly, and the full existing host suite exited 0.
- Visual PASS: inspected `artifacts/visual-smoke-composer-context.png` and `artifacts/visual-smoke-composer-context-sidebar.png`. Chips remain one-line/removable, the temporary action menu stays contained, and the bottom composer/Run controls remain visible at 1440px and 520px.
- Release matrix PASS in 141.4s: compile, static, context, index, approvals, modes, sessions, support, readiness, ordered progress, real Edge browser validation, difficult-proof offline contract, workers 100/100, visual, and diff check. Line-ending warnings only.
- Delivery PASS: `forge-agent-0.83.0.vsix`, 152 packaged files, 3,188,086 bytes. Forced install succeeded and Antigravity lists `kennyg.forge-agent@0.83.0`.
- Installed product proof PASS after `Developer: Reload Window` replaced the stale 0.82 webview. Accessibility exposed `Attach workspace context`, `Active file or selection`, `Workspace file...`, and `Problems and diagnostics`. Clicking the active-file action captured the native open `Forge-agent-harness/server.ts`, rendered a removable `server.ts` chip, and changed the control to `Attach workspace context (1)`. Screenshot: `artifacts/installed-composer-context-083.jpg`.
- Honest boundary: snapshots are explicit and bounded, not automatic semantic context. File attachments over 64 KiB require a focused selection. The file picker searches at most 2,000 native workspace results. Removing context affects future prompts, not model messages already sent. Hot-installed Antigravity webviews may require `Developer: Reload Window`. Paid Phase-80 difficult weak-model proof remains unrun pending explicit consent.
- Suggested commit title: `Phase 83: add host-owned composer context attachments`.
# Phase 84 - Indexed @File And @Folder Mentions - PASS

- Reconciled explicit research gap: `RESEARCH_IMPLEMENTATION_GAP_ANALYSIS.md` called for `@file / @folder` composer mentions. Phase 83 required a Paperclip action and native picker, leaving inline task writing behind Kilo-style agents.
- Plans: `docs/plans/2026-07-13-indexed-context-mentions-design.md` and `docs/plans/2026-07-13-indexed-context-mentions-implementation.md`.
- Selected boundary: token-boundary `@query`, host search over validated index metadata, at most 20 file/folder candidates, host revalidation on selection, source snapshot for files, path-only 500-entry/64-KiB manifest for folders, and existing durable context chips.
- Built: deterministic file/folder ranking and provenance; host search/attach APIs and webview bridge; folder context kind with path-manifest validation; inline compact picker; ready/stale/missing disclosure; ArrowUp/ArrowDown/Enter/Tab/Escape behavior; revised industry-familiar `Ask anything, @ to attach context, / for actions` placeholder.
- Causal `npm run test:context` PASS: exact filename ranking, derived folder results, 20-result cap, contained folder capture, path-only manifest, source-sentinel exclusion, outside/tampered/binary/oversized/total-budget negatives, session persistence, and required prompt injection.
- Extension-host E2E PASS in 142s: mention commands registered, live fixture index search ranked the exact file, folder attachment returned metadata only, forged traversal was rejected against the validated index, and full host regressions exited 0.
- Visual PASS: Playwright renders a compact 384px desktop picker and available-width sidebar picker with file/folder icons, provenance, existing chips, and highlighted keyboard selection. The smoke test presses ArrowDown+Enter and proves the highlighted file path reaches the host while the incomplete mention token clears. Screenshots: `artifacts/visual-smoke-context-mentions.png`, `artifacts/visual-smoke-context-mentions-sidebar.png`.
- Release matrix PASS in 129.4s: compile, static, context, index, approvals, modes, sessions, support, readiness, progress, real Edge browser validation, difficult-proof offline contract, workers 100/100, visual, and diff check.
- First installed `@src` inspection exposed irrelevant `.tmp` proof workspaces as duplicate folder candidates. `.tmp` is now an index exclusion, the causal index fixture includes a proof-workspace leak sentinel, and focused index/context/static tests pass after the correction.
- Delivery PASS: corrected `forge-agent-0.84.0.vsix`, 152 files, 3,190,399 bytes; forced Antigravity install reports `kennyg.forge-agent@0.84.0`. After reload/rebuild, the installed index contains 140 files, 2,105 symbols, and zero `.tmp` paths. Screenshot: `artifacts/installed-index-clean-084.jpg`.
- Honest boundary: `@` searches files/folders, not symbols, diagnostics, images, remote URIs, or semantic neighbors. Folder manifests provide paths and rely on agent search/read tools for source. Mentions require a built index; stale candidates are labeled and still revalidated against current filesystem state. Live weak-model uplift attributable to mentions remains unmeasured.
- Suggested commit title: `Phase 84: add indexed file and folder mentions`.

# Phase 85 - Governed Browser And Computer Use - PASS

- Plans: `docs/plans/2026-07-13-governed-browser-computer-use-design.md` and `docs/plans/2026-07-13-governed-browser-computer-use-implementation.md`.
- Selected boundary: loopback-only Playwright browser inspection/action plus Windows-only exact-allowlist UI Automation inspection/action. Models receive state-bound target IDs, never scripts/selectors/coordinates/process IDs. Computer use defaults off; browser/computer actions always require exact persisted human approval, including under `auto` workspace policy.
- Causal gates PASS from current tree: `npm run compile`; `npm test`; direct `browser-use-smoke.mjs`, `computer-use-smoke.mjs`, `human-approval-smoke.mjs`, `browser-validation-smoke.mjs`, and `mode-registry-smoke.mjs`. Browser proof used installed Edge and covered click/fill, remote rejection, forged target, and consumed-state replay. Desktop proof used a purpose-built WPF window and covered invoke/set-value, screenshot, disabled/wrong allowlist, forged target, replay, and coordinate-action rejection. Approval proof reports `externalActionsAlwaysAsk: true` with zero pre-approval worker execution.
- Broad no-spend regression PASS: progress, provider readiness, support privacy, durable sessions, difficult-proof offline contract, workspace index, composer context, workers 100/100, and desktop/sidebar visual smoke. No paid OpenRouter call ran.
- Extension-host E2E PASS in 163.3s; the host briefly reported unresponsive/responsive during existing heavy fixtures and exited 0.
- Package PASS: `forge-agent-0.85.0.vsix`, 155 files, 3,206,318 bytes, SHA-256 `C6C32A42E9FCB78F9BF1E613B05FED04A8640AE7B1624738413BC1209A88E4EA`.
- Install/runtime PASS: Antigravity forced install succeeds and lists `kennyg.forge-agent@0.85.0`; installed `loop.js`, `computerUse.js`, and manifest markers prove both tool families, native policy, and `ALWAYS_APPROVAL_TOOLS = browser_action, computer_action`.
- Installed visual PASS: direct inspection of the actual Antigravity IDE opened registered Forge Studio, exposed the compact bottom composer and run/evidence controls, and showed Settings with OpenRouter `READY`, secret-storage credential, and 345 live models. No cloned editor, terminal, file tree, or permanent browser/computer panel returned.
- Honest boundary: browser actions remain loopback-only; computer actions are Windows/UIA/exact-title only. Standard UIA patterns worked on the WPF fixture; the first WinForms fixture exposed legacy panes without Invoke/Value patterns and was correctly replaced rather than counted as proof. External interaction evidence cannot satisfy the composite code oracle.
- AAR: sustain state-bound one-action contracts, unconditional external approval, and separate evidence. Improve future fixture selection by using UIA-native controls. The next product boundary is Phase 86: route ordinary conversation into this existing authority without creating a parallel tool loop.
- Suggested commit title: `Phase 85: add governed browser and computer use`.

# Phase 86 - Unified Conversational Agent - PASS

- PLAN/RECONCILE/DOCUMENT: `docs/plans/2026-07-13-unified-conversational-agent-design.md` and `docs/plans/2026-07-13-unified-conversational-agent-implementation.md` record the contract before product edits. Reconciliation proved the defect was two submission paths, while the existing harness already owned all mutation, approval, verification, evidence, and terminal gates.
- IMPLEMENT: added deterministic `ConversationController`; registered `forge-agent.submitMessage`; routed answer/start/continue/steer/clarification/approval/pause/resume/cancel/status/research through trusted state; factored serialized run scheduling; promoted bounded chat/context into the run session; retained legacy bridge names as host-only aliases. The webview now emits only `submit-message` and has one composer send affordance with contextual pause/resume.
- CAUSAL/NEGATIVE PASS: `node scripts/conversation-controller-smoke.mjs` proves natural language -> real harness mutation -> same-session green evidence, advisory no-mutation, red oracle no-success, mode ceilings, pending-gate precedence, terminal non-resurrection, direct cancellation, and persisted boundary cancellation before another provider call.
- REGRESSION PASS: `npm run compile`; `npm test`; progress, approval, session, mode, readiness, context, and index smoke scripts; real Edge browser use/validation; WPF UI Automation; workers 100/100. No paid OpenRouter call ran.
- VISUAL PASS: `npm run test:visual`; inspected `artifacts/visual-smoke-run.png`. The normal composer contains one submit action and no Play/single-step controls; progress and approval remain inline.
- EXTENSION HOST PASS: final `npm run test:e2e` exited 0 in 191.9s and exercised the registered unified command's ambiguity and non-mutating mode ceiling in the real Extension Development Host.
- REVIEW: found a concurrency defect where pause/cancel could call `runStep` during an in-flight provider wait. Fixed with serialized scheduling and durable `cancelRequested` control consumed at the next safe boundary. Added a zero-provider-call regression. `git diff --check` passes apart from line-ending warnings.
- PACKAGE PASS: `forge-agent-0.86.0.vsix`, 156 files, 3,209,902 bytes, SHA-256 `83E788F464FA4D8689B12CF6352DA9B13205DCC6A008845071201C9986461DA1`.
- INSTALL PASS: forced installation succeeds in VS Code and Antigravity; both list `kennyg.forge-agent@0.86.0`. Installed controller, command, and webview markers are present.
- INSTALLED INTERACTION PASS: a fresh actual Antigravity IDE window opened Forge Studio, exposed one `Send to Forge Agent` action and no composer Play/Step controls, submitted `This could be useful.`, and visibly returned `Should Forge change the workspace, or are you asking for a read-only answer?` with `Forge route: clarify intent.` Screenshot: `artifacts/installed-unified-conversation-response-086.png`.
- Honest boundary: installed paid model-driven implementation was not run. Deterministic routing is conservative and can ask more often than a future bounded structured classifier. Phases 87-91 remain open.
- AAR: sustain host-owned route authority, single-loop mutation, same-session proof, and no-spend causal fixtures. Improve cancellation tests whenever scheduling changes. The voice icon was briefly activated during accessibility-index drift; it failed/was denied and caused no provider or workspace action. Use fresh state or stable coordinates for installed webview interaction.
- Suggested commit title: `Phase 86: unify conversation with the governed agent loop`.

# Phase 87 - Governed MCP And Weak-Worker Tool Execution - PASS

- PLAN/RECONCILE/DOCUMENT: `docs/plans/2026-07-13-governed-mcp-design.md` and `docs/plans/2026-07-13-governed-mcp-implementation.md` define the local-stdio/loopback boundary, exact host policy, one generic `external_tool` proposal, SecretStorage credentials, immutable evidence, and strong-Architect/weak-worker proof before product edits.
- IMPLEMENT: added the official MCP SDK v1 gateway, Ajv schema validation, exact discovery-policy intersection, role/scope/path/side-effect/approval enforcement, bounded output and process lifetime, credential injection/redaction, MCP evidence/counters, harness/firewall/mode integration, native commands, sanitized catalog artifact, and one collapsed Settings row.
- CAUSAL/NEGATIVE PASS: `npm run test:mcp` discovers four configured fixture tools and hides undeclared tools; rejects malformed schema payloads, role mismatch, traversal, stale policy, forged approval, dangerous object keys, and output overflow; redacts a fixture credential; proves pre-approval workspace immutability; and runs separate `strong-planner` Architect plus `weak-worker` Editor sessions through two MCP calls to an approved workspace write and green composite oracle.
- REGRESSION PASS: `npm run compile`; `npm test`; real Edge browser validation/use; WPF UI Automation; approval, conversation, readiness, modes, sessions, context, index, progress, support, and worker stress 100/100. `npm audit --omit=dev` reports zero vulnerabilities. No paid OpenRouter call ran.
- VISUAL PASS: `npm run test:visual` and inspection of `artifacts/visual-smoke.png` show the compact collapsed external-tools row without a permanent MCP panel.
- EXTENSION HOST PASS: final bundled `npm run test:e2e` exited 0 in 173.4s. It exercised registered MCP commands, official-SDK stdio discovery, SecretStorage credential handling, sanitized native catalog opening, and the existing real-browser command. Defects found and fixed: Electron fixture launch needed `ELECTRON_RUN_AS_NODE=1`; bundling changed `workerHost.js` resolution; bundled Playwright required an explicit `browsers.json` runtime asset.
- PACKAGE PASS: `forge-agent-0.87.0.vsix`, 1,795,285 bytes, 50 runtime files from `vsce ls --no-dependencies`, SHA-256 `172C064AC826C3D9CDB69C2C9DC6EFAEA1A5EC80B0A021C982773632F65000B2`. The extension host is bundled into `out/extension.js`; no `node_modules` directory is shipped.
- INSTALL PASS: exact VSIX installs in VS Code and Antigravity; both CLIs report `kennyg.forge-agent@0.87.0`. Installed copies contain the browser runtime asset plus governed MCP, `external_tool`, and credential-redaction markers.
- INSTALLED INTERACTION PASS: the first inspection correctly exposed a stale pre-install webview despite correct installed files. After `Developer: Reload Window`, actual Antigravity accessibility exposed `EXTERNAL TOOLS (0 SERVERS · 0 TOOLS)` and the visible Settings view retained OpenRouter `READY` with 345 models. Screenshot: `artifacts/installed-governed-mcp-087.jpg`.
- REVIEW: MCP output cannot mutate state, select or satisfy oracles, declare success, widen roles, introduce tools, or bypass workflow/human approval. Workspace-writing tools checkpoint and re-run composite verification. The Architect never invokes MCP; the weak worker receives the bounded committed handoff and sanitized catalog in a separate session.
- Honest boundary: only local stdio and loopback Streamable HTTP are supported. Persistent workers, parallel fan-out, persistent role worktrees, remote MCP, and rigor-matched per-role economics remain Phases 88-90. The weak-worker proof is scripted and causal; live paid uplift is unclaimed.
- AAR: sustain exact discovery-policy intersection, inert external output, unconditional side-effect approval, and separate role sessions. Bundle-aware subprocess/runtime resolution belongs in extension-host tests. Installed proof must reload hot-installed Antigravity webviews before reading accessibility state.
- Suggested commit title: `Phase 87: govern MCP and weak-worker tool execution`.

# Phase 88 - Persistent Isolated Sub-Agents - PASS

- PLAN/RECONCILE/DOCUMENT: `docs/plans/2026-07-13-persistent-subagents-design.md` and `docs/plans/2026-07-13-persistent-subagents-implementation.md` define task-scoped workers, typed handoffs, immutable routes, cumulative retained staging, independent review, atomic host merge, and fresh active verification before product edits.
- IMPLEMENT: added `SubAgentCoordinator` with bounded Explorer/Architect/Editor/Reviewer/Escalation workers, separate provider sessions, focused context, persistent Git-worktree/filtered-copy staging, cumulative multi-file baselines, Escalation stage transfer, staged oracle remediation, independent Reviewer gating, optimistic all-path conflicts, rollback, cleanup/abandonment, per-role/model metrics, four native artifacts/commands, and one compact `agents workers/merges` webview summary.
- FOCUSED PASS: `npm run test:subagents` returned `pass:true`; direct topology used two workers and one merged handoff; staged-red and conflict negatives remained non-green/non-merged; cumulative repair retained the first edit across a red oracle and merged two files; injected partial merge rolled back; missing and expired staging roots abandoned deterministically; the product fixture terminaled `success` with seven provider calls, four workers, one merge, and active bytes unchanged during review.
- REFLECTION PASS: the two-task A/B remained `reflectionOnSolved=2`, `reflectionOffSolved=0`, `delta=1`, with two honest halts. Staged red results now enter the `red_oracle` reflection path without counting as active green evidence.
- REGRESSION PASS: `npm run build`, `npm test`, browser validation/use, computer use, progress, readiness, modes, support, sessions, difficult proof, human approvals, workspace index, composer context, conversation controller, MCP, and worker-process stress 100/100 all pass. No paid OpenRouter call ran.
- VISUAL PASS: `npm run test:visual` passes. Inspection of `artifacts/visual-smoke.png` and `artifacts/visual-smoke-run.png` confirms the compact conversation/settings product remains intact and worker detail is not expanded into a dashboard.
- EXTENSION HOST PASS: final `npm run test:e2e` exited 0 in 233 seconds and exercised the registered topology/handoff/merge/metrics commands and staged product path in Extension Development Host. Two transient extension-host unresponsive/profiling warnings recovered; Phase 89 retains this as a performance/resource-isolation signal rather than hiding it.
- REVIEW FIXES: initial one-action staging could not support multi-file convergence; staging is now cumulative. Reviewer calls were absent from totals; they now reconcile. Escalation changed the Editor route in place; it now transfers the retained stage to a distinct worker. Staged-red evidence was omitted from remediation and reflection; both now receive it. Expired/missing roots now clean up and record abandonment.
- PACKAGE PASS: `forge-agent-0.88.0.vsix`, 1,816,851 bytes, 52 runtime paths from `vsce ls --no-dependencies`, SHA-256 `604E14C36202AF1C37052B668BB1937BE82215E852D5D8F199650FB2574D4006`. Packaging retained the pre-existing missing-LICENSE warning.
- INSTALL PASS: exact VSIX installs in VS Code and Antigravity; both CLIs report `kennyg.forge-agent@0.88.0`. Installed runtime files contain `Sub-agents cannot spawn`, `transferStage`, and `stagedOracleGreen` markers.
- INSTALLED INTERACTION PASS: in the actual Antigravity IDE, `Forge Studio: Open Panel` opened from the native command palette after reload. The installed compact unified conversation rendered with the `agents 0/0` footer. Screenshot: `artifacts/installed-persistent-subagents-088.jpg`.
- Honest boundary: persistent retained staging covers native `apply_patch`/`write_file`. Workspace-writing MCP retains Phase 87 checkpoint/approval transactions. Mutating workers are sequential; kernel/container/socket isolation and a live paid role-economics A/B remain unclaimed.
- AAR: sustain host-owned topology, immutable worker routes, cumulative staging, independent review, and fresh active verification. Improve extension-host responsiveness and runtime ceilings in Phase 89. A red oracle is normal repair state and must not destroy useful staged work. Escalation must preserve route-accounting integrity by creating a new worker identity.
- Suggested commit title: `Phase 88: add persistent isolated sub-agents`.

# Phase 89 - Stronger Runtime Isolation - PASS

- PLAN/RECONCILE/DOCUMENT: `docs/plans/2026-07-13-runtime-isolation-design.md` and `docs/plans/2026-07-13-runtime-isolation-implementation.md` define explicit process, Node-permission, proven OS-sandbox, and strict-unavailable grades. Environment reconciliation found Node 24 permission support but no usable WSL distribution, Docker/Podman image, bubblewrap, or proven non-elevated socket backend.
- IMPLEMENT: added host-owned command authority classification, expected-authority mismatch rejection, Node permission-caged native workers, child-process denial, 384 MiB memory and 2 MiB output ceilings, timeout/truncation/process-tree evidence, optional non-installing backend probes, strict-unavailable rejection, standalone worker bundling, `.forge/runtime-isolation.json`, and `Forge Agent: Open Runtime Isolation Report`.
- FOCUSED PASS: `npm run test:isolation` reports nine classification/bypass cases, forged mismatch rejection, inline-interpreter strict-unavailable rejection, permission-caged workspace write, child denial, bounded result output, timeout, and descendant termination. Node permission probe reports filesystem restriction available and process authority limited; Docker/Podman remain unavailable without `FORGE_SANDBOX_IMAGE`.
- REGRESSION PASS: `npm run build`; `npm test`; persistent sub-agents; worker-process stress 100/100; real Edge browser validation/use; WPF computer use; conversation controller; MCP; and all existing no-spend workflow/readiness/approval/session/mode/context/index/support gates pass. No paid OpenRouter call ran.
- VISUAL PASS: `npm run test:visual` passes with no product-layout expansion.
- EXTENSION HOST PASS: final compiled `npm run test:e2e` exited 0 in 247.9 seconds. A transient extension-host unresponsive/profile warning recovered. Earlier failures correctly exposed stale compiled assertions and deterministic `get_diff` attempting to spawn Git inside a child-denied worker; compilation and trusted coordinator execution fixed both.
- REVIEW: network-write remains denied. Unknown/network-read require a proven socket-isolating backend and therefore reject on this host. Known local verify/workspace commands receive process/resource or transactional bounds but are not represented as network-contained. Browser/computer tools retain their dedicated policies and approvals.
- PACKAGE REVIEW: the first VSIX was 12.19 MB/2,648 files because `.kilo/node_modules` bypassed the shallow exclusion. `.vscodeignore` now excludes nested `**/node_modules/**`; the corrected package has no `node_modules` entries and retains `.kilo/agent-manager.json` only.
- PACKAGE PASS: `forge-agent-0.89.0.vsix`, 54 runtime paths from `vsce ls --no-dependencies`, 3,102,603 bytes, SHA-256 `A72FBE24F9905F9C11808D069F1919289AC76CE6428E84FDB11AB611505D84BD`. It contains `out/workerHost.js`, `out/harness/runtimeIsolation.js`, and no dependency tree. The pre-existing missing-LICENSE warning remains.
- INSTALL PASS: VS Code and Antigravity both list `kennyg.forge-agent@0.89.0`; installed copies contain the standalone worker and runtime-isolation modules. The correct Antigravity IDE CLI is `C:\Users\Moshi\AppData\Local\Programs\Antigravity IDE\bin\antigravity-ide.cmd`; the separate lowercase `antigravity\Antigravity.exe` is the hub and was the cause of the first failed attempt.
- INSTALLED INTERACTION PASS: after `Developer: Reload Window`, actual Antigravity displayed `Forge Agent: Open Runtime Isolation Report` in the native command palette. Invoking it reached the installed Forge command and correctly reported that `.forge/runtime-isolation.json` did not exist before a governed 0.89 run. This proves registration/activation, not a fabricated report-open claim.
- Honest boundary: no live paid network or model-economics run was performed. No socket-isolating backend is available/configured on this host, and process grade is not a kernel/container sandbox. Mutating sub-agents remain sequential.
- AAR: sustain explicit effective guarantees, fail-closed strict execution, standalone runtime assets, and package-content inspection. Improve E2E responsiveness and context load in Phase 90. Compile before bundled E2E and keep coordinator-only deterministic operations outside child-denied workers.
- Suggested commit title: `Phase 89: enforce runtime isolation grades`.

# Phase 90 - Context And Model Optimization - PASS

- PLAN/RECONCILE/DOCUMENT: `docs/plans/2026-07-13-context-model-optimization-design.md` and `docs/plans/2026-07-13-context-model-optimization-implementation.md` define bounded symbol/image context, exact-model profiles, opt-in explicit worker pools, immutable routing, and a same-rigor topology comparison before edits.
- IMPLEMENT: added symbol candidates and bounded declaration/reference neighbors; governed PNG/JPEG/WebP attachments; transient multimodal provider parts; vision capability rejection; exact-model/role/context prompt budgets; sanitized context/routing artifacts; opt-in least-expensive-capable Explorer/Editor routing; native report commands; and a plan-big/execute-small product A/B.
- FOCUSED PASS: context optimization proves 16K/32K/128K+ monotonic bounded profiles, required-section-preserving compaction, zero provider calls for non-vision images, and one transient multimodal call for vision. Composer context proves six attachment paths with metadata-only persistence. Workspace index reports 321/321 cases.
- TOPOLOGY PASS: both rigor-matched scripted lanes solved with seven calls, zero failures, zero fallback, `actuallyModelDriven:true`, green evidence, and reviewed diffs. Solo frontier reports `$0.0140`; plan-big/execute-small reports `$0.0102`; delta `$0.0038`. This is scripted causal/accounting evidence, not a live provider-economics claim.
- REGRESSION PASS: compile, static tests, workspace index, composer context, context optimization, topology, persistent sub-agents, runtime isolation, governed MCP, browser validation/use, computer use, conversation controller, and worker-process stress 100/100 all exit 0. No paid OpenRouter call ran.
- EXTENSION HOST PASS: final `npm run test:e2e` exited 0 in 233.3 seconds. It exercised all four Phase 90 native commands. Two extension-host unresponsive/responsive warnings recovered; a marketplace fallback error did not fail the suite.
- VISUAL PASS: `npm run test:visual` passes. Manual inspection of `artifacts/visual-smoke-context-mentions-sidebar.png` and `artifacts/visual-smoke-composer-context-sidebar.png` confirms the compact sidebar remains unclipped and image controls appear only in the temporary attachment menu.
- PACKAGE PASS: `forge-agent-0.90.0.vsix`, 3,119,583 bytes, SHA-256 `9B3F84B0BBA3144F07BCD294AB6DD88702A491A3410AE1D82E96550EAF7F7B48`. Package inspection confirms `out/extension.js`, `out/workerHost.js`, `out/harness/topologyEval.js`, `out/harness/modelRouting.js`, and `out/harness/contextOptimization.js`. The pre-existing missing-LICENSE warning remains.
- INSTALL PASS: forced installation succeeds in VS Code and Antigravity; both list `kennyg.forge-agent@0.90.0`.
- INSTALLED INTERACTION PASS: after an actual Antigravity `Developer: Reload Window`, the native palette exposed both Phase 90 topology commands. Invoking the exact Run command generated `.forge/evals/latest-plan-big-execute-small.json` at `2026-07-13T09:03:10.082Z`; invoking the exact Open command displayed that JSON in a native Antigravity editor tab.
- REVIEW: persisted image records contain metadata/digest/provenance, not raw bytes. Non-vision image routes reject before provider use. Cost-aware routing is off by default, requires an explicit pool, and cannot replace Architect/Reviewer bindings. Scripted/custom providers without a capability API retain deterministic text-only compatibility; unknown image capability fails closed.
- Honest boundary: live model-written compaction, retrieval uplift, and paid plan-big/execute-small economics remain unproven. Phase 91 owns the 15-25 task benchmark and release floors.
- AAR: sustain exact command filtering for installed interaction, report-content verification, role-authority preservation, and claim-level separation. The first fuzzy command search selected an unrelated Go command; the first ambiguous palette selection produced no report and was not counted. E2E responsiveness remains a release-discipline signal.
- Suggested commit title: `Phase 90: optimize context and model routing`.

# Phase 91 - Production Benchmark And Release Discipline - IMPLEMENTED; LIVE FLOOR PENDING

- PLAN/RECONCILE/DOCUMENT: `docs/plans/2026-07-13-production-benchmark-design.md` and `docs/plans/2026-07-13-production-benchmark-implementation.md` select one wrapper over the existing fixed Tier 2/3/4 runner. The combined suite has 16 diverse disposable tasks and preserves runner-only held-out judges.
- IMPLEMENT: added canonical suite/input/judge SHA-256 digests, complete-suite identity validation, equal-lane input records, immutable final archives, bare/harness/model-driven/false-success/provider-failure/cost/wall-clock accounting, versioned benchmark floors, and separate `benchmarkPassed`/`releaseReady` status.
- SPEND/SELECTION GATES: product and CLI accept only `qwen/qwen-2.5-7b-instruct`, exactly 16 tasks, 4-12 harness steps, 15-120 second call timeout, and explicit action-time credit consent. Frontier substitution, missing consent, and cherry-picked subsets reject before readiness/provider work.
- FABLE/MYTHOS PROMPT: exact Claude Fable 5/Mythos 5 slugs receive one concise required host profile for action readiness, scope restraint, tool-grounded status, genuine ask boundaries, reversible continuation, and no reproduced reasoning. Other models are unchanged; deterministic authority remains intact. Source guidance: Anthropic Claude Platform Fable 5 prompting guide, accessed 2026-07-13.
- FOCUSED PASS: `npm run test:production-benchmark` validates 16 tasks and suite digest `6d81750ab8aef18b64e27f39d3a0199bb4bed74937d548f535ccdd4e20bada09`; rejects drift, duplicates, partials, no consent, frontier substitution, 15-task subsets, false success, no uplift, and scripted claims; proves duplicate archive writes preserve first bytes. `npm run test:model-prompts` passes Fable/Mythos positives and non-target negatives.
- REGRESSION PASS: the full no-spend matrix exited 0 in 143.9 seconds: compile, static invariants, production benchmark, 321/321 workspace index, six composer attachments, model/context optimization, topology A/B, persistent sub-agents, runtime isolation, governed MCP, real browser validation/use, WPF computer use, conversation controller, and worker stress 100/100. No paid provider call ran.
- VISUAL PASS: desktop and 520px `artifacts/visual-smoke-production-benchmark*.png` show a collapsed-by-default control, independent credit checkbox, contained actions, and honest `Benchmark floors` versus `Release ready` state without restoring a permanent dashboard.
- EXTENSION HOST PASS: final post-Fable `npm run test:e2e` exited 0 in 211.6 seconds and exercised both registered benchmark commands plus frontier, consent, and subset rejection before provider work. Two unresponsive/responsive cycles recovered and remain a performance warning.
- PACKAGE PASS: final document-closed `forge-agent-0.91.0.vsix` contains 61 files and is 3,131,408 bytes with SHA-256 `B98A7AF98D7839C2EA5B0AAECD6F423B0F2BECC9907EFE9AE46C13D35D010FEE`. It contains `out/extension.js`, `out/workerHost.js`, `out/harness/productionBenchmark.js`, `out/harness/modelPromptProfile.js`, and webview assets. The pre-existing missing-LICENSE warning remains.
- INSTALL/INTERACTION PASS: VS Code and Antigravity both report `kennyg.forge-agent@0.91.0`. After `Developer: Reload Window`, actual Antigravity visibly exposes the collapsed Production Benchmark control and both native commands. Invoking Open Report reaches Forge and correctly reports that `.forge/evals/latest-production-benchmark.json` does not exist yet.
- Honest boundary: the live 16-task benchmark was not run because provider-credit spend was not authorized. No fresh live solve rate, uplift, cost, `benchmarkPassed`, or `releaseReady` claim exists. Implementation/install is complete; live release-floor evidence remains pending.
- AAR: archive immutability must become true only after successful exclusive creation; metric names must describe what they measure; fixed-suite identity is part of release policy; installed command visibility is not a live benchmark result; and recovered host stalls remain evidence.
- Suggested commit title: `Phase 91: enforce production benchmark release floors`.

# Phase 92 - Prompt Enhancement And MCP Onboarding - PASS

- PLAN/RECONCILE/DOCUMENT PASS: inspected installed Kilo Code 7.4.5 in actual Antigravity across Models, Providers, Agent Behaviour (Agents/MCP/Rules/Workflows/Skills), Auto-Approve, Browser, Checkpoints, Display, Autocomplete, Notifications, Context, Commit Message, Indexing, Experimental, Language and About. `docs/KILO_FEATURE_SETTING_AUDIT.md` records ADOPT/EQUIVALENT/HOST/REJECT decisions before code edits.
- IMPLEMENT PASS: `src/harness/promptEnhancer.ts` uses one exact host setting, strict six-field JSON schema, deterministic rendering, pre-spend input bounds and malformed/extra/oversized output rejection. The webview preserves the original on failure, displays model/cost and never auto-submits. MCP add/replace/remove commands reuse strengthened gateway validation; raw secret argument names and remote HTTP reject; `forge.mcpTimeoutMs` is bounded 1,000-120,000 ms.
- FOCUSED PASS: final `npm run test:prompt-enhancement` exited 0 in 13.9s. It proves one exact cheap-model call, no fallback, structured rendering, pre-provider empty/12,001-character rejection, malformed/extra-field rejection, no auto-submit, MCP add/replace/remove, remote rejection, secret-argument rejection and role-policy rejection.
- STATIC/VISUAL PASS: `npm test` exited 0 in 2.0s. Initial `npm run test:visual` exited 0 in 11.6s; screenshot inspection found a stale-model label and inherited checkpoint popover. After repair, `npm run compile; npm run test:visual` exited 0 in 18.3s. Proof: `artifacts/visual-smoke-prompt-enhancement.png`, `artifacts/visual-smoke-prompt-enhancement-sidebar.png`, `artifacts/visual-smoke-mcp-onboarding.png`, and `artifacts/visual-smoke-mcp-onboarding-sidebar.png`.
- AFFECTED MATRIX PASS: governed MCP, conversation-controller, model-profile, prompt-enhancement and 100-worker suites all exited 0 in one 136.4s batch. Conversation remained causal/non-mutating where required; worker stress was 100/100 with all children exited.
- EXTENSION HOST PASS: `npm run test:e2e` exited 0 in 219.5s. It exercised validated MCP add/replace/remove persistence, remote rejection and host-owned enhancement-model set/get/rejection in addition to existing extension-host gates. One unresponsive/responsive cycle recovered and remains a performance warning.
- PRELIMINARY PACKAGE/INSTALL PASS: `npm run package` plus forced VS Code/Antigravity install exited 0 in 76.0s. The preliminary `forge-agent-0.92.0.vsix` contains 62 files and about 2.99 MB; the pre-existing missing-LICENSE warning remains.
- ACTUAL ANTIGRAVITY PASS: after reload, the command palette exposed `Forge Agent: Enhance Prompt` and `Forge Agent: Add Governed MCP Server`; installed Forge Settings reported 345 live models, selected `google/gemini-2.5-flash-lite`, and exposed collapsed Add/Remove/Refresh/Open/Configure MCP actions. The installed Run composer described no-auto-submit behavior, and clicking Enhance with an empty draft showed `Enter a prompt before enhancing it.` before provider work.
- HONEST BOUNDARY: no paid enhancement call and no Phase 91 benchmark call ran. Phase 92 proves deterministic integration and installed interaction, not live prompt quality. Phase 91 remains pending explicit provider-credit authorization.
- REVIEW/AAR: fixed inappropriate generic Fable tool-loop prose in the enhancement request, cancellation-to-empty MCP config, stale configured-model rendering and contaminated visual state. Reuse strict host authority; never infer product proof from a cosmetic control.
- FINAL ARTIFACT: `forge-agent-0.92.0.vsix`, 3,140,076 bytes, SHA-256 `B59C5999D4D505EFA1BFB46EA9C2F8AA94699165C07919E96E6A0A8845D11E02`, 62 files. Package inspection includes `out/extension.js`, `out/workerHost.js`, `out/harness/promptEnhancer.js`, `out/harness/mcpGateway.js`, webview assets, `ROADMAP.md`, and `package.json`; all package/lock versions are `0.92.0`.
- INSTALL NOTE: VS Code registered `kennyg.forge-agent@0.92.0`. The Antigravity CLI printed `Extension 'forge-agent-0.92.0.vsix' was successfully installed.` and then terminated with native V8 exit `134` during shutdown (`antigravityAnalytics` was not registered). Direct inspection of `C:\Users\Moshi\.antigravity-ide\extensions\kennyg.forge-agent-0.92.0` confirms manifest version `0.92.0` plus installed `out/extension.js`, `out/harness/promptEnhancer.js`, and `out/harness/mcpGateway.js`. This abnormal host CLI exit is recorded rather than hidden; actual installed interaction remains proven by the running Antigravity IDE smoke above.
- Suggested commit title: `Phase 92: add governed prompt enhancement and MCP onboarding`.
